# Seed-local ignores. node_modules/ and dist/ are also covered by the repo root
# .gitignore, but .astro/ (Astro's generated type cache) is not — keep it here.
node_modules/
dist/
.astro/

# vend project-state, incl. the generated `.vend/work-graph.svg` board the /board
# route renders — generated by `vend svg`, never committed (the seed ships honest-empty).
.vend/
