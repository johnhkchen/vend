node_modules/
baml_client/
*.tsbuildinfo
dist/
.lisa-layout.kdl

# Runtime telemetry stays ignored — but keep the steering decision log,
# which is durable training data (see docs/knowledge/playbooks/steering-data-model.md).
.vend/*
!.vend/decisions.jsonl
!.vend/forward-e1.jsonl

# Secrets / local env — managed via Doppler (see README). Never commit real values.
.env
.env.*
!.env.example
.doppler.env
.lisa-commit.lock
