---
id: T-042-04
story: S-042-02
title: doctor-cast-precondition-guard
type: task
status: open
priority: high
phase: done
depends_on: [T-042-02]
---

## Context

Reuse the doctor check as a precondition before a cast (mirror lisa's check_required_deps-before-run_loop) so a cast against a broken environment refuses cleanly at the door instead of crashing mid-run after a budget is committed.

_Advances: P3, P4, P7_

## Acceptance Criteria

- [ ] Test: invoking a cast (castWork) in an env with a broken dep refuses BEFORE any budget is spent — emitting the same named-check + hint refusal and a non-zero outcome — with no partial metered run; a wired env proceeds unchanged.
