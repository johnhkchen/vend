---
id: T-059-03
story: S-059-02
title: gold-master-re-drive-on-the-corrected-seed
type: feature
status: open
priority: high
phase: done
depends_on: [T-059-01, T-059-02]
---

## Context

**Re-prove the Set-B channel live — the corrected gold master (LIVE, the one metered cast of this epic).**
With the seed intent + tuned charter wired (T-059-01, T-059-02), re-run the E-058 drive end-to-end on a fresh
hackathon seed and capture the corrected outcome. This turns E-058's negative gold master into a closed loop.

- Drive the shipped flow: clone/instantiate `examples/templates/hackathon-seed/` → `lisa init` → `vend init
  --template hackathon` (green `vend doctor`) → `vend steer`.
- Confirm `vend steer` now stages a **real, grounded board** under `docs/active/pm/staged/steer.md` — ≥1
  signal traceable to the seed's one-line `SEED.md` ("a team-finder page…") — **no longer honest-empty**.
- Render the staged board on the **SVG surface** (`vend svg`, E-055/E-056) beside the running Astro preview —
  the designer's window onto the work.
- Re-capture `examples/templates/hackathon-seed/EXPECTED-OUTCOME.md` as a **positive, re-runnable** gold
  master (real numbers, host/model/spend, the re-run block) and accrue a forward-E1 record.

**Scope = the board-renders proof (the make-or-break).** A *full* slice clear (decompose → work) stays gated
on E-058 finding #3 (`codebase-memory-mcp` absent in a fresh seed) and is OUT OF SCOPE — record honestly
where the drive stops, do not paper over it. Budget shape (finding #2) is likewise a separate signal; fund
the steer cast appropriately for this run and note the envelope. The human running the drive **authorizes
the metered spend (P7).**

**Cites:** `examples/templates/hackathon-seed/` (the seed under proof); `examples/templates/hackathon-seed/
EXPECTED-OUTCOME.md` (the gold master to re-capture); `docs/active/work/T-058-05/` (the prior negative
drive — the comparison baseline); `src/present/svg-file.ts` (`vend svg`); E-058; `docs/active/epic/E-059.md`.

## Acceptance Criteria

- [ ] A live drive of the shipped flow stages a **non-empty, grounded** steer board off the seed's `SEED.md`
      (≥1 signal tracing to the one-line idea); the board renders on the SVG surface beside the app.
- [ ] `EXPECTED-OUTCOME.md` is re-captured as a positive, re-runnable gold master (real spend/host/model + a
      re-run block) and a forward-E1 record is accrued. The make-or-break (A3) is recorded as **closed**.
- [ ] Honest boundaries recorded: where the drive stops short of a full slice clear (finding #3) and the
      budget envelope used (finding #2) are stated plainly, not papered over.
- [ ] No `src/` change in this ticket (it measures the corrected artifact); the spend is human-authorized (P7).
