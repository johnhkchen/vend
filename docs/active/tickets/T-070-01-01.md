---
id: T-070-01-01
story: S-070-01
title: run-log-seat-defaulted-marker-schema
type: task
status: done
priority: medium
agent: codex
phase: done
depends_on: []
---

## Context

Add the seat-defaulted degradation marker to the run record schema — the append-only-ledger foundation every consumer of the disposition flip writes through.

_Advances: P4 — Autonomy by default, not supervision; P6 — Executor-agnostic underneath_

## Acceptance Criteria

- [ ] A `src/log/run-log.test.ts` unit test: a `RunRecordInput` carrying a `seatDefaulted` marker (requested raw seat, applied default, reason) serializes and revives byte-stable, and an input WITHOUT it produces a record byte-identical to a pre-E-070 record (the one-way/absent contract that `overEnvelope`/`reducedGrounding` already follow) — green under `bun test src/log/run-log.test.ts`.
