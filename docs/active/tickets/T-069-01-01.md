---
id: T-069-01-01
story: S-069-01
title: agent-seat-contract-and-input-field
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: []
---

## Context

Establish the canonical known-seat list and a pure seat-validation guard, and thread an optional `agent` seat through the decompose input shape mirroring `--after` — the settled field + vocabulary every consumer reads.

_Advances: P6 — Executor-agnostic underneath; P2 — The run is two gestures; E-069-done_

## Acceptance Criteria

- [ ] A new addon-free unit test passes: `findUnknownSeat("gpt")` returns `"gpt"` and `findUnknownSeat("claude")`/`("codex")` return null against a single `KNOWN_SEATS = ["claude","codex"]` constant; and `assembleInputs({epicPath, agent:"codex"})` returns `DecomposeInputs` carrying `agent:"codex"` while an absent flag leaves the field `undefined` (object shape byte-identical to today).
