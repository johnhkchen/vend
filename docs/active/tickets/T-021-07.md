---
id: T-021-07
story: S-021-03
title: one-way-authority-guarantee
type: task
status: open
priority: critical
phase: done
depends_on: [T-021-01, T-021-05]
---

## Context

Prove and enforce the non-negotiable boundary: the projection/render path never writes the canonical graph — reads only, no write-back.

_Advances: P5, one-way-authority_

## Acceptance Criteria

- [ ] An end-to-end test snapshots docs/active/** byte-hashes, runs load→project→render, and asserts every source file is byte-unchanged; a static check fails the build if any presentation module imports a writer/fs-write against docs/active.
