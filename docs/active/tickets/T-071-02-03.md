---
id: T-071-02-03
story: S-071-02
title: effect-infers-and-stamps-default-seat
type: task
status: open
priority: high
phase: ready
depends_on: [T-071-02-01, T-071-02-02, T-071-01-02]
---

## Context

When a board-writing gesture omits `--agent`, infer the cooler seat from the ledger and stamp it as the default while recording the decision; an explicit `--agent` still wins and both-cool stays unrouted.

_Advances: P2 — The run is two gestures; P4 — Autonomy by default, not supervision; P6 — Executor-agnostic underneath; P7 — Budget is a hard contract_

## Acceptance Criteria

- [ ] An effect-level test through a stub executor: no-`--agent` + hot-claude ledger fixture stamps `agent: codex` on every ticket and threads a `seatInferred` marker onto the record; a both-cool fixture mints unrouted byte-identical to today; an explicit `--agent claude` overrides with no `seatInferred` marker; both `decompose` and `chain` exercise the one injection; `bun run check` green.
