---
id: T-058-05
story: S-058-04
title: gold-master-live-drive-on-the-seed
type: feature
status: open
priority: high
phase: implement
depends_on: [T-058-01, T-058-03, T-058-04]
---

## Context

**LIVE: drive the Astro seed end-to-end and capture the gold master — the channel proof.** With the seam
(T-058-01), the seed + wiring (T-058-03), and the visual (T-058-04) in place, run the actual designer drive
against the shipped `SEED.md` and record what a good session yields. This is the **make-or-break A3 risk**:
the first real test that `vend steer`/`survey` produce a coherent board off a *thin non-vend domain seed*
(they were authored against the vend repo; de-risked by the shipped steer self-referential-demotion fix,
E-044 — without it, steer may rank the template's own scaffolding as a target).

**The drive (run it for real, in a throwaway copy of the seed — DO NOT mutate the committed template):**
1. Copy the seed to a sandbox, `lisa init` (if needed) → `vend init --template hackathon` → `vend doctor`
   (assert green).
2. `vend steer` off the shipped `SEED.md` → assert a **coherent ranked board + genuine forks** (not junk,
   not self-referential).
3. `vend work --budget <…>` → clear **≥1 real slice** (a run-log record; a forward-E1 record accrued).
4. Generate the **SVG board** (`vend svg`) and confirm it renders the seed's work-graph (the designer's view).
5. **Capture the real outcome into `examples/templates/hackathon-seed/EXPECTED-OUTCOME.md`** (the gold
   master: the coherent board, the forks, the cleared slice) — comparable + re-runnable for consistency.

**This is a LIVE METERED cast** (real `claude -p` — survey/steer + work), NOT a free proof; the human
running the loop authorizes the spend (P7 authorized, not bypassed). Honest-on-outcome: if steer produces a
weak board off the thin seed, RECORD THAT (it's the A3 finding — the charter/seed need tuning), do not
paper over it. Settle a short verdict (drive log + the captured gold master).

**Codebase-memory (use it — project coding-task suggestion):** confirm the steer/work cast + run-log + the
forward-E1 record with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`) via `search_code`:
- `search_code(pattern: "steer|survey|castWork|spendDown|runs.jsonl|forward|intervened|EXPECTED-OUTCOME", regex: true, mode: "full", path_filter: "^src/(play|engine|log)/")` — the live drive gestures + the run-log/forward-E1 record the verdict cites.

**Cites:** `src/play/steer.ts`/`survey.ts` (the board-from-seed cast); `src/play/work.ts` (`vend work` —
clear a slice); `examples/templates/hackathon-seed/EXPECTED-OUTCOME.md` (the gold master to FILL, from
T-058-03's target stub); E-044 (the steer-demotion that de-risks A3); `docs/active/epic/E-058.md`.

## Acceptance Criteria

- [ ] A real live drive of the seed is run (sandboxed copy — the committed template is NOT mutated):
      `vend init --template hackathon` + `doctor` green; `vend steer` yields a coherent ranked board +
      genuine forks (or the weak-board A3 finding is recorded honestly); `vend work` clears ≥1 slice with a
      run-log record + a forward-E1 record.
- [ ] `examples/templates/hackathon-seed/EXPECTED-OUTCOME.md` is filled from the REAL run (the gold master —
      comparable + re-runnable), and a short drive-log/verdict is settled (honest-on-outcome).
- [ ] The SVG board renders the seed's work-graph (the designer's visual view confirmed end-to-end).
