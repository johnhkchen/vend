---
id: T-068-03-02
story: S-068-03
title: doctor-orphan-check
type: task
status: open
priority: medium
agent: codex
phase: done
depends_on: [T-068-03-01]
---

## Context

Surface the orphan as a `vend doctor` board-hygiene check so doctor goes non-green on a half-minted board instead of staying green on it.

_Advances: P3 — Gates are the contract; P4 — Autonomy by default, not supervision_

## Acceptance Criteria

- [ ] `vend doctor` over a board with an orphan epic emits a red Check naming the epic id + a fix-it hint and exits non-zero; a clean board keeps doctor green (probe test with injected board facts).
