---
id: T-069-01-04
story: S-069-01
title: run-option-and-effect-relabel
type: task
status: open
priority: high
agent: codex
phase: done
depends_on: [T-069-01-02]
---

## Context

Add `agent` to `RunOptions`, have `decomposeEffect` pass the seat into `materialize` and catch `UnknownSeatError` — relabeling it to a named `unknown-seat` run-log outcome (the andon), replacing the supervised hand-edit pass.

_Advances: P4 — Autonomy by default, not supervision; P6 — Executor-agnostic underneath; E-069-done_

## Acceptance Criteria

- [ ] The `RunOutcome` union includes `unknown-seat`; a test drives `decomposeEffect` with `ctx.inputs.agent:"codex"` and observes materialized tickets carrying `agent: codex`, and with an unknown seat the effect returns `{ ok:false, outcome:"unknown-seat" }` with zero files written.
