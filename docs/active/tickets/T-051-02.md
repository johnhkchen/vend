---
id: T-051-02
story: S-051-01
title: route-askuserquestion-denylist-autonomous-casts-only
type: task
status: done
priority: high
phase: done
depends_on: [T-051-01]
---

## Context

Thread an AskUserQuestion denylist through the autonomous-cast path only (propose-epic, decompose-epic, work) so a piped agent finds the interactive tool unavailable and proceeds; interactive/TUI casts emit no flag and stay byte-for-byte unchanged.

_Advances: P4, E-051: autonomy holds end-to-end through the propose→decompose→work chain — an unattended cast never blocks on a prompt it cannot deliver_

**Codebase-memory (use it — project coding-task suggestion):** confirm where a cast resolves its tool-set
with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "dispense|allowedTools|castPlay|DispenseOptions|decompose-epic|propose-epic", regex: true, mode: "full", path_filter: "^src/(executor|engine|play)/")` — the autonomous-cast option path to route the AskUserQuestion denylist through (propose/decompose/work ONLY); confirm interactive/TUI surfaces stay flag-free.

**Cites:** `src/executor/claude.ts` (`dispense`/`buildArgs` — the seam) + T-051-01 (the `disallowedTools`
option); `src/play/decompose-epic.ts` / `src/play/work.ts` (the autonomous casts to route it through);
`docs/active/epic/E-051.md`.

## Acceptance Criteria

- [ ] Deterministic test asserts an autonomous cast's resolved dispense argv carries `--disallowedTools AskUserQuestion` while a non-autonomous/interactive cast's argv carries no such flag (PURE, no live model). Confirmed in the dogfood: the E-049-style decompose that previously stalled on a mid-cast question now completes unattended.
