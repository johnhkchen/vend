---
id: T-074-01-01
story: S-074-01
title: executor-dispensability-probe-on-boundary
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: [T-073-02-02]
---

## Context

Add a near-zero-cost dispensability/reachability probe to the Executor boundary, distinct from PATH presence, so callers can ask 'can THIS environment actually dispense?' rather than 'is the binary on PATH?'.

_Advances: P6 — Executor-agnostic underneath; P5 — Local-first; P3 — Gates are the contract_

## Acceptance Criteria

- [ ] A new `Executor.probe()` (returning a structured `{ ok, reason?, hint? }`) exists on the interface and is implemented by both ClaudeExecutor (verifies the config-store/Keychain is READABLE from this env — no token spend, no dispense) and the openai-compat executor (endpoint/auth reachable); unit tests exercise the ok and not-ok (config-store denied) branches via injected facts, and `dispense` behavior is unchanged (existing claude.test.ts stays green).
