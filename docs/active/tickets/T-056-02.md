---
id: T-056-02
story: S-056-02
title: blocked-flag-on-projection-link
type: task
status: open
priority: high
phase: done
depends_on: []
---

## Context

Enrich ProjectionLink (project.ts:59) with a status-derived `blocked` flag computed purely inside projectGraph/buildLinks from the frozen graph — a link is blocked when its upstream `from` ticket is not done — so the projection IR carries dependency-decision weight without any new data authority.

_Advances: P5, E-056-edge-payload_

**Codebase-memory (use it — project coding-task suggestion):** confirm the link model + `projectGraph` with
the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "ProjectionLink|buildLinks|projectGraph|TicketNode|stateKey|done|status", regex: true, mode: "full", path_filter: "^src/(present/project|graph/model)")` — the bare `ProjectionLink {from,to}` to enrich with `blocked`, computed PURELY in `buildLinks`/`projectGraph` from the frozen graph's ticket status (blocked = upstream `from` not done).

**Cites:** `src/present/project.ts` (`ProjectionLink` :59, `buildLinks` :185, `projectGraph` — add the
`blocked` flag from graph status); `src/graph/model.ts` (the ticket done/status field);
`docs/active/epic/E-056.md`.

## Acceptance Criteria

- [ ] project.test.ts: projectGraph over a fabricated frozen graph emits blocked:true for a link whose `from` is not done and blocked:false when it is done; the input graph is returned reference-unchanged (one-way-authority / authority-guard test passes) and the projection is byte-identical on repeat (no clock/random).
