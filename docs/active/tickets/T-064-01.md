---
id: T-064-01
story: S-064
title: init-template-standalone-no-clone-no-doppler
type: task
status: open
priority: high
phase: ready
depends_on: [T-061-02]
---

## Context

Extend the existing E-058 `vend init --template <name>` seam (NOT a new command) so a brew-installed binary lays a workspace into an empty dir with no repo clone and no Doppler, against a minimal/placeholder template.

_Advances: P5, P6, E-061: `vend init --template <name>` lays down a workspace, no clone, no Doppler, BYO claude login_

## Acceptance Criteria

- [ ] In an empty directory with no checkout and no Doppler env, `vend init --template <name>` writes the workspace files; a test covers the no-clobber converge and the absence of any repo/Doppler dependency.
