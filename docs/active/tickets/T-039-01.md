---
id: T-039-01
story: S-039-01
title: the-live-re-sweep
type: feature
status: done
priority: high
phase: done
depends_on: []
---

## Context

**The metered re-sweep — confirm the macro-wallet CLEARS now the censor is fixed.** E-037 watched it
clear 0 (propose-epic guillotined at 72,785 ms); E-038 gave the per-cast timeout 2× headroom
(`timeoutMsFor`, proven deterministically). Now run it live and watch a pull actually clear.

> **This ticket performs a LIVE, METERED spend** — real `claude -p` casts, real tokens, real
> wall-clock, and it **mints real epics+tickets**. The human running `lisa loop` authorizes it; the
> budget bounds it (P7). NOT a free proof.

Steps:

1. **Deterministic pre-check (free, quick).** Confirm E-038 is live: `timeoutMsFor({timeMs:T})` returns
   `T × 2` (so propose-epic's 72,785 ms envelope now allows ~145 s — the ~72–73 s casualties would
   finish). Confirm the staged `steer.md` (16:02) is **stale** vs the live project (the freshness gate
   will refuse it) so a fresh board is genuinely needed. Affordability still gates on the bare price
   (E-038 isolated). A one-line go.
2. **Stage a FRESH, CONCRETE board (live cast).** `bun run src/cli.ts steer` (or `survey`) → a fresh
   `staged/steer.md`. **Inspect the #1 signal:** the point is to clear *real product work*. The board
   now carries concrete demand (Frontier 7 `vend init`, the frontiers), so #1 should be decomposable.
   **If #1 is again self-referential/meta** (e.g. "run the sweep" — the E-037 degenerate case),
   **record it as a finding** and point the sweep at the top **concrete** signal instead (e.g. via a
   board whose #1 is real product demand) — a degenerate meta-target is not a valid clear-test.
3. **Fund a bounded budget and walk away.** `bun run src/cli.ts work --no-intervened --budget
   3600000,1000000` (~1h/~1M, same as E-037 for comparability ≈ ~2 chains). `--no-intervened` is the
   walk-away test. Let it spend down to a **clean P7 stop**.
4. **Capture** to `docs/active/work/T-039-01/sweep-log.md` (verbatim output): the receipt; **whether
   propose now FINISHES** (no `andon: timed-out` at ~72.8 s — the E-038 fix working live); the
   **cleared pull(s)** (real epic id(s)+tickets; run `lisa validate`, confirm green); and the **first
   CLEARED forward-E1 record** in `.vend/runs.jsonl` (`intervened:false` + `outcome:success`).

**Honest on outcome (E-037's standard):** if it still 0-clears, record exactly where it stopped — and
note the bottleneck moved (e.g. "propose finished in ~90 s ✓ but decompose censored", which still
confirms E-038 worked). A clean 0-clear with a named cause is an honest result, not a failure to hide.
**P7 holds:** the wallet debits real actuals and stops clean; nothing partial.

**Codebase-memory (use it — project coding-task suggestion):** confirm the spend path + the E-038 fix
seam with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via
`search_code`/`query_graph`, not raw grep:
- `search_code(pattern: "timeoutMsFor|TIMEOUT_HEADROOM|castWork|spendDown|renderReceipt", regex: true, mode: "full", path_filter: "^src/(budget|play|engine)/")` — the headroom fix (to confirm live) + the spend/receipt path you're casting.
Confirmed coordinates at authoring: `timeoutMsFor`/`TIMEOUT_HEADROOM` `src/budget/budget.ts`; `castWork`
`src/play/work.ts:147`; `renderReceipt` `src/play/work-core.ts:151`; the CLI `work` arm in `src/cli.ts`.

**Cites:** `src/budget/budget.ts` (`timeoutMsFor` — E-038, the fix to confirm live); `src/play/work.ts`
(`castWork`; `DEFAULT_BOARDS`); `src/cli.ts` (`work`/`steer` arms); `work/T-037-02/sweep-log.md` (the
prior 0-clear + the self-referential-#1 lesson).

## Acceptance Criteria

- [ ] Deterministic pre-check: `timeoutMsFor` returns `T × 2` (E-038 live) and the staged `steer.md` is
      confirmed stale → a fresh board is needed. A one-line go.
- [ ] A **fresh, concrete board** staged (freshness gate passes); the #1 signal recorded — if
      self-referential, noted as a finding and the top **concrete** signal cleared instead.
- [ ] `vend work --no-intervened --budget ~1h,~1M` run live to a clean P7 stop, with **propose
      FINISHING** (no timeout at ~72.8 s — E-038 confirmed live).
- [ ] **≥1 real pull CLEARED** (real epic+tickets, `lisa validate` green, auth==exec held) + the
      **first CLEARED forward-E1 record** (`intervened:false` + `success`) in the ledger — **or** an
      honest record of where it stopped, with the bottleneck named.
- [ ] `sweep-log.md` captures the verbatim steps + receipt + cleared id(s) + ledger delta.
