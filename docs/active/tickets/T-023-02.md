---
id: T-023-02
story: S-023-01
title: converge-or-accept-the-head
type: feature
status: done
priority: high
phase: done
depends_on: [T-023-01]
---

## Context

Run the top-pick stability probe (T-023-01) on **survey** as a bounded sweep (N≈3–5, cost-aware,
logged to `work/T-023-02/sweep-logs/`), read the **head-vs-tail** verdict, then take the
**warranted path** — the measurement decides which, honestly (the E-022 fork discipline):

- **head-stable → accept (no build).** Amend **IA-17** (`information-architecture.md`): survey's
  divergence is *tail re-ordering, by-design* — the **#1 pull is consistent run-to-run**, the
  ranking below it is not promised. The contract gets *more precise*, and survey's "converge"
  verdict is honestly downgraded with evidence. Record the verdict in a findings note.

- **head-flips → converge (bounded build).** Build the **consensus-cast convergence**: cast survey
  N×, use the existing equivalence judge (T-022-01) as a **gate** to converge the N boards to their
  agreed top-ranked signal, and stage a single **consensus board** whose #1 is the agreed pull.
  The judge becomes a gate, not just a meter (the IA-17 contract enforced at survey). Keep the pure
  consensus-selection logic unit-tested; the N-cast + judge-gate sweep is the impure harness. Amend
  IA-17 to record survey as *converged at the head*, with the lever's measured effect.

Surface the resulting state recommendation-first (IA-5): the deciding framing is the human's, the
evidence + the warranted path are ours. Honest about the sample (small N → directional, not proof).

**Cites:** T-023-01 (the head-isolating probe); `work/T-022-02/findings.md` (the 0.00 whole-board
datum + the head-vs-tail caveat); `src/play/survey.ts` (the play to gate, budget 300k);
`src/probe/equivalence.ts` (the judge reused as the consensus gate); `information-architecture.md`
**IA-17** (the contract to amend) + **IA-1** (the single-recommendation home the head feeds); E-014
(the measure-then-decide precedent).

## Acceptance Criteria

- [ ] The probe sweep runs on survey (bounded N, logged); the head-vs-tail verdict lands in a
      findings note at `docs/active/work/T-023-02/`.
- [ ] **The warranted path is taken:** head-stable → **IA-17 amended** to *tail-divergence-by-design*
      (the #1 pull is consistent), no lever; head-flips → the **consensus-cast convergence** lever
      is built (judge-as-gate, consensus board staged) and IA-17 amended to *converged at the head*.
- [ ] Honest about the sample (small N → directional). If a lever is built, its effect is measured
      (the converged board's head is stable across a re-sweep) — no unmeasured claim of success.
- [ ] `bun run check:*` green.
