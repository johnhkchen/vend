---
id: T-070-01-03
story: S-070-01
title: cast-records-and-warns-on-seat-default
type: task
status: open
priority: medium
agent: codex
phase: done
depends_on: [T-070-01-01, T-070-01-02]
---

## Context

Thread the effect's seat-defaulted report onto the run record and emit the honest requested-vs-default warning at cast time — closing degrade-don't-discard with recorded, visible provenance.

_Advances: P4 — Autonomy by default, not supervision; P2 — The run is two gestures_

## Acceptance Criteria

- [ ] An end-to-end cast test through a stub executor: `--agent kodex` materializes the full board, the written run record carries the seat-defaulted marker (requested raw seat + default applied), and cast stdout emits the requested-vs-default warning line (the shape of the reduced-grounding note); `bun run check` green.
