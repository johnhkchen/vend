---
id: T-058-02
story: S-058-02
title: astro-react-cloudflare-frontend-scaffold
type: feature
status: open
priority: high
phase: done
depends_on: []
---

## Context

**The frontend the designer clones — an Astro + React app that auto-deploys to Cloudflare.** Build
`examples/templates/hackathon-seed/` as a real, minimal, runnable frontend stack:

- **Astro + React** starter (Astro with the `@astrojs/react` integration; a couple of React components +
  a page; `package.json`/`astro.config` so `npm install && npm run dev` serves a preview and `npm run
  build` succeeds). Keep it minimal — a hackathon seed, not a product.
- **Cloudflare auto-deploy config present** — a Cloudflare Pages config (`wrangler.toml` / the Pages build
  command + output dir, or a `.github/workflows/deploy.yml` to Pages) so the app **auto-deploys on the
  designer's push**. HONEST BOUNDARY: this is **config + a green build, NOT a live deploy** in this env
  (no Cloudflare creds) — assert the config exists and the build passes, do not attempt a real deploy.
- Self-contained under `examples/templates/hackathon-seed/`; it does NOT affect the vend repo's own build
  (it's example content, not `src/`). Confirm `bun run check` (vend's gate) is unaffected.

(The vend drive wiring — README/SEED/charter/shelf-note/EXPECTED-OUTCOME — is T-058-03, layered into this
same seed dir.)

**Codebase-memory (use it — project coding-task suggestion):** this is new example content, so confirm only
where it must NOT collide — the vend gate + the examples path discipline — with the **codebase-memory-mcp**
(project `Volumes-ext1-swe-repos-vend`) via `search_code`:
- `search_code(pattern: "examples|check:test|bun test|tsconfig|include|exclude", regex: true, mode: "files", path_filter: "^(package.json|tsconfig|bunfig)")` — confirm the seed under `examples/` is excluded from vend's typecheck/test so the frontend stack doesn't pollute the gate.

**Cites:** `examples/templates/hackathon-seed/` (NEW — the Astro + React + Cloudflare frontend); `package.json`/
`tsconfig`/`bunfig` (ensure `examples/` is outside vend's typecheck/test scope); `docs/active/epic/E-058.md`;
`docs/active/pm/brief-hackathon-example.md` (the seed spec).

## Acceptance Criteria

- [ ] `examples/templates/hackathon-seed/` is a runnable Astro + React app: `npm install && npm run build`
      succeeds (a CI/check step or a documented command); a dev-server preview renders a React component.
- [ ] A Cloudflare Pages / `wrangler` auto-deploy config is present and valid in shape (would deploy on
      push) — NOT live-deployed here; the boundary is documented.
- [ ] The seed does NOT regress vend's own gate — `examples/` is excluded from vend's typecheck/test;
      `bun run check` stays green.
