---
id: T-049-03
story: S-049-01
title: branching-worked-example-through-castgraph
type: task
status: open
priority: medium
phase: ready
depends_on: [T-049-02]
---

## Context

Prove the authored predicate threads end-to-end through the impure castGraph shell with a deterministic stub-node branching example in graph-example.ts, confirming a playbook author declares the branch once on the edge and the not-taken subgraph is an observable skip — pure-core/impure-shell split preserved.

_Advances: P1, P6, E-049: proven by deterministic stub-node tests with no live model, green in the existing check gate_

**Codebase-memory (use it — project coding-task suggestion):** confirm the deterministic stub worked-example discipline with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`) via `search_code`:
- `search_code(pattern: "graph-example|diamondExample|runSharedWalletFanout|runPerNodeFanout|stub", regex: true, mode: "full", path_filter: "^src/engine/graph-example")` — the stub-node worked-example pattern (E-046/E-048 precedent) to extend with a branching example; confirm NO test value-imports `graph.ts` (the `castGraph` import discipline).

**Cites:** `src/engine/graph-example.ts` (the deterministic stub example pattern to extend); `src/engine/graph.ts` (`castGraph` — the impure shell the predicate threads through); T-049-02; `docs/active/epic/E-049.md`.

## Acceptance Criteria

- [ ] graph-example.ts gains a branching example cast via castGraph with deterministic stub nodes; graph-example.test.ts proves the authored edge predicate selects the taken branch and the other subgraph lands in skipped end-to-end; no test value-imports graph.ts (the castGraph discipline holds); `bun run build` + full check gate green.
