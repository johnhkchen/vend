---
id: T-071-02-01
story: S-071-02
title: lane-heat-reader
type: task
status: open
priority: high
phase: ready
depends_on: [T-071-01-01]
---

## Context

A pure reader that ranks the two lanes by recent cost-weighted burn and names the cooler seat — or declines when both read cool, so the mint stays unrouted.

_Advances: P4 — Autonomy by default, not supervision; P7 — Budget is a hard contract; P6 — Executor-agnostic underneath_

## Acceptance Criteria

- [ ] `inferDefaultSeat` unit tests: a fixture with one lane clearly hot returns the cooler seat plus a heat reason; both-cool / tied / empty-ledger return null; the lane list is read from `KNOWN_SEATS` (adding a seat needs no reader edit) and burn reuses run-log's `totalTokens`; `bun run check` green.
