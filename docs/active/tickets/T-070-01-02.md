---
id: T-070-01-02
story: S-070-01
title: materialize-degrades-on-unknown-seat
type: task
status: open
priority: high
agent: codex
phase: done
depends_on: []
---

## Context

Flip the disposition: on an unknown seat, omit the `agent:` key and materialize the full board, reporting the requested-vs-default fact instead of throwing — retiring the unknown-seat hard refusal from the effect path.

_Advances: P6 — Executor-agnostic underneath; P2 — The run is two gestures; P4 — Autonomy by default, not supervision_

## Acceptance Criteria

- [ ] A materialize + decompose-effect unit test: casting a fixture plan with `agent: 'kodex'` writes every ticket with NO `agent:` key (byte-identical to the default mint) and returns `ok: true` reporting the seat-defaulted fact (requested 'kodex', defaulted) via `EffectResult` — no `UnknownSeatError` thrown, no `unknown-seat` outcome produced; a valid `agent: 'codex'` still stamps `agent: codex`. Green under `bun test`.
