---
id: T-045-01
story: S-045-01
title: the-live-cadence-sweep
type: feature
status: open
priority: high
phase: research
depends_on: []
---

## Context

**The next metered `--no-intervened` sweep — advance the cadence + confirm the hardening live.** E-039
proved the wallet clears (forward-E1 now 8/10, 4 cleared). E-043 (idempotent mint) + E-044 (concrete-
demand ranker) hardened it and deferred their live confirmation to "the next sweep." This is it.

> **This ticket performs a LIVE, METERED spend** — real `claude -p` casts, real tokens, real minted
> epics. The human running `lisa loop` authorizes it; the budget bounds it (P7). NOT a free proof.

Steps:

1. **Stage a FRESH board (live cast) — and confirm E-044 live.** `bun run src/cli.ts steer` (or
   `survey`) → a fresh `staged/steer.md` (the 17:07 one is stale; the freshness gate refuses it).
   **Record the #1 signal:** it must be **concrete product demand** (e.g. the Frontier 7 hackathon
   example the PM prepped, the multi-node DAG, a real frontier) — **NOT** a self-referential "run the
   sweep" meta-task. **If #1 is still self-referential, E-044 didn't take — record it as a finding**
   (don't silently re-point; the point here is to test E-044).
2. **Fund a bounded budget and walk away.** `bun run src/cli.ts work --no-intervened --budget
   3600000,1000000` (~1h/~1M, the E-039 budget that cleared 2). Let it spend down to a **clean P7
   stop**.
3. **Confirm E-043 live + capture.** After the sweep: confirm **no orphan epics** — no childless
   duplicate of any minted title (the E-041 failure mode); the idempotent mint held. Capture to
   `docs/active/work/T-045-01/sweep-log.md` (verbatim): the #1 signal, the receipt, the cleared
   chain(s) (real epic id(s)+tickets, `lisa validate` green), and the new **forward-E1 records**
   (`intervened:false` + `success`) appended to `.vend/runs.jsonl`.

**Honest on outcome:** a clean stop clearing ≥1 chain is the goal (forward 8 → ≥10); a 0-clear is
recorded with the cause; a still-self-referential #1 or any orphan is a finding (the hardening regressed
or didn't take). **P7 holds:** the wallet debits real actuals, stops clean, nothing partial.

**Codebase-memory (use it — project coding-task suggestion):** confirm the spend path + the two
hardening seams with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready)
via `search_code`/`query_graph`, not raw grep:
- `search_code(pattern: "castWork|spendDown|proposeEpicEffect|findExistingByTitle|renderReceipt", regex: true, mode: "full", path_filter: "^src/play/")` — the spend path + the E-043 idempotent mint to confirm held.
Confirmed coordinates at authoring: `castWork` `src/play/work.ts:147`; `proposeEpicEffect`/
`findExistingByTitle` `src/play/propose-effect.ts` / `id-guard.ts` (E-043); `steer.baml`/`survey.baml`
concrete-demand steering (E-044); `auditWalkAway` `src/ledger/walk-away.ts:160`.

**Cites:** `src/play/work.ts` (`castWork`); `src/cli.ts` (`work`/`steer` arms); `src/play/propose-effect.ts`
(E-043 — confirm no orphan); `baml_src/steer.baml` (E-044 — confirm concrete #1); `work/T-039-02/
verdict.md` (the 8/10 baseline).

## Acceptance Criteria

- [ ] A **fresh board** staged (freshness gate passes); the **#1 signal recorded** — **concrete product
      demand** confirms E-044 live, or a still-self-referential #1 is recorded as a finding.
- [ ] `vend work --no-intervened --budget ~1h,~1M` run live to a clean P7 stop; auth==exec held.
- [ ] **≥1 chain cleared** (real epic+tickets, `lisa validate` green) with **NO orphan epic** afterward
      (E-043 live-confirmed) — or an honest record of a 0-clear / regression with the cause.
- [ ] New **forward-E1 records** (`intervened:false` + `success`) appended — forward count moved toward
      ≥10.
- [ ] `sweep-log.md` captures the verbatim #1 + receipt + cleared id(s) + ledger delta + the E-043/E-044
      confirmations.
