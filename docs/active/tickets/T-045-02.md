---
id: T-045-02
story: S-045-01
title: settle-the-cadence-and-bar
type: feature
status: open
priority: high
phase: structure
depends_on: [T-045-01]
---

## Context

**Settle the sweep — judge the ≥10 bar honestly, neither over- nor under-claiming.** T-045-01 ran the
metered cadence sweep; this reads the result against the E-014/E-026 standard. Four reads:

1. **Forward-E1 count + composition.** Run `bun run src/cli.ts audit`. State the new **forward (live)**
   count (was **8/10, 4 cleared**) and its **composition**: how many cleared (`success`) vs censored
   (`timed-out`/`budget-exhausted`) vs genuinely-intervened (`--intervened`). Cite **forward-only**
   numbers, never the combined pool (the T-026-04 trap).
2. **The ≥10-bar verdict — the load-bearing call.** If forward records reached **≥10**, the **pre-wired
   rule (E-014/E-026) is met** — say so plainly (no *under*-claim: the bar the project set, if met, is
   met). **But** state the composition caveat honestly: the evidence is **"didn't-break," not
   "stress-tested"** (most forward records are walk-aways that didn't *need* intervention; the genuine
   `--intervened` count is the real stress signal). So the go may **upgrade from provisional to
   bar-met**, *not* to "bulletproof" (no over-claim — the E-026/T-026-04 trap). If it did **not** reach
   ≥10, say how many short and that the go stays provisional + cadence continues.
3. **Hardening confirmed live (E-043/E-044).** Confirm the sweep's #1 was **concrete** (E-044 live) and
   **no orphan** was minted (E-043 live) — or record the regression. These were the deferred live
   confirmations; this settle closes them.
4. **Clear quality + P7/auth.** Assess what minted (sound frontier epic vs thin); confirm P7 held
   (clean stop, truthful receipt) and auth==exec (E-025).

**Board update:** crystallize E-045 on `demand.md` Frontier 1 — the forward-E1 count + composition, the
≥10-bar verdict (met/short + what it means for the provisional go), and the E-043/E-044 live
confirmations. Free + deterministic (reads the ledger the sweep wrote).

**Codebase-memory (use it — project coding-task suggestion):** confirm the audit/forward-split
coordinates with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via
`search_code`/`query_graph`, not raw grep:
- `search_code(pattern: "auditWalkAway|forward|attested|intervened|success|censored", regex: true, mode: "full", path_filter: "^src/ledger/")` — the forward count + composition the verdict must read correctly.
Confirmed coordinates at authoring: `auditWalkAway` `src/ledger/walk-away.ts:160`; forward baseline
**8/10, 4 cleared** (`vend audit`, post-E-039).

**Cites:** `src/ledger/walk-away.ts` (`auditWalkAway`); `work/measurement-sprint/findings.md` +
`docs/active/epic/E-014.md` (the ≥10 bar + the didn't-break-vs-stress-tested caveat); `work/T-026-04/
verdict.md` + `work/T-039-02/verdict.md` (the standard to hold); `work/T-045-01/sweep-log.md` (the run
this settles); `docs/active/demand.md` (Frontier 1 — the board to update).

## Acceptance Criteria

- [ ] `vend audit` re-run; the new **forward (live)** count + **composition** (cleared/censored/
      intervened) stated, citing forward-only numbers (never the combined pool).
- [ ] The **≥10-bar verdict**: if met, the pre-wired rule is **met** (no under-claim) WITH the
      composition caveat (didn't-break vs stress-tested — no "bulletproof" over-claim); if short, how
      many short + go stays provisional.
- [ ] **E-043/E-044 confirmed live** (no orphan; concrete #1) — or the regression recorded.
- [ ] A **clear-quality** read + P7/auth==exec confirmation; `demand.md` Frontier 1 updated.
- [ ] `bun run check:*` green.
