---
id: T-061-02
story: S-061-01
title: version-command-embeds-semver
type: task
status: done
priority: high
phase: done
depends_on: [T-061-01]
---

## Context

Add a `vend --version` surface that reports the manifest semver, embedded at build so it survives a single-file compiled binary (runtime package.json read is unavailable post-compile).

_Advances: P5, E-061: `vend --version` reports a real semver_

## Acceptance Criteria

- [ ] `vend --version` prints the package.json semver (not 0.0.0); a test asserts the printed value equals the manifest version, and it still resolves when run from a compiled binary with no node_modules.
