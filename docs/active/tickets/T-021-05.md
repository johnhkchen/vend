---
id: T-021-05
story: S-021-03
title: pure-graph-spec-to-projection
type: task
status: done
priority: high
phase: done
depends_on: [T-021-01, T-021-02, T-021-04]
---

## Context

The pure core: a (graph, spec) → projection function that applies grouping, density, field-visibility, labels and color-language to produce the face/details/links projection — same graph, many renders.

_Advances: P5, P1, data-presentation-split_

## Acceptance Criteria

- [ ] The function is pure (same inputs → identical projection, graph arg untouched); a test renders the live graph under two specs differing only in group_by and asserts the projection regroups while the underlying graph object is reference-unchanged.
