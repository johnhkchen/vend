---
id: T-057-03
story: S-057-02
title: vend-annotate-cli-gesture
type: task
status: done
priority: high
phase: done
depends_on: [T-057-02]
---

## Context

Add the thin, MCP-independent gesture `vend annotate <node-id> "<feedback>" [--seat <seat>]` — a pure arg parser plus a lazy-import dispatch arm casting the annotation — so feedback arrives as plain text input on one machine, the live comment FETCH deferred behind this seam.

_Advances: P5, P2, E-057: `vend annotate` exists as a real reusable gesture alongside `vend expand`_

**Codebase-memory (use it — project coding-task suggestion):** confirm the CLI parse/dispatch pattern with
the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "parseExpandArgs|cmd.*expand|usage|annotate|castExpand", regex: true, mode: "full", path_filter: "^src/cli")` — the `parseExpandArgs` (:345) + `expand` dispatch arm (:656) to MIRROR for `vend annotate` (positional node-id + feedback + `--seat`, usage banner, lazy-import cast).

**Cites:** `src/cli.ts` (`parseExpandArgs` :345, the `expand` dispatch arm :656, the usage banner — mirror
for `annotate`) + T-057-02; `docs/active/epic/E-057.md`.

## Acceptance Criteria

- [ ] cli.test.ts pins that `annotate <node-id> "<feedback>" --seat designer` parses node id, feedback, and seat (and rejects a missing node-id/feedback with the usage error, mirroring parseExpandArgs); the usage banner lists `vend annotate`; running it end-to-end stages a provenance-bearing signal and touches nothing on the board.
