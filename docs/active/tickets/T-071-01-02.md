---
id: T-071-01-02
story: S-071-01
title: cast-stamps-seat-of-execution
type: task
status: open
priority: high
agent: codex
phase: ready
depends_on: [T-071-01-01]
---

## Context

Stamp the resolved executor's lane onto every run record's `seatOfExecution`, so the ledger accrues real per-lane burn instead of an empty field.

_Advances: P7 — Budget is a hard contract; P4 — Autonomy by default, not supervision_

## Acceptance Criteria

- [ ] A cast through a stub executor writes a record whose `seatOfExecution` equals the executor's KNOWN_SEATS lane; a `cast-core` unit test pins the executor→lane mapping (claude executor → claude lane, openai-compat → codex); the field is spread only when known (a lane-less cast omits it, like `turnsUsed`); `bun run check` green.
