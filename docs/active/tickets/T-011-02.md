---
id: T-011-02
story: S-011-01
title: propose-decompose-chain-and-gesture
type: feature
status: open
priority: high
phase: ready
depends_on: [T-011-01]
---

## Context

The headline: a demand signal → ProposeEpic → an epic card → DecomposeEpic →
stories/tickets, in **one gesture**. Define the propose→decompose chain with the
`castChain` primitive (T-011-01): `castProposeEpic` → adapter (the minted epic path) →
`castDecomposeEpic` on it; wire it to `vend` (a `vend chain <signal>` command and/or the
shelf). End-to-end gating: a STOP in ProposeEpic (value/bounds) halts **before**
DecomposeEpic runs.

**Pull-discipline (PE-1):** one explicit pulled signal, never a board drain.

**Cites:** `src/engine/chain.ts` (T-011-01); `src/play/propose-epic.ts`
(`castProposeEpic` + the minted epic path it now surfaces); `src/play/decompose-epic.ts`
(DecomposeEpic takes an `epicPath`); `src/cli.ts` / `src/shelf/press.ts` (the gesture).

## Acceptance Criteria

- [ ] The propose→decompose chain is defined via `castChain` — ProposeEpic's `produced`
      epic path adapts to DecomposeEpic's `epicPath` input.
- [ ] A gesture (`vend chain <signal>` and/or the shelf) casts the **whole chain**: on
      success it produces **both** the epic card **and** its stories/tickets, each gated
      and logged (two run-log records); a ProposeEpic STOP halts before DecomposeEpic.
- [ ] A fixture/canned-reply test proves the chain (signal → epic → tickets); the
      threaded epic is the exact one ProposeEpic minted.
- [ ] `bun run check:*` green. (The live end-to-end cast — signal in, tickets out — is
      the human verification at sweep.)
