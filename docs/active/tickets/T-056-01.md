---
id: T-056-01
story: S-056-01
title: flip-designer-default-to-coarse-axis
type: task
status: open
priority: high
phase: done
depends_on: []
---

## Context

Re-aim only the non-dev default — change DESIGNER_PRESET.groupBy (spec.ts:114, currently the fine 'story' axis → ~62 columns) to a coarse axis already in GROUPINGS ('status' ≈5), so the default `vend svg` renders a board taken in at a glance; DEV_PRESET keeps its finer 'epic' axis.

_Advances: P5, E-056-glanceability_

**Codebase-memory (use it — project coding-task suggestion):** confirm the preset's `groupBy` default with
the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "DESIGNER_PRESET|DEV_PRESET|defaultPresetForSeat|groupBy|GROUPINGS", regex: true, mode: "full", path_filter: "^src/present/(presets|spec)")` — the non-dev default preset's `groupBy` to re-aim to a coarse axis (only the DEFAULT changes; every axis already exists in `GROUPINGS`).

**Cites:** `src/present/spec.ts` (`DESIGNER_PRESET.groupBy` ~:114; `GROUPINGS`); `src/present/presets.ts`
(`defaultPresetForSeat`); `src/present/svg-file.ts` (the seam projecting under the designer preset);
`docs/active/epic/E-056.md`.

## Acceptance Criteria

- [ ] A test (presets.test.ts / svg-file.test.ts) asserts defaultPresetForSeat('designer').groupBy is the coarse axis and DEV_PRESET.groupBy is unchanged; `vend svg` over the live board returns groupCount ≈ a handful (≤~6), not ~62, observable in the written .vend/work-graph.svg. Full suite green.
