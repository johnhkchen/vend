---
id: T-007-02
story: S-007-01
title: generic-cast-loop
type: task
status: done
priority: high
phase: done
depends_on: [T-007-01]
---

## Context

R1: extract the **play-agnostic spine** from `runDecomposeEpic` into one generic
`castPlay`. The loop is identical for every play — `render(play) → dispense(claude -p
seam, budget timeout) → budget.check → parse(play) → gates(play) → on pass
effect(play) → appendRunLog` — branching on the same pure `classify` verdict
(timeout / budget / gate). The play-specific bits all come from the `Play`
(T-007-01); reuse the pure decision core in `src/play/decompose-epic-core.ts`.

**Cites:** `src/play/decompose-epic.ts` (`runDecomposeEpic` — the body to
generalize; the impure-verb / pure-core split to preserve); `src/executor/claude.ts`
(the dispense seam); `src/budget/budget.ts`; `src/log/run-log.ts`; `src/gate/gates.ts`.

## Acceptance Criteria

- [ ] `src/engine/cast.ts` exports `castPlay(play, inputs, budget, opts) -> RunSummary`
      — the generic render→dispense→parse→gate→effect→log loop, branching on the
      reused pure `classify`; streams every message to both surfaces; appends exactly
      one countable run-log record per cast.
- [ ] **Play-agnostic:** no `DecomposeEpic`-specific code in `cast.ts` — it touches the
      `Play` interface only.
- [ ] The impure orchestration is the single untested verb; its decision core stays
      pure and tested (mirror the current runner split).
- [ ] `bun run check:test` / `check:typecheck` green.
