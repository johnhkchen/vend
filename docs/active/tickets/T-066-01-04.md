---
id: T-066-01-04
story: S-066-01
title: workflow-doc-story-contract
type: task
status: open
priority: medium
phase: done
depends_on: [T-066-01-01]
---

## Context

The story layer rotted to a shell partly because `docs/knowledge/rdspi-workflow.md` gives it no
job — `story:` is defined only as "parent story ID," so nothing in the loop reads a story body.
Add a story-layer section: the story is the contract between epic intent and ticket execution (name
its five sections); its readers are executing agents — especially cross-vendor cold ones arriving
with `AGENTS.md` and nothing else — plus the sweep and downstream allocation policy; and the working
rule is that a worker reads its ticket's parent story before the Research phase. lisa injects this
doc into every agent's context, so this section is the mechanism by which the contract actually
reaches workers. Keep it self-contained — no bare charter codes.

_Advances: P3 (a contract nobody reads is not a contract — this names the reader)._

## Acceptance Criteria

- [ ] `rdspi-workflow.md` gains the story-contract section: five sections named, readers named,
      read-your-parent-story rule stated; the section dereferences no charter codes.
