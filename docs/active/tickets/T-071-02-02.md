---
id: T-071-02-02
story: S-071-02
title: run-log-seat-inferred-marker-schema
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: [T-071-01-01]
---

## Context

Add the `seatInferred` provenance marker to the run record — the overflow analog of `seatDefaulted` — so which seat was inferred and why stays countable and auditable in the ledger.

_Advances: P4 — Autonomy by default, not supervision; P3 — Gates are the contract; E-071-auditable-inference-provenance_

## Acceptance Criteria

- [ ] `buildRunRecord`/`reviveRecord` tests show `seatInferred` (chosen seat + heat reason) written when supplied and omitted byte-identically when absent/malformed (the `normalizeSeatDefaulted` atomic-marker discipline), surviving a `readRuns` round-trip; `bun run check` green.
