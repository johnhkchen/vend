---
id: T-076-03-01
story: S-076-03
title: doctor-reviewer-dispensability
type: task
status: open
priority: medium
agent: codex
phase: ready
depends_on: [T-076-01-01]
---

## Context

Doctor's `executor dispensable` check (T-074-01-02) proves the PRIMARY seat only — it was green
on both machines while every cast was about to dial an unreachable reviewer. Extend it: when a
complement reviewer would resolve under current config (post-T-076-01-01 semantics), probe its
dispensability and go red with a named check + fix-it hint on failure. With default
(unprovisioned) config, report the inert state honestly in green ("cross-review: not provisioned
— casts skip review") instead of silence.

_Advances: P5 — doctor green must mean "everything this cast will dial is reachable"; P3 — the
preflight can't lie by omission_

## Acceptance Criteria

- [ ] Default config: doctor green, with the inert cross-review line visible.
- [ ] Provisioned + reachable: green named check. Provisioned + unreachable: red, names the
  reviewer seat, gives the fix — covered by unit tests via the existing probe-injection pattern.
- [ ] Reuses S-076-01's resolution and T-074-01-01's probe — no new probe mechanism.
- [ ] `bun run check` green.
