---
id: T-061-01
story: S-061-01
title: package-json-publishable-metadata
type: task
status: open
priority: critical
phase: done
depends_on: []
---

## Context

Make package.json shippable: real semver, a `bin.vend` entry, and drop `private:true` so the binary/formula pipeline has something installable to point at.

_Advances: P5, E-061: --version reports a real semver, not 0.0.0; brew-installable_

## Acceptance Criteria

- [ ] package.json `version` is a non-zero semver (e.g. 0.1.0), `private` key is absent, `bin.vend` resolves to the CLI entrypoint, and `bun run check` still passes.
