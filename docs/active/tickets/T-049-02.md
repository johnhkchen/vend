---
id: T-049-02
story: S-049-01
title: conditional-edges-in-concurrent-wave-dispatcher
type: task
status: open
priority: high
phase: done
depends_on: [T-049-01]
---

## Context

Mirror the same predicate-edge firing into runGraphConcurrent (the wave dispatcher) so conditional branching is byte-for-byte identical under concurrent dispatch as under the sequential reference, with the shared-wallet budget path (E-048) and back-compat untouched.

_Advances: P6, P7, E-049: behavior identical under both runGraph and castGraph (sequential and concurrent wave dispatch)_

**Codebase-memory (use it — project coding-task suggestion):** confirm the concurrent wave dispatcher + the E-048 shared-wallet path with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`) via `search_code`:
- `search_code(pattern: "runGraphConcurrent|castGraph|Promise.all|authorizeWave|debitWave|Wallet", regex: true, mode: "full", path_filter: "^src/engine/")` — the wave dispatcher to mirror the predicate firing into, leaving the E-048 shared-wallet path (`authorizeWave`/`debitWave`) and back-compat UNCHANGED.

**Cites:** `src/engine/graph-core.ts` (`runGraphConcurrent`) + `src/engine/graph.ts` (`castGraph` wave dispatcher — the seam to mirror); `src/budget/wallet.ts` (the E-048 shared wallet — unchanged); T-049-01 (the sequential reference this must equal); `docs/active/epic/E-049.md`.

## Acceptance Criteria

- [ ] graph-core.test.ts asserts runGraphConcurrent returns a GraphResult equal to runGraph's for the same predicated spec — same cast node set, same skipped ids+reasons, same produced map — including a multi-wave branch and a branch-not-taken under a budgeted wallet; all existing E-046/E-048 concurrency + wallet tests still green.
