---
id: T-054-01
story: S-054-01
title: pure-errored-outcome-unit
type: task
status: open
priority: high
phase: research
depends_on: []
---

## Context

Add a deterministic 'errored' RunOutcome plus a pure helper that maps a thrown cast into a marked, non-proceeding node summary — the single routing primitive both runners will reuse, keeping the pure-core/impure-shell split intact.

_Advances: P3, P6, E-054: a throw becomes a marked node outcome, not a crash_

**Codebase-memory (use it — project coding-task suggestion):** confirm the `RunOutcome` enum + the
`decideThread` gate with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready)
via `search_code`, not raw grep:
- `search_code(pattern: "RUN_OUTCOMES|RunOutcome|decideThread|RunSummary|proceed|produced", regex: true, mode: "full", path_filter: "^src/(log/run-log|engine/(chain-core|graph-core))")` — the outcome enum to extend with `errored`, and the shared `decideThread` gate the errored summary must route through (refuse ⇒ the existing halt path).

**Cites:** `src/log/run-log.ts` (`RUN_OUTCOMES`/`RunOutcome` — add `errored`); `src/engine/chain-core.ts`
(`decideThread` — must refuse `errored`, reused by both runners); `docs/active/epic/E-054.md`.

## Acceptance Criteria

- [ ] RUN_OUTCOMES gains an 'errored' value; a graph-core unit test (no runner, no live model) asserts the pure error helper yields outcome 'errored' with produced undefined, and that the existing decideThread refuses it (proceed:false) so it routes through the halt path unchanged.
