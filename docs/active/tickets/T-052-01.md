---
id: T-052-01
story: S-052-01
title: thread-shared-wallet-through-cast-real-play-graph
type: task
status: done
priority: high
phase: done
depends_on: []
---

## Context

Allocate ONE shared Wallet (E-048) sized to survey + 2 proposes + note and pass it as castGraph's third arg in castRealPlayGraph, so both proposes debit the same envelope instead of leaking per-node — the wiring gap at graph-real-play.ts:168.

_Advances: P7, P6, E-052 done#4-single-wallet-bounds-spend_

**Codebase-memory (use it — project coding-task suggestion):** confirm the real-play harness + the E-048
shared-wallet seam with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready)
via `search_code`, not raw grep:
- `search_code(pattern: "castRealPlayGraph|GraphRealPlayOptions|castGraph|allocate|proposeBudget|surveyBudget|Wallet", regex: true, mode: "full", path_filter: "^src/(play/graph-real-play|engine/graph|budget/wallet)")` — the per-node-budget harness to re-wire to ONE shared wallet (castGraph's optional `wallet?` 3rd arg, E-048).

**Cites:** `src/play/graph-real-play.ts` (`castRealPlayGraph` — `return castGraph(nodes, edges)` at :168;
per-node budgets at :61–62/:88–89); `src/engine/graph.ts` (`castGraph(nodes, edges, wallet?)` — the E-048
shared-wallet param at :101); `src/budget/wallet.ts` (`allocate`); `docs/active/epic/E-052.md`.

## Acceptance Criteria

- [ ] GraphRealPlayOptions carries a macro/wallet override; castRealPlayGraph builds it via allocate(...) and calls castGraph(nodes, edges, wallet); new graph-real-play-core.test.ts assertions pin that the third arg is a Wallet sized to cover all four nodes (not per-node), and `bun test` + typecheck stay green — the pure WIRING judgment, no live spend yet.
