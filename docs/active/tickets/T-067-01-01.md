---
id: T-067-01-01
story: S-067-01
title: charter-code-snapshot-resolver
type: task
status: open
priority: high
phase: done
depends_on: []
---

## Context

Pays the charter resolution exactly once, in one pure place — a code→one-line snapshot map cut-time consumers can trust, so downstream artifacts never need the live charter to mean something (P6).

_Advances: P6, P4_

## Acceptance Criteria

- [ ] Pure unit tests (no fs, no BAML addon) pin the resolver: fed the live charter text it maps every P1..P7 and N1..N4 code to its one-line text, and an unknown or retired code resolves to a typed absence the caller must handle — never a silent empty string.
