---
id: T-009-02
story: S-009-01
title: propose-core-gates-and-card-render
type: task
status: open
priority: high
phase: done
depends_on: [T-009-01]
---

## Context

The pure core of ProposeEpic — its PE gates and the card→markdown shaping (R1, no
addon/fs). The gates (`playbooks/propose-epic.md`):

- **value** — the card names a signal it clears + a charter value it `advances`;
- **bounds** — no non-goal (`N1–N4`) violated; prerequisites named; `advances` holds;
- **structural** — valid card frontmatter, and the **minted epic id is disjoint from
  the live board** — *reuse* `src/play/id-guard.ts` `detectCollisions` over the existing
  epic ids (the E-004 discipline, now at epic granularity), and compute the next free
  `E-0XX`.

Plus the pure `EpicCard → E-0XX.md` markdown renderer (frontmatter + card block + body).

**Cites:** `playbooks/propose-epic.md` (the gates); `src/play/id-guard.ts`
(`detectCollisions` — reuse); `src/gate/gates.ts` (the `GateResult` shape);
`card-model.md` (the markdown card block).

## Acceptance Criteria

- [ ] `src/play/propose-core.ts`: a **pure** `clear(card, ctx) -> GateResult` running
      value → bounds → structural (structural reuses `detectCollisions` for epic-id
      disjointness + mints the next free id); and a pure `renderCard(card) -> string`
      (the `E-0XX.md` markdown).
- [ ] Fully unit-tested: a passing card clears; a non-goal-violating card → bounds STOP;
      a colliding/duplicate id → structural STOP; the rendered markdown round-trips the
      card fields.
- [ ] Depends only on the generated `EpicCard` type + `id-guard` — no engine/fs.
      `bun run check:*` green.
