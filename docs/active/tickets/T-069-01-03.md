---
id: T-069-01-03
story: S-069-01
title: thread-agent-through-chain-gesture
type: task
status: open
priority: medium
agent: codex
phase: done
depends_on: [T-069-01-01]
---

## Context

Add the optional `agent` seat to the propose→decompose chain options and thread it into the decompose step's input assembly, so `vend chain … --agent codex` carries the seat to mint.

_Advances: P2 — The run is two gestures; P4 — Autonomy by default, not supervision_

## Acceptance Criteria

- [ ] An addon-free thread test proves `castProposeDecomposeChain({signal, agent:"codex"})`'s decompose step assembles `DecomposeInputs.agent === "codex"` via `assembleInputs`, while omitting `agent` leaves the field absent on the assembled inputs (chain shape unchanged).
