---
id: T-066-01-03
story: S-066-01
title: materialize-contract-body
type: task
status: done
priority: high
phase: done
depends_on: [T-066-01-01]
---

## Context

`src/play/materialize.ts` writes a story body of one line today ("Materialized by Vend's
`decompose-epic` play — N ticket(s)"). Write the contract body instead: the five sections from the
parsed story, a DAG block derived from the tickets' `depends_on` edges (derived, not duplicated —
the edges stay the single source), and the old provenance line demoted to a footer. The file should
still say a play cut it, and when — provenance stays honest; it just stops being the whole story.
Frontmatter is unchanged (id, title, type, status, priority, tickets).

_Advances: P1 (author once — the judgment the decompose cast produced lands in the artifact instead
of evaporating) and P6 (executor-agnostic — a self-contained story is what a cold cross-vendor
worker can pick up without ambient context)._

## Acceptance Criteria

- [ ] A golden-file test: materializing a contract-shaped parse yields a story file with all five
      sections, a DAG block consistent with the tickets' `depends_on` edges, and the provenance
      footer — byte-exact against the golden.
- [ ] Materializing tickets is byte-identical to today (only the story writer changes).
