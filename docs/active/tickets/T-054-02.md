---
id: T-054-02
story: S-054-01
title: thread-throw-into-both-runners
type: task
status: open
priority: high
phase: ready
depends_on: [T-054-01]
---

## Context

Wrap each node's cast in runGraph (per-node try/catch in the topo for-loop, graph-core.ts:209) and runGraphConcurrent (per-thunk try/catch inside the Promise.all wave, graph-core.ts:429-441) so a thrown thunk becomes the errored node summary and cascades through the EXISTING halt-dependent-subgraph machinery instead of rejecting the wave.

_Advances: P3, P4, P6, E-054: halt only dependents, preserve independent siblings' completed work_

**Codebase-memory (use it — project coding-task suggestion):** confirm both runners' cast-invocation points
with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "runGraph|runGraphConcurrent|Promise.all|allSettled|decideThread|skipped|halt", regex: true, mode: "full", path_filter: "^src/engine/graph-core")` — the sequential topo for-loop (try/catch per node, ~:209) + the concurrent `Promise.all` wave (per-thunk try/catch or `allSettled`, ~:429-441) where a throw must be caught into the errored summary, reusing the existing halt-dependent-subgraph machinery.

**Cites:** `src/engine/graph-core.ts` (`runGraph` for-loop ~:209; `runGraphConcurrent` `Promise.all` wave
~:429-441 — wrap each cast; reuse halt/skip) + T-054-01 (the errored helper); `docs/active/epic/E-054.md`.

## Acceptance Criteria

- [ ] Per-runner tests with a stub throwing thunk: the throwing node appears as an 'errored' entry in GraphResult.nodes, its transitive dependents land in `skipped` with a recorded reason, an independent sibling node still completes, and the runner promise RESOLVES to a clean GraphResult (never rejects) — proven for both runGraph and runGraphConcurrent with no live model.
