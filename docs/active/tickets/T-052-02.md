---
id: T-052-02
story: S-052-01
title: recast-diamond-live-and-settle-join-verdict
type: task
status: open
priority: high
phase: ready
depends_on: [T-052-01]
---

## Context

Cast the diamond LIVE through the wallet-threaded harness and settle an honest verdict + cast log proving the 2-upstream JOIN actually ran end-to-end — the single remaining stub-only property on the E-046/E-047/E-048 substrate.

_Advances: P4, P7, P6, E-052 done#1-note-ran, E-052 done#2-two-entry-upstreams, E-052 done#3-concurrency-held_

**Codebase-memory (use it — project coding-task suggestion):** confirm the live-cast entry + the
`GraphResult` join-evidence fields with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`,
indexed/ready) via `search_code`:
- `search_code(pattern: "castRealPlayGraph|GraphResult|NodeUpstreams|captureNote|walletRemaining|skipped|produced", regex: true, mode: "full", path_filter: "^src/(play/graph-real-play|engine/graph)")` — the live entry + the `GraphResult` fields (2-entry `NodeUpstreams` join, capture-note materialized, wallet readout) the verdict reads.

**Cites:** `src/play/graph-real-play.ts` (`castRealPlayGraph` — the live entry) + T-052-01 (the
wallet-threaded harness); `src/engine/graph.ts` (`GraphResult` — `produced`/`skipped`/wallet readout the
verdict cites); `docs/active/work/T-047-02/graph-cast-log.md` (the E-047 cast-log + honest-verdict pattern
to mirror); `docs/active/epic/E-052.md`.

## Acceptance Criteria

- [ ] A settlement artifact (graph-cast-log + verdict) under docs/active/work/T-052-02 records, from the live cast: capture-note's outcome materialized (not skipped); it received a 2-entry NodeUpstreams map from BOTH proposes' produced epic paths; the two propose casts overlapped in time; total spend was bounded by the single shared wallet (walletRemaining shows one envelope, not a per-branch leak); and the verdict labels it a live metered cast (≈4 real claude -p casts), distinct from a free deterministic proof.
