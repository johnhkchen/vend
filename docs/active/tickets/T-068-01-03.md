---
id: T-068-01-03
story: S-068-01
title: cost-weight-total-tokens
type: task
status: done
priority: critical
phase: done
depends_on: [T-068-01-01]
---

## Context

Mirror the cost-weighting in run-log.ts totalTokens (inline, preserving the run-log⊥budget zero-coupling invariant) so recalibrate/walk-away/spend recompute in cost units from the existing runs.jsonl buckets.

_Advances: P7 — Budget is a hard contract; P4 — Autonomy by default, not supervision_

## Acceptance Criteria

- [ ] totalTokens over a cache-dominated fixture record equals countTokens over the same usage; recalibrate over historical records returns cost-denominated p90 envelopes with no history re-run (unit test), and run-log.test.ts stays green.
