---
id: T-081-01-02
story: S-081-01
title: ledger-records-the-capped-unit
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: [T-081-01-01]
---

## Context

The durable turn field that audits and envelope math read records the same unit the summary line prints and --max-turns caps — deduped agent turns — ending the 2/20/45 mixed-unit poisoning of the measurement layer, with the executor event count kept only under its own honestly-named key if the spike shows it earns its place.

_Advances: P7 — Budget is a hard contract; P3 — Gates are the contract; E-081_

## Acceptance Criteria

- [ ] A cast through a stub executor writes a ledger row whose turn field equals the summary line's agent-turn figure (progress.turns), with the executor num_turns — if retained per the spike's verdict — under a separate honestly-named key; run-log build/revive tests pin the new key(s) round-trip, zero-is-a-value, and omission-when-unknown; a pre-E-081 line revives byte-identical with its old unit documented in the schema note rather than rewritten; T-077-01-01's characterization test is updated deliberately to the new contract (not silently broken) and the kitchen-seed EXPECTED-OUTCOME jq line reads the new key; bun run check green.
