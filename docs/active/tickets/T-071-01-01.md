---
id: T-071-01-01
story: S-071-01
title: run-log-seat-of-execution-field
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: []
---

## Context

Add a `seatOfExecution` field to the run record so recent usage is attributable to the lane that burned it — the substrate lane-heat inference reads.

_Advances: P7 — Budget is a hard contract; E-071-countable-heat-substrate_

## Acceptance Criteria

- [ ] `buildRunRecord`/`reviveRecord` unit tests show `seatOfExecution` written verbatim when supplied and OMITTED (byte-identical to a pre-E-071 record) when absent, surviving a `readRuns` round-trip; the log preserves the raw seat without policing it against KNOWN_SEATS (the `seatDefaulted` precedent); `bun run check` green.
