---
id: T-076-02-01
story: S-076-02
title: reviewer-failure-lands-as-andon
type: task
status: open
priority: high
agent: codex
phase: ready
depends_on: [T-076-01-02]
---

## Context

A provisioned reviewer that fails at dispense time (ConnectionRefused, timeout, malformed reply)
is currently an UNCAUGHT rejection — the raw-stack crash both field reports hit
(`dispenseReviewVerdict` → `dispenseOpenAICompat` fetch, src/engine/cast.ts:384). Catch it at the
settlement seam and land it as the existing amber andon shape (E-074's executor-unreachable
precedent) with the named cause and a fix-it hint — an outcome on the record, never a crash.

_Advances: P3 — refusal is a recorded outcome, not a stack trace; P4 — a walk-away cast must
fail legibly_

## Acceptance Criteria

- [ ] A unit test injects a throwing reviewer into castPlay and asserts: named andon outcome,
  no unhandled rejection, the andon line printed (no stack).
- [ ] The failure names the reviewer seat and endpoint category in plain words with a fix hint.
- [ ] A successful review's behavior is byte-identical to today (E-073 semantics untouched).
- [ ] `bun run check` green.
