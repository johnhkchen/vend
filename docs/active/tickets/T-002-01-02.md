---
id: T-002-01-02
story: S-002-01
title: test-gate-subclass-and-router
type: task
status: open
priority: high
phase: done
depends_on: [T-002-01-01]
---

## Context

The one gate's code (no Docker needed to build it). `/ci/src/test.ts` is a `Test`
sub-class that spins a **Bun** container, mounts the app source, and **invokes
`bun run check:test`** — it runs the script, it does not reimplement the check (the
Central Rule: the definition of "good" stays in the app's script). `/ci/src/index.ts`
routes `test()` and **nothing else yet** — one gate = one sub-class = one file.

**Shares `/ci/src/index.ts` with `T-002-01-01`** → depends on it (R4, file ownership).

**Cites:** `playbooks/ci-structural-gate.md` (the `X = test` instance, the 6 steps);
`ci-strategy.md` (the Central Rule; the TS "can't split index" constraint → sub-class
per file); `package.json` (`check:test`, from E-001's scaffold).

## Acceptance Criteria

- [ ] `/ci/src/test.ts`: a `Test` sub-class that runs a Bun container invoking
      `bun run check:test` against the mounted app source. **No check logic inlined** —
      it only calls the script.
- [ ] `/ci/src/index.ts` routes `test()` and nothing else (stays a thin router).
- [ ] `/ci` still imports nothing from the app; `dagger develop` not run casually.
- [ ] Compiles within the `/ci` module's own toolchain; the app build is untouched.
