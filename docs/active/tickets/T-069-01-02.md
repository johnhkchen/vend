---
id: T-069-01-02
story: S-069-01
title: materialize-stamps-agent-and-seat-write-guard
type: task
status: open
priority: high
agent: codex
phase: done
depends_on: [T-069-01-01]
---

## Context

Stamp `agent: <seat>` into ticket frontmatter after `priority:` at write time and refuse an unknown seat with a typed error before any write — the bare-code/collision write-guard pattern applied to the seat.

_Advances: P2 — The run is two gestures; P7 — Budget is a hard contract; P6 — Executor-agnostic underneath_

## Acceptance Criteria

- [ ] materialize.test.ts golden cases pass: a plan materialized with seat `codex` renders every ticket file with `agent: codex` on the line immediately after `priority:`; with no seat the rendered bytes equal the pre-change golden (no `agent:` key); and with seat `gpt` `materialize` throws `UnknownSeatError` having created zero files (target dirs empty).
