---
id: T-042-02
story: S-042-01
title: doctor-probe-effect
type: task
status: open
priority: high
phase: ready
depends_on: [T-042-01]
---

## Context

envinfo-backed probe effect running the ~3 vend-specific checks — lisa & claude on PATH, the BAML native addon loadable, and active-executor config present (default-Claude needs none; the open-model path needs its endpoint vars) — each emitting a Check result and never throwing.

_Advances: P6, P5, P3_

## Acceptance Criteria

- [ ] Test (mocked or guarded-live): the probe returns all-ok in a wired env; with lisa off PATH (or the BAML addon unloadable, or the open-model endpoint var unset) the corresponding Check returns failed with its hint and the probe returns a result rather than raising; envinfo is added to package.json deps. Scope-guard: a basic executor-config presence check only, not a full open-model validation matrix (that is the named follow-up).
