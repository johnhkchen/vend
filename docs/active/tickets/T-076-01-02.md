---
id: T-076-01-02
story: S-076-01
title: cross-review-skipped-marker
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: [T-076-01-01]
---

## Context

When a complement WOULD be relevant (lane-ful author, diff captured) but no reviewer is
provisioned, the run record must say so honestly: add a `crossReviewSkipped` marker (the
seatDefaulted/seatInferred analog in src/log/run-log.ts — reason skipped, what would bind it)
and wire it at the settlement seam in src/engine/cast.ts where resolution returns null.

_Advances: P3 — Gates are the contract (an inert gate that says nothing is indistinguishable
from a gate that ran); P7 — provenance keeps requested-vs-actual honest_

## Acceptance Criteria

- [ ] `crossReviewSkipped` is declared in run-log.ts following the seatDefaulted pattern
  (schema, normalize, round-trip through the ledger read path) with unit tests.
- [ ] castPlay stamps the marker exactly when a complement would be relevant and resolution is
  inert; a lane-less or diff-less cast carries NO marker (byte-identical record to today).
- [ ] `bun run check` green.
