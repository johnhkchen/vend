---
id: T-056-03
story: S-056-02
title: weight-blocked-edges-in-svg
type: task
status: done
priority: medium
phase: done
depends_on: [T-056-02]
---

## Context

Give blocked edges visual WEIGHT in projectionToSvg (projection-svg.ts:82/149) — a blocked link renders thicker with a distinct stroke while a satisfied depends_on edge stays light — so 'what should I unblock first?' pops off the board; scope held to stroke weight/color only, no palette redesign.

_Advances: P5, E-056-edge-payload_

**Codebase-memory (use it — project coding-task suggestion):** confirm the edge-render path with the
**codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "projectionToSvg|svgLine|stroke|edge|ProjectionLink|<line", regex: true, mode: "full", path_filter: "^src/present/(projection-svg|svg|project)")` — where `projectionToSvg` draws each link as a `<line>`, to give a `blocked` link a heavier/distinct stroke while satisfied edges keep the light style (stroke weight/color only — no palette redesign).

**Cites:** `src/present/projection-svg.ts` (the edge-draw path ~:82/:149) + `src/present/svg.ts`
(`svgLine` primitive) + T-056-02 (the `blocked` flag); `docs/active/epic/E-056.md`.

## Acceptance Criteria

- [ ] projection-svg.test.ts asserts a blocked:true link renders the heavy/distinct stroke (thicker strokeWidth + distinct color) and a blocked:false link renders the existing light EDGE style; same projection → byte-identical SVG; opening .vend/work-graph.svg shows heavy strokes only on unsatisfied dependencies. House gates green.
