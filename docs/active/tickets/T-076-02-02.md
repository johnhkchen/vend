---
id: T-076-02-02
story: S-076-02
title: ledger-line-and-artifact-survive-settlement-throw
type: task
status: open
priority: high
agent: codex
phase: ready
depends_on: [T-076-02-01]
---

## Context

Both field failures spent real tokens and left NO ledger line — the crash pre-empted the
run-record write — and boilerplate-demo additionally has a stray
`.vend/artifacts/run-….diff` with no matching record. Make the record write survive any
settlement-path throw (try/finally or equivalent in castPlay), and make artifact + ledger row
land together: an artifact without a row (or a row referencing a missing artifact) can no longer
be produced by a single cast's failure.

_Advances: P7 — spent tokens always land on the ledger (envelopes, lane heat, and audit all read
it); honest-on-outcome — the ledger is the trust substrate_

## Acceptance Criteria

- [ ] A unit test throws from the settlement tail and asserts the run record is still written
  with an honest outcome.
- [ ] Artifact/record consistency is asserted: the diff-capture path either writes both or
  records the discrepancy on the run record.
- [ ] `bun run check` green.
