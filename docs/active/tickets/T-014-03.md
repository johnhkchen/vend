---
id: T-014-03
story: S-014-01
title: findings-note-and-go-reroute-decision
type: feature
status: done
priority: high
phase: done
depends_on: [T-014-01, T-014-02]
---

## Context

The synthesis (PRD KR4) — consume the E1 (trust) and E2 (consistency) outputs and produce
a **one-page findings note** returning the decision the whole epic exists for:

- **E1 + E2 green** → **go**: build the macro-wallet as *trust capitalized*.
- **E1 weak** (author keeps intervening) → **reroute** to the andon UX / design-language —
  trust must precede walk-away.
- **E2 weak** (gates don't reduce variance) → **reroute** to the core consistency promise
  before scaling autonomy at all.

The note's home is `docs/active/work/T-014-03/findings.md` (and it bridges back to
`demand.md` — the decision re-ranks the next pull). Evidence, not a roadmap.

**Cites:** T-014-01 (E1 numbers), T-014-02 (E2 number); the PRD §8 (what V1 gates);
`discovery-foundation.md` (the decision framework); `demand.md` (the macro-wallet signal
this decision gates).

## Acceptance Criteria

- [ ] A findings note states the **two numbers** (walk-away/intervention trend; gate-driven
      variance reduction) with a one-paragraph read each.
- [ ] It returns an explicit **go / reroute** recommendation per the decision rule above,
      and names the concrete next pull each branch implies.
- [ ] The note is honest about the sample's limits (one self-reporting user, ≤5 casts) —
      a directional signal, not a proof.
- [ ] `bun run check:*` green. (Producing the two numbers requires running T-014-01/02's
      instruments — the live measurement is the human step at sweep.)
