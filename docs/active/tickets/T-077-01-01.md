---
id: T-077-01-01
story: S-077-01
title: characterize-max-turns-live-seam
type: task
status: open
priority: high
agent: codex
phase: ready
depends_on: []
---

## Context

Pin, in a committed test, what `--max-turns 15` binds at the live executor seam, what the accumulator counts, and what cap-hit does — the factual precondition for honest turn labeling and next-repair-action.

_Advances: P4 — Autonomy by default, not supervision; P7 — Budget is a hard contract; E-077_

## Acceptance Criteria

- [ ] A committed characterization test drives a decompose cast through the real executor argv seam and asserts buildArgs emits `--max-turns 15` (DECOMPOSE_MAX_TURNS), the live accumulator counts distinct deduped assistant message-ids while turnsUsed captures the executor's num_turns (a different unit), and cap-hit at the seam is recorded — extending T-072-04-01's synthetic diagnosis to the impure shell it left out (progress.md:69).
