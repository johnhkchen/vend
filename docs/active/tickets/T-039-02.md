---
id: T-039-02
story: S-039-01
title: settle-the-clear-and-cadence
type: feature
status: open
priority: high
phase: structure
depends_on: [T-039-01]
---

## Context

**Settle the re-sweep — and refuse to over-claim it (the E-014/E-026 standard).** T-039-01 ran the
metered sweep with the censor fixed; this reads the result honestly. Three reads:

1. **Cleared vs censored forward-E1.** Run `bun run src/cli.ts audit`. Distinguish the **first CLEARED
   forward record** (`intervened:false` + `success`) from the censored ones already in the ledger (the
   4/10 were all `timed-out`). State the new forward read — cleared count and total — citing the
   **forward-only** numbers, never the combined pool (the T-026-04 over-count trap). If it 0-cleared,
   say so plainly and name where it stopped.
2. **Clear quality + the E-038 effect.** Assess WHAT minted: is the epic+tickets a **sound, grounded**
   card for a real frontier, or thin/degenerate (e.g. a self-referential meta-epic)? Confirm **propose
   FINISHED** where E-037's timed out (the E-038 fix's live effect — even if a downstream step then
   limited). Confirm **P7 held** (clean stop, truthful receipt, nothing partial) and **auth==exec held**
   (E-025).
3. **The honest verdict + cadence.** Write `docs/active/work/T-039-02/verdict.md`: whether the loop is
   now watched **clearing** (not just refusing); the forward-E1 cleared count; and that the go stays
   **provisional + forward-leaning** with a **named cadence to ≥10** (one clear is the first, +N to go)
   — or graduates only if genuinely earned. **No "forward-confirmed" off one clear** (the load-bearing
   non-goal). If it still 0-cleared, the honest verdict is "E-038 moved the bottleneck to X; the clear
   awaits the next fix" — name X.

**Board update:** crystallize E-039 honestly on `demand.md` Frontier 1 — what the re-sweep proved
(watched clearing, or the moved bottleneck), the forward cleared count, and the remaining cadence.
Free + deterministic (reads the ledger the sweep wrote).

**Codebase-memory (use it — project coding-task suggestion):** confirm the audit/forward-split
coordinates with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via
`search_code`/`query_graph`, not raw grep:
- `search_code(pattern: "auditWalkAway|forward|attested|intervenedAttestation", regex: true, mode: "full", path_filter: "^src/ledger/")` — the cleared-vs-censored forward read the verdict must cite correctly.
Confirmed coordinates at authoring: `auditWalkAway` `src/ledger/walk-away.ts:160`; the forward/attested
split (E-028); current forward baseline **4/10, all censored** (`vend audit`, post-E-037).

**Cites:** `src/ledger/walk-away.ts` (`auditWalkAway` — the forward split, E-028); `work/T-026-04/
verdict.md` + `work/T-037-03/verdict.md` (the standard to hold); `work/T-039-01/sweep-log.md` (the run
this settles); `docs/active/demand.md` (Frontier 1 — the board to update).

## Acceptance Criteria

- [ ] `vend audit` re-run; the **cleared vs censored** forward read stated (the first cleared record
      distinguished), citing **forward-only** numbers — never the combined pool. (Or an honest 0-clear
      with the stopping point named.)
- [ ] A **clear-quality** read (sound/grounded vs thin) + confirmation **propose FINISHED** (E-038
      live), **P7 held**, and **auth==exec held** (E-025).
- [ ] `verdict.md`: the loop is watched **clearing** (or the bottleneck moved), forward cleared count
      stated, go **provisional + named cadence to ≥10** (no over-claim off one clear).
- [ ] `demand.md` Frontier 1 updated honestly.
- [ ] `bun run check:*` green.
