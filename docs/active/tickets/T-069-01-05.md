---
id: T-069-01-05
story: S-069-01
title: cli-agent-flag-parse-and-dispatch
type: task
status: open
priority: medium
agent: codex
phase: done
depends_on: [T-069-01-03, T-069-01-04]
---

## Context

Parse `--agent <seat>` on `vend run` and `vend chain`, thread it through dispatch into both cast-path option shapes, and surface it in usage — so seat allocation is chosen as part of the two-gesture mint at the counter.

_Advances: P2 — The run is two gestures; P4 — Autonomy by default, not supervision_

## Acceptance Criteria

- [ ] cli.test.ts passes: `parseArgs(["chain","sig","--agent","codex"])` → `{cmd:"chain", agent:"codex"}`, `parseArgs(["run","decompose-epic","e.md","--budget","…","--agent","codex"])` carries `agent:"codex"`, a dangling `--agent` (no value) returns a `usage` error `missing --agent <seat>`, and the usage string lists `[--agent <seat>]` on both gestures.
