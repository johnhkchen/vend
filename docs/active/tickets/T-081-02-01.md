---
id: T-081-02-01
story: S-081-02
title: fold-charges-what-the-meter-charges
type: task
status: open
priority: high
agent: codex
phase: ready
depends_on: [T-081-01-01]
---

## Context

The number the operator allocates by mid-cast tracks the meter's weighted spend and is labeled for what it is — and the same fold's deduped turn counter counts only the capped unit per the spike's evidence — so the live line stops inviting the exact misallocation E-068 existed to end.

_Advances: P7 — Budget is a hard contract; P3 — Gates are the contract; E-081_

## Acceptance Criteria

- [ ] Replaying the captured jank-run fixture transcript through accumulateCastProgress yields a final weightedTokens within a stated, named tolerance of that run's ledger totalTokens (the ~2× undercount closed), with the tolerance value and the line's label pinned by cast-core tests; formatCastProgress names what its token figure is; sidechain handling in both fold counters matches the spike's counted evidence; bun run check green.
