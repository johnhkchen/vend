---
id: T-001-01
story: S-001
title: scaffold-bun-project
type: chore
status: done
priority: critical
phase: done
depends_on: []
---

## Context

Vend has no code yet — docs only. Stand up the Bun/TypeScript project every later
ticket builds on, plus the `bun run check:*` command surface that the play invokes
as andon gates and CI (E-002) invokes independently. Per `ci-strategy.md` the
check *logic* lives in these scripts ("Dagger invokes, it never defines") — so
getting this surface right now is load-bearing. Stack is settled in `stack.md`:
TypeScript on Bun, strict. BAML-on-Bun is already verified (see E-001).

## Acceptance Criteria

- [ ] `package.json` with `"type": "module"`, Bun as runtime + package manager;
      runtime dependency `@boundaryml/baml@^0.222.0` (the verified version).
- [ ] `tsconfig.json` strict: `"strict": true`, `"noUncheckedIndexedAccess": true`.
- [ ] `src/` skeleton dirs present: `executor/ budget/ log/ gate/ play/`.
- [ ] `bun run check:test` runs `bun test` and passes on one trivial smoke test.
- [ ] `bun run check:typecheck` runs `tsc --noEmit` clean.
- [ ] `.gitignore` covers `node_modules/`, build output, `.vend/`; `bun install`
      succeeds from a clean clone.
- [ ] Scaffold only — no app logic. (Lint/format deferred to E-002/CI per stack.md.)
