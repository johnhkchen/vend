---
id: T-049-01
story: S-049-01
title: predicate-on-dagedge-sequential-rungraph
type: task
status: open
priority: high
phase: done
depends_on: []
---

## Context

Add an optional predicate to DagEdge and make the sequential runGraph fire only the out-edges whose predicate holds over the upstream's produced — the branch-not-taken's in-edge becomes unsatisfied so its dependent subgraph cascade-skips, landing in `skipped` with a branch-not-taken andon distinct from the upstream-halt andon.

_Advances: P6, P3, E-049: a node's produced result selects which downstream edges fire; not-taken branch is an observable skip, never silently dropped_

**Codebase-memory (use it — project coding-task suggestion):** confirm the edge model + the sequential runner with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`) via `search_code`/`query_graph`, not raw grep:
- `search_code(pattern: "DagEdge|DagSpec|validateDag|NodeUpstreams", regex: true, mode: "full", path_filter: "^src/engine/dag-core")` — the `{from,to}` edge model to extend with an optional predicate (over `produced`, the edge payload).
- `search_code(pattern: "runGraph|skipped|SkippedNode|andon|decideThread|halt", regex: true, mode: "full", path_filter: "^src/engine/graph-core")` — the fan-out + halt-dependent-subgraph / `skipped` machinery to REUSE for branch-not-taken (the design spine, not new infra).

**Cites:** `src/engine/dag-core.ts` (`DagEdge {from,to}` — add the optional predicate); `src/engine/graph-core.ts` (`runGraph` fan-out + the `skipped`/andon cascade to reuse); `docs/active/epic/E-049.md`.

## Acceptance Criteria

- [ ] New graph-core.test.ts case: a 1→{A,B} fan-out whose two out-edges carry mutually-exclusive predicates over the upstream `produced` runs ONLY the matching branch under runGraph; the not-taken node is present in result.skipped with a reason that reads 'branch not taken' and is textually distinct from the existing 'dependent on halted upstream' reason; validateDag still returns ok for the predicated spec; `bun test` green.
