---
id: T-079-01-01
story: S-079-01
title: settle-core-pure-verdict
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: [T-078-02-03]
---

## Context

Retire the operator's inspector role into a pure, testable computation — the gate contract's state assembled as data the machines already have, read not judged.

_Advances: P3 — Gates are the contract; P4 — Autonomy by default, not supervision_

## Acceptance Criteria

- [ ] bun test src/settle green: a fixture board plus a prior last-settle marker yields a verdict carrying the board delta (tickets newly done since the marker), per-epic cleared counts with an all-done set, gate/presweep/review-concern fields, and an ordered exceptions list each with a nextAction; no prior marker degrades to a full-board first-settle summary, and a malformed marker is a named refusal, not a crash.
