---
id: T-051-01
story: S-051-01
title: buildargs-disallowedtools-symmetric-denylist
type: task
status: open
priority: high
phase: ready
depends_on: []
---

## Context

Add the symmetric disallowedTools option to buildArgs/DispenseOptions so the headless seam can emit `--disallowedTools`, beside the existing allowedTools allowlist and under the same empty-omitted-emits-nothing discipline.

_Advances: P4, P6, E-051: a clean tool-UNAVAILABILITY plumbing rather than a hang_

**Codebase-memory (use it — project coding-task suggestion):** confirm the pure headless argv builder with
the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`, not
raw grep:
- `search_code(pattern: "buildArgs|allowedTools|disallowedTools|DispenseOptions|strictMcp|--allowedTools", regex: true, mode: "full", path_filter: "^src/executor/claude")` — the PURE argv builder + the `--allowedTools` precedent to mirror (the empty-omitted-emits-nothing back-compat discipline, claude.ts:131–164).

**Cites:** `src/executor/claude.ts` (`buildArgs`/`DispenseOptions` — add `disallowedTools` beside
`allowedTools` at :146/:164, same empty-omitted rule); E-032 (the `--allowedTools` allowlist this mirrors);
`docs/active/epic/E-051.md`.

## Acceptance Criteria

- [ ] New claude.test.ts cases: buildArgs({disallowedTools:["AskUserQuestion"]}) yields argv containing the flag with the comma-joined value as ONE element; buildArgs({}) and buildArgs({disallowedTools:[]}) are byte-identical to the pre-change argv; the exact flag spelling is confirmed against `claude -p --help` and noted in-code as E-032 did. Full claude.test.ts green.
