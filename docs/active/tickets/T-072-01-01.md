---
id: T-072-01-01
story: S-072-01
title: help-command-and-grouped-usage
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: []
---

## Context

Make the most-typed gesture answer: `vend --help`/`vend help` prints the complete command list grouped free-vs-metered instead of erroring `unknown command`.

_Advances: P2 — The run is two gestures; epic-dim2-discoverability_

## Acceptance Criteria

- [ ] parseArgs(['--help']) and parseArgs(['help']) return a `help` command (not a `usage` error); the dispatch shell prints the regrouped banner and exits 0; a src/cli.test.ts case asserts every real verb appears under either the free or the metered group and none is missing.
