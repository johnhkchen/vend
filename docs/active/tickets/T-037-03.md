---
id: T-037-03
story: S-037-01
title: settle-the-evidence-and-verdict
type: feature
status: open
priority: high
phase: done
depends_on: [T-037-02]
---

## Context

**Settle the watched run into honest evidence — and refuse to over-claim it.** T-037-02 spent the
wallet live; this reads the result the way the project holds itself to (the E-014/E-026 standard:
don't dress an unverified claim as measured). Three reads:

1. **Forward-E1 moved.** Run `bun run src/cli.ts audit`; record the **new forward (live) walk-away
   rate** (was **1/2** at authoring). State the delta — how many genuine forward records the sweep
   added (each `--no-intervened` chain that cleared = one). Cite the forward-only count, never the
   combined 14/15 (the T-026-04 over-count this project already corrected).
2. **Quality, not just quantity (trust = walk-away QUALITY).** Assess WHAT it cleared: is the
   autonomously-minted epic+tickets a **sound, grounded** card for a real frontier, or thin/
   misaimed? A walk-away that clears junk is not trust. Confirm **P7 held** (clean stop, truthful
   receipt, nothing partial) and **auth==exec held** (E-025 — cast ran under the authorized price).
3. **The honest verdict + cadence — NO over-claim.** Write `docs/active/work/T-037-03/verdict.md`:
   the headline gesture is now **watched** (P4/P7 demonstrated live, not just wired); the forward-E1
   count moved to **N/M**. State plainly whether the go stays **provisional + forward-leaning** (one
   bounded session does **not** meet the ≥10-genuine bar) with a **named cadence** to ≥10 (each
   future `--no-intervened` sweep accrues records), or — only if genuinely earned — graduates. **Do
   not claim "forward-confirmed" off a small sample** (the load-bearing non-goal — the exact E-026/
   T-026-04 trap).

**Board update:** crystallize E-037 honestly — Frontier 1's keystone is now *watched* (the feature
demonstrated live) **and** the forward-E1 gate moved on real evidence; name what remains to fully
ungate (the ≥10 cadence). Free + deterministic (reads the ledger the sweep wrote).

**Codebase-memory (use it — project coding-task suggestion):** confirm the audit/forward-split
coordinates with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready)
via `search_code`/`query_graph`, not raw grep:
- `search_code(pattern: "auditWalkAway|forward|attested|intervenedAttestation|reviveRecord", regex: true, mode: "full", path_filter: "^src/ledger/")` — the forward (live) vs attested back-fill split the verdict must cite correctly.
Confirmed coordinates at authoring: `auditWalkAway` `src/ledger/walk-away.ts:160`; the forward/
attested split (E-028); `attest-intervention.ts` (the marker); current baseline **forward (live)
1/2** (`vend audit`, 2026-06-20).

**Cites:** `src/ledger/walk-away.ts` (`auditWalkAway` — the forward/attested split, E-028);
`work/T-026-04/verdict.md` (the over-count correction — the standard to hold); `work/T-037-02/
sweep-log.md` (the run this settles); `docs/active/demand.md` (Frontier 1 — the board to update).

## Acceptance Criteria

- [ ] `vend audit` re-run; the **new forward (live) rate** recorded with the delta (genuine forward
      records added by the sweep), citing the **forward-only** count, never the combined number.
- [ ] An honest **quality** read of what cleared (sound/grounded vs thin) + confirmation **P7 held**
      (clean stop, truthful receipt) and **auth==exec held** (E-025).
- [ ] `verdict.md`: the gesture is **watched** (live P4/P7), forward-E1 moved to N/M, and the go is
      stated as **provisional + named cadence to ≥10** (or graduated only if earned) — **no
      "forward-confirmed" over-claim** off a small sample.
- [ ] `demand.md` Frontier 1 updated honestly (watched + evidence moved + what remains).
- [ ] `bun run check:*` green.
