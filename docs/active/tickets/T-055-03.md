---
id: T-055-03
story: S-055-01
title: svg-file-output-seam
type: task
status: open
priority: medium
phase: ready
depends_on: [T-055-02]
---

## Context

A thin CLI/file seam that loads the live board, projects it (projectGraph), runs projectionToSvg, and writes the resulting `.svg` to the staged output path — the unblocked, MCP-independent visual half of the non-dev round-trip, a portable offline file rather than a live Linear integration.

_Advances: P5, P3, E-055: unblocked file-based path to Frontier 4 value, no external service_

**Codebase-memory (use it — project coding-task suggestion):** confirm the present surface load path + the
authority guard with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via
`search_code`:
- `search_code(pattern: "projectGraph|loadGraph|authority|oneWay|staged|present", regex: true, mode: "full", path_filter: "^src/(present|graph|cli)")` — how the board is loaded (`loadGraph` → `projectGraph`) + the `authority-guard` one-way gate the seam must keep green (write the STAGED artifact, never `docs/active`).

**Cites:** `src/present/project.ts` (`projectGraph`); `src/graph/load.ts` (load the live board);
`src/present/authority-guard.ts` (the one-way-authority gate) + T-055-02 (`projectionToSvg`);
`docs/active/epic/E-055.md`.

## Acceptance Criteria

- [ ] Running the seam against the live board writes a `.svg` that opens in any viewer and shows a swimlane per group, a labeled box per card, and an edge per link; the authority-guard one-way-authority build gate stays green (the seam writes the staged artifact, never docs/active) and the loaded graph object is reference-unchanged.
