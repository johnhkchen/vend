---
id: T-057-02
story: S-057-01
title: thread-annotation-through-expand-staging
type: task
status: done
priority: high
phase: done
depends_on: [T-057-01]
---

## Context

Thread annotation provenance through expand-fragment's existing cast/effect (feedback = fragment + provenance metadata) so a cleared annotation STAGES one Signal under docs/active/pm/staged/ carrying the trailer + back-link — reusing the inherited clearing, gates, and pricing, never rebuilding them.

_Advances: P1, P4, E-057: priced by the same expand clearing, N2: staged inbox a human pulls, not a live approval loop_

**Codebase-memory (use it — project coding-task suggestion):** confirm expand's cast/effect + the
one-way-authority staging with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`,
indexed/ready) via `search_code`:
- `search_code(pattern: "STAGING_DIR|writeFile|ExpandFragmentInputs|castPlay|projectRoot|expand", regex: true, mode: "full", path_filter: "^src/play/expand")` — the existing clearing/staging effect to thread the annotation metadata through (stage ONLY to `pm/staged/`, NEVER the board — inherit, don't weaken).

**Cites:** `src/play/expand-effect.ts` (`STAGING_DIR` :36 — stages only to `pm/staged/`;
`ExpandFragmentInputs` :44) + `src/play/expand-fragment.ts` (the cast to reuse) + T-057-01;
`docs/active/epic/E-057.md`.

## Acceptance Criteria

- [ ] An effect test stubbing expand-fragment's cast (exactly as expand-effect.test.ts does, against a temp-dir projectRoot) shows the staged <slug>.md contains the provenance trailer + back-link AND asserts NO write occurs to demand.md, epic/, stories/, or tickets/ (one-way authority holds); full suite stays green via bun run check.
