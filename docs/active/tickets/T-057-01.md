---
id: T-057-01
story: S-057-01
title: annotation-input-and-provenance-render-core
type: task
status: done
priority: high
phase: done
depends_on: []
---

## Context

Define the typed Annotation (feedback text + annotated work-item/node id + author seat) and the PURE render of a provenance trailer + back-link, so a staged signal can name its non-dev source — captured once, at annotation, as data.

_Advances: P1, E-057: provenance trailer naming seat X on node Y, E-057: back-link to the annotated work item_

**Codebase-memory (use it — project coding-task suggestion):** confirm expand-fragment's staged-signal
render with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "renderStagedSignal|STAGING_DIR|Signal|origin|trailer|slugify", regex: true, mode: "full", path_filter: "^src/play/expand-effect")` — the PURE staged-signal render + its origin-trailer pattern (`:80`/`:82`) to extend with the annotation provenance + back-link.

**Cites:** `src/play/expand-effect.ts` (`renderStagedSignal` :82 + the origin trailer :80 — the pure render
to extend; `Signal`); `docs/active/epic/E-057.md`.

## Acceptance Criteria

- [ ] A pure unit test (sibling of expand-effect.test.ts) asserts the render function, given a Signal + {seat, nodeId}, returns markdown containing both a provenance line naming the seat and node id and a back-link referencing the annotated work item; the function imports no BAML and no fs, deterministic across repeat calls.
