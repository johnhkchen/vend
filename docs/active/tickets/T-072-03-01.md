---
id: T-072-03-01
story: S-072-03
title: parse-humane-budget-units
type: task
status: open
priority: medium
agent: codex
phase: ready
depends_on: [T-072-01-02]
---

## Context

Let the counter accept `40m,350k` / `2h,1.5m` while the raw `<ms>,<tokens>` still works — the input half of humane units.

_Advances: P7 — Budget is a hard contract; P2 — The run is two gestures_

## Acceptance Criteria

- [ ] parseBudgetArg('40m,350k') deep-equals parseBudgetArg('2400000,350000'), `2h,1.5m` parses to {timeMs:7200000,tokens:1500000}, the raw form round-trips unchanged, and a malformed suffix (`40x,350k`) throws the existing RangeError shape — all asserted in src/cli.test.ts.
