---
id: T-074-01-03
story: S-074-01
title: cast-time-executor-unreachable-andon
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: [T-074-01-01]
---

## Context

Route an executor-unreachable failure at cast time to the existing missing-capability amber andon with the named cause + hint, so the first cast in a broken environment refuses honestly instead of emitting a raw stack.

_Advances: P3 — Gates are the contract; P5 — Local-first_

## Acceptance Criteria

- [ ] Casting a play whose active executor's probe reports not-ok returns a `missing-capability` RunSummary (materialized:false) and appends exactly one run-log line with outcome `missing-capability`, usage {}, cost 0, and the named executor-unreachable cause + hint — no raw stack, nothing dispensed; a cast whose executor IS dispensable is byte-identical to today. Covered by a new cast-core.test.ts classifier case plus the andon path in cast.ts.
