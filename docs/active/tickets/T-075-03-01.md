---
id: T-075-03-01
story: S-075-03
title: cold-start-confidence-count
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: []
---

## Context

Thread the sub-threshold successful-run count onto the shelf `default` confidence so a thin-but-real play reads 'N runs, measured at 3' instead of the collapsing 'no runs yet'.

_Advances: P3 — Gates are the contract; P5 — Local-first; E-075: labels that never lie — thin-but-real ≠ none_

## Acceptance Criteria

- [ ] shelf-row.test.ts asserts 1–2-run → '(default — N runs, measured at 3)', 0-run → '(default — no runs yet)', and the discriminated union still makes a runs-bearing measured-0 unconstructable.
