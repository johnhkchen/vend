---
id: T-081-01-01
story: S-081-01
title: transcript-forensics-turn-and-token-census
type: spike
status: done
priority: high
agent: codex
phase: done
depends_on: []
---

## Context

Evidence before design: reconcile the 45-in-a-15-capped-dispense arithmetic and census which message kinds inflate num_turns, whether sidechain/subagent assistant ids inflate the deduped agent-turn count, and which buckets or kinds the live fold misses against the ledger's weighted spend — the facts both fixes build against instead of the working hypothesis.

_Advances: P7 — Budget is a hard contract; E-081_

## Acceptance Criteria

- [ ] A written root-cause note in docs/active/work/T-081-01-01/ that (a) accounts for 45 vs the 15 cap from a replayed real transcript with per-message-kind counts (main-loop vs sidechain/parent_tool_use_id split) reconciling exactly to num_turns, (b) states with counted evidence whether sidechain assistant ids inflate progress.turns, and (c) itemizes the live fold's final figure against the same run's ledger totalTokens bucket by bucket, reproducing the ~2× gap (105k vs ~214k) and naming each miss (first-event-vs-final usage per id, skipped kinds, terminal-skip residual). Every conclusion cites run ids and counted transcript lines; sanitized fixture excerpts for facts the fix tickets will pin are committed.
