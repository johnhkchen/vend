---
id: T-054-03
story: S-054-01
title: deterministic-dual-runner-throw-equivalence
type: task
status: open
priority: high
phase: ready
depends_on: [T-054-02]
---

## Context

Prove a throwing-node spec yields an IDENTICAL clean GraphResult under the sequential runGraph and the concurrent runGraphConcurrent — closing the last unbuilt graph primitive on the E-046 DAG substrate and making the dispatcher total over node behavior (success, non-success outcome, AND exception).

_Advances: P3, P6, E-054: holds identically in both runners; graph cast always returns a clean GraphResult_

**Codebase-memory (use it — project coding-task suggestion):** confirm the dual-runner equivalence test
pattern with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via
`search_code`:
- `search_code(pattern: "runGraphConcurrent|runGraph|toEqual|GraphResult|skipped|halted", regex: true, mode: "files", path_filter: "graph-core.test")` — the `runGraphConcurrent == runGraph` equivalence pattern (E-048/E-049 asserted it for predicated/budgeted specs) to mirror for a throwing-node spec.

**Cites:** `src/engine/graph-core.test.ts` (the `runGraphConcurrent == runGraph` equivalence pattern,
E-048/E-049) + T-054-02; `src/engine/graph-core.ts` (`GraphResult`); `docs/active/epic/E-054.md`.

## Acceptance Criteria

- [ ] A deterministic test asserts the GraphResult (nodes / skipped / outcome / halted) for a spec with a throwing node and an independent sibling is equivalent across runGraph and runGraphConcurrent with a stub throwing thunk and no live model; the full `bun test` suite stays green.
