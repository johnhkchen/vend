---
id: T-055-01
story: S-055-01
title: svg-primitives-and-deterministic-layout
type: task
status: done
priority: high
phase: done
depends_on: []
---

## Context

Land the pure SVG-emitter primitives (XML-escaped <rect>/<text>/<line> builders) and the hand-rolled, dependency-light grid/swimlane geometry (group→column, card→box coordinates) the renderer will compose — the leaf layer, mirroring paper.ts's sanitizeId/mmLabel/helpers slice.

_Advances: P3, P5, E-055: deterministic hand-rolled grid/swimlane layout, no force-directed/external engine_

**Codebase-memory (use it — project coding-task suggestion):** confirm `paper.ts`'s helper/primitive slice
with the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "sanitizeId|mmLabel|escape|honest|deepFreeze|render", regex: true, mode: "full", path_filter: "^src/present/(paper|project)")` — the pure helper/primitive pattern (paper.ts's `sanitizeId`/`mmLabel`/escape) to mirror for the SVG emitters + the `Projection`/`ProjectionGroup`/`ProjectedCard` shapes the geometry lays out.

**Cites:** `src/present/paper.ts` (the helper/primitive precedent — purity, escaping); `src/present/project.ts`
(`ProjectionGroup`/`ProjectedCard` — the shapes the geometry positions); `docs/active/epic/E-055.md`.

## Acceptance Criteria

- [ ] Pure unit tests pass: given N groups of M cards the geometry returns deterministic non-overlapping box rects, the XML-escape helper neutralizes <, >, &, " in face text, and each primitive emits byte-identical strings for identical input; module imports no fs/clock/random (Date/Math.random absent).
