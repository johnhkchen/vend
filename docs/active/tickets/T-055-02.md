---
id: T-055-02
story: S-055-01
title: projection-to-svg-renderer-core
type: task
status: open
priority: high
phase: ready
depends_on: [T-055-01]
---

## Context

The pure `projectionToSvg(projection, overlays?) -> string` in src/present/ — the THIRD consumer of the E-021 Projection IR — rendering each ProjectionGroup as a labeled swimlane, each ProjectedCard as a box carrying its face text + color token, and each ProjectionLink as an edge, mirroring renderPaper's (Projection, overlays?) signature, purity, and honest-empty discipline exactly.

_Advances: P3, P5, E-055: realize a second visual surface over the multi-consumer Projection contract_

**Codebase-memory (use it — project coding-task suggestion):** confirm the renderer contract to mirror with
the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`:
- `search_code(pattern: "renderPaper|Projection|ProjectionGroup|ProjectedCard|ProjectionLink|overlays|honest", regex: true, mode: "full", path_filter: "^src/present/(paper|project)")` — `paper.ts`'s `(Projection, overlays?) -> string` signature + honest-empty + never-writes-back discipline to mirror EXACTLY as the third consumer.

**Cites:** `src/present/paper.ts` (`renderPaper` — the contract: signature, honest-empty, purity, one-way) +
T-055-01 (the SVG primitives/geometry); `src/present/project.ts` (the `Projection` IR consumed);
`docs/active/epic/E-055.md`.

## Acceptance Criteria

- [ ] Deterministic test suite (no live model) passes: exactly one <rect> per card, one group label per group, one <line>/edge per depends_on link, an empty projection renders a valid minimal <svg> (honest-empty), rendering the same frozen projection twice is byte-identical, and the input projection is returned reference-unchanged (never written back).
