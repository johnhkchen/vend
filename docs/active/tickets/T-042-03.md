---
id: T-042-03
story: S-042-02
title: doctor-cli-command
type: task
status: open
priority: high
phase: done
depends_on: [T-042-01, T-042-02]
---

## Context

Wire the `doctor` subcommand into the CLI — a pure parseDoctorArgs + USAGE entry and a thin dispatch arm that runs the probe, prints the rendered report, and exits with the computed code; mirrors the existing `work` dispatch and the pure-parse / lazy-import split.

_Advances: P2, P4, P3_

## Acceptance Criteria

- [ ] parseArgs(['doctor']) unit tests pass (bare `doctor` + unknown-flag→usage) and USAGE lists the doctor line; running `vend doctor` in a correctly-wired project prints all-green and exits 0; in a project with any broken dep it prints the named failing check + fix-it hint and exits non-zero with no stack trace.
