---
id: T-075-03-02
story: S-075-03
title: plain-empty-board-line
type: task
status: open
priority: medium
agent: codex
phase: ready
depends_on: []
---

## Context

Replace the cryptic '(no actions)' empty-board guidance (src/shelf/menu.ts renderMenu([])) with a plain line so the bare-vend header doesn't leak jargon when there is no demand.

_Advances: P3 — Gates are the contract; P5 — Local-first_

## Acceptance Criteria

- [ ] menu.test.ts and home.test.ts assert `renderMenu([])` and the empty-board Home path render the new plain line, with no '(no actions)' anywhere.
