---
id: T-001-03
story: S-001
title: budget-control
type: task
status: open
priority: high
phase: ready
depends_on: [T-001-01]
---

## Context

Budget is a **hard contract** (charter P7): a run is allocated time + tokens up
front and is accountable to them, both ways. For the single-shot slice, budget is
(a) a wall-clock allowance that becomes the seam's `timeoutMs`, and (b) a token
ceiling checked against the dispense `result.usage`. Exhaustion is a clean hard
stop with a clear **andon**, never a silent overrun. A separate module from the
seam (no shared files) so it runs parallel to T-001-02.

## Acceptance Criteria

- [ ] `src/budget/budget.ts` exports a `Budget` (time ms + token ceiling) and
      helpers: derive `timeoutMs` for the seam; `check(usage)` → `ok` |
      `exhausted` carrying the overage.
- [ ] An exhausted budget produces a typed, named outcome (an andon the runner can
      surface) — not a discarded console line.
- [ ] Pure and fully unit-tested; no network, no fs.
- [ ] **No import of the executor seam** — budget is composed by the runner, not
      welded to the seam.
