---
id: T-076-02-03
story: S-076-02
title: no-network-characterization-test
type: task
status: open
priority: high
agent: codex
phase: ready
depends_on: [T-076-02-02]
---

## Context

The defect shipped because unit tests mock the reviewer fetch and release acceptance never runs
a metered cast. Close exactly that gap: a characterization test that runs a full castPlay with
DEFAULT config and nothing listening on 11434 — the primary executor's dispense mocked, but
resolution, settlement, marker, ledger, and artifact paths all REAL — and asserts the cast
completes with cross-review inert. This is the field-failure shape as a permanent regression
bar.

_Advances: P3 — the gold-master pattern applied to the crash that shipped; honest boundary made
into a test_

## Acceptance Criteria

- [ ] Test runs castPlay end-to-end with default config, mocked primary dispense, no network
  listener assumptions beyond "11434 is not required"; asserts success outcome,
  `crossReviewSkipped` marker present, ledger line written, artifact/record consistent.
- [ ] A companion case provisions an unreachable reviewer and asserts the andon outcome + ledger
  line + no unhandled rejection (the T-076-02-01 behavior, proven at the castPlay level).
- [ ] If full fidelity proves impossible in-suite, the review artifact says exactly what is and
  isn't covered and why — no silent narrowing.
- [ ] `bun run check` green.
