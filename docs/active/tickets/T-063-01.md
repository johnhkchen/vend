---
id: T-063-01
story: S-063
title: vend-rb-own-tap-formula
type: task
status: open
priority: high
phase: ready
depends_on: [T-062-03]
---

## Context

Author `vend.rb` in its own tap (johnhkchen/homebrew-vend), mirroring lisa.rb — compiled-binary, sha-per-variant, MIT, no livecheck — so `brew install johnhkchen/vend/vend` selects, verifies, and installs the released tarball.

_Advances: P5, E-061: the tap formula selects/verifies/installs it the way lisa's does; brew-installable_

## Acceptance Criteria

- [ ] `brew install johnhkchen/vend/vend` on a fresh arm64-mac installs the binary, the formula's sha matches the release asset, and `vend --version` then reports the real semver.
