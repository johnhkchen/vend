---
id: T-047-02
story: S-047-01
title: settle-the-real-play-graph
type: feature
status: open
priority: high
phase: structure
depends_on: [T-047-01]
---

## Context

**Settle the live graph cast — prove the concurrency was real and read the first composition honestly.**
T-047-01 cast the `survey → [propose ×2] → capture-note` graph live; this judges it against the
project's standard (no over-claim). Four reads:

1. **Concurrency was real (the headline).** From the run-log / cast-log, show the **two proposes ran
   overlapping in time** (not sequential) — the `castGraph` wave dispatcher worked live. This is the
   thing `runChain` (sequential) and the stub example structurally cannot demonstrate. If they did NOT
   overlap (ran sequentially), say so plainly — the substrate ran but the concurrency claim is unproven.
2. **The join received both.** Confirm the `capture-note` join consumed **both** upstream epic paths
   (the multi-entry `NodeUpstreams`) and the note references both minted epics — the real multi-upstream
   join, live.
3. **Minted-epic quality + decompose status.** Assess the 2 epics the proposes minted: sound demand vs
   filler? `lisa validate` clean (E-043 — no orphan)? Are they decomposed or minted-only (the E-046
   partial-chain pattern)? Note whether they should be kept, decomposed, or are throwaway proof
   artifacts.
4. **P7 + the honest first-composition read.** Confirm the spend was bounded + clean (P7). Then read
   this first composed playbook honestly: it proves the substrate carries real plays + concurrency
   live, but it is **modest** (the current plays don't compose richly — E-046's "richer playbooks" are
   downstream). **No over-claim** that this is a profound playbook; **no under-claim** of what it proved
   (real concurrency + multi-upstream join, live).

**Board update:** crystallize E-047 on `demand.md` Frontier 3 — the substrate is now proven LIVE with
real plays + concurrency; what the first composition showed + its limits; any real-play wiring gaps
surfaced. Free + deterministic (reads what the cast wrote).

**Codebase-memory (use it — project coding-task suggestion):** confirm the run-log/concurrency read with
the **codebase-memory-mcp** (project `Volumes-ext1-swe-repos-vend`, indexed/ready) via `search_code`/
`query_graph`, not raw grep:
- `search_code(pattern: "startedAt|endedAt|wallClockMs|GraphResult|runGraph", regex: true, mode: "full", path_filter: "^src/(engine|log|ledger)/")` — the timestamps/result the concurrency proof reads.
Confirmed coordinates at authoring: run-log records (`startedAt`/`endedAt`) `src/log/run-log.ts`;
`GraphResult` `src/engine/graph-core.ts`; `work/T-047-01/graph-cast-log.md` (the run this settles).

**Cites:** `src/engine/graph-core.ts` (`GraphResult`); `src/log/run-log.ts` (the per-cast timestamps the
concurrency proof reads); `work/T-047-01/graph-cast-log.md` (the run); `docs/active/epic/E-046.md` (the
substrate); `docs/active/demand.md` (Frontier 3 — the board to update).

## Acceptance Criteria

- [ ] **Concurrency proof**: the two proposes shown to have run **overlapping in time** (the wave
      dispatcher, live) — or an honest record that they ran sequentially (concurrency unproven).
- [ ] **Join confirmed**: the `capture-note` join consumed **both** upstream epic paths; the note
      references both — the real multi-upstream join, live.
- [ ] **Minted-epic quality + decompose status** read (sound vs filler; `lisa validate` clean, E-043;
      decomposed vs minted-only); **P7** confirmed bounded + clean.
- [ ] An honest **first-composition** verdict (real concurrency + join proven live; modest playbook —
      no over-claim, no under-claim); `demand.md` Frontier 3 updated.
- [ ] `bun run check:*` green.
