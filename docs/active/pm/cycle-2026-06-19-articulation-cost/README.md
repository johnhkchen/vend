# PM cycle — articulation-cost (2026-06-19)

Archived snapshot of a completed discovery→processing cycle (see `../README.md` →
*Workspace layout*). The desk surveyed **where the builder's real friction was** and found
it wasn't autonomy — it was **articulation** (*"I can feel the work but can't formulate
it"*, opportunity O1 = 0.72, the widest gap on the desk). Processed 2026-06-19; the gate
was lowered after synthesis.

## What it produced — all promoted and cleared

The synthesized `proposed-batch.md` ranked the articulation lift first, and its top signals
were pulled and built:

- **`expand-fragment`** (recommended pull) → **E-016** (done) — a rough fragment → a
  grounded, priced signal.
- **Survey play** → **E-017** (done) — `expand` at project scale: read a project → a ranked
  staged board.
- **Steer play** → **E-018** (done) — board + the genuine human forks.

So the articulation trilogy that automates STEER traces directly to this cycle. The strategic
read also held: articulation was *not* trust-gated, so it shipped while the macro-wallet was
parked — and dogfooding it accrued the forward-E1 data the wallet needed.

## Contents (discovery desk, frozen)

- `proposed-batch.md` — the synthesized ranked batch (the cycle's output).
- `brainstorm-lower-articulation-cost.md`, `cognitive-overhead-articulation.md` (O1–O4
  opportunity scores), `job-stories-articulation.md` — the articulation discovery.
- `articulation-block-utilization.md`, `brainstorm-block-utilization-depth.md` — the
  block-utilization thread (how a 2-hour block actually gets spent).
- `Outcome-Roadmap-2026.md` — the outcome-roadmap lens used to rank.

*(Some done epic cards — E-016 esp. — cite these by filename as provenance; the files still
resolve here in the archive. Visual-surface prep — `linear-surface-*.md`,
`job-stories-visual-surface.md` — deliberately stayed at the desk root: it's cited by live
`src/present/` and is prep for the still-pending Linear renderer, so it's a living anchor,
not a closed-cycle artifact.)*
