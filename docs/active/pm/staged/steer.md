# Steer — staged board + forks

A ranked demand board read off the whole project, highest-leverage first. Un-promoted: review and pull a row.

| Signal | Value | Budget (envelope) | Status |
|---|---|---|---|
| **Build the typed multi-node DAG — plays composing into a real graph (fan-out, join, conditional) beyond the linear propose→decompose chain.** — The literal architectural centerpiece of the v1 vision ('typed, graph-structured agent orchestration') is still unbuilt; the engine casts only single plays + one linear chain. Closes the largest remaining vision-distance and is the substrate the open-model runner wants underneath it. | **Keystone** | several blocks (>2h) | ready (advances [core feature — typed, graph-structured agent orchestration (the v1 vision)] · grounded in demand.md Frontier 3 'Multi-node DAG … the architectural centerpiece of typed, graph-structured agent orchestration still unbuilt. High/Keystone'; snapshot src/** shows no graph module.) |
| **Ship the driveable hackathon `examples/` template wired to `vend init --template <name>` — a domain seed → driven board with a gold-mastered expected outcome.** — Closes the onboarding gap (the board was silent on getting Vend onto a new project) AND compounds the autonomy-loop keystone — more driveable projects → more cleared runs → forward-E1 accrual that ungates the wallet. Also the test of assumption A3 (steer/survey useful off a thin seed, not just this repo). | **High** | ~1–2 blocks | ready (advances [P5, core feature — feeds the P4/P7 keystone (cleared forward-E1)] · grounded in demand.md Frontier 7 'a driveable hackathon examples/ template'; PRD-distribution-onboarding.md §8 'v1.1 — the value proof' + §7.4 A3; run-log obs 22996 (PM 2026-06-20 build-ready brief). Unblocked now that vend init (E-040) shipped.) |
| **Build an agentic open-model runner — an open model autonomously reading the repo, running tools, iterating turns, and materializing a ticket behind the Executor seam.** — Proves P6 (executor-agnostic) end-to-end. Both execution (E-035) and authoring (E-036) seams are config-capable, but no open model autonomously clears work yet — model-plurality made real, not just declared. | **High** | several blocks (>2h) | blocked: likely wants the multi-node DAG (Frontier 3) underneath it (advances [P6] · grounded in demand.md Frontier 2 'Agentic open-model runner (the deferred remainder of E-035/E-036) … the real lift that proves P6 end-to-end. (likely wants Frontier 3 underneath it)'.) |
| **Build the Linear renderer — project the work-graph into Linear (read-only, one-way authority) for non-dev teammates.** — Opens the visual-thinking segment and advances P5 (the non-dev round-trip); downstream of E-021's MCP-independent projection slice. Respects N2/N4 — read-only, Vend keeps authority. | **High** | ~2 blocks | ready (advances [P5] · grounded in demand.md Frontier 4 'Linear renderer — project the work-graph into Linear for non-dev teammates (downstream of E-021's MCP-independent slice)'.) |
| **Build the annotation→demand round-trip — a teammate's annotation flows back into the board as a priced signal (reuses expand-fragment).** — Closes the non-dev loop (P5) so visual thinkers feed demand back without dev tooling — the inbound half of the Frontier 4 round-trip. | **High** | ~2 blocks | blocked: pairs the Linear renderer (the outbound projection) (advances [P5] · grounded in demand.md Frontier 4 'Annotation → demand round-trip … reuses expand-fragment. Closes the loop.') |
| **Ship a single-binary delivery path — a Homebrew formula and/or `bun --compile` build so Vend installs without a dev toolchain.** — Removes the install barrier for new users (P5 local-first onboarding); pairs the examples template to make Vend genuinely grab-and-go off a machine, not just a cloned repo. | **Standard** | ~1–2 blocks | ready (advances [P5] · grounded in demand.md Frontier 7 'Homebrew / bun --compile delivery'.) |
| **Thread the structured stop-reason onto the run record so honest-empty and budget-exhausted stops are countable (not stdout-only).** — Unblocks clean consistency/trust measurement — the probe + `vend audit` currently can't split honest-empty from budget-exhausted, and trust measurement is exactly what the keystone cadence is accruing. | **Standard** | small (~1h) | ready (advances [P3, core feature — verifiable clearing (countable stop-reasons)] · grounded in demand.md Frontier 6 'Thread the structured stop-reason onto the run record … so it's countable. Standard. Ready — small (~1h)'.) |
| **Let a play declare which model/BAML-client it runs on — per-play executor selection.** — The natural successor to E-032 (per-play tooling) + E-035/E-036's selection seams; enables cost-routing (cheap client for screening, Opus for decomposition) and is an enabler for the open-model runner above. | **Standard** | ~1 block | ready (advances [P6] · grounded in demand.md Frontier 3 'Per-play executor / BAML-client selection … the natural successor to E-032 + E-035/E-036's selection seams. Standard.') |

## Pull these

A human pulls any one staged signal onto the board with one gesture:

```
vend chain "Build the typed multi-node DAG — plays composing into a real graph (fan-out, join, conditional) beyond the linear propose→decompose chain. — The literal architectural centerpiece of the v1 vision ('typed, graph-structured agent orchestration') is still unbuilt; the engine casts only single plays + one linear chain. Closes the largest remaining vision-distance and is the substrate the open-model runner wants underneath it."   # recommended next pull (highest leverage)
vend chain "Ship the driveable hackathon `examples/` template wired to `vend init --template <name>` — a domain seed → driven board with a gold-mastered expected outcome. — Closes the onboarding gap (the board was silent on getting Vend onto a new project) AND compounds the autonomy-loop keystone — more driveable projects → more cleared runs → forward-E1 accrual that ungates the wallet. Also the test of assumption A3 (steer/survey useful off a thin seed, not just this repo)."
vend chain "Build an agentic open-model runner — an open model autonomously reading the repo, running tools, iterating turns, and materializing a ticket behind the Executor seam. — Proves P6 (executor-agnostic) end-to-end. Both execution (E-035) and authoring (E-036) seams are config-capable, but no open model autonomously clears work yet — model-plurality made real, not just declared."
vend chain "Build the Linear renderer — project the work-graph into Linear (read-only, one-way authority) for non-dev teammates. — Opens the visual-thinking segment and advances P5 (the non-dev round-trip); downstream of E-021's MCP-independent projection slice. Respects N2/N4 — read-only, Vend keeps authority."
vend chain "Build the annotation→demand round-trip — a teammate's annotation flows back into the board as a priced signal (reuses expand-fragment). — Closes the non-dev loop (P5) so visual thinkers feed demand back without dev tooling — the inbound half of the Frontier 4 round-trip."
vend chain "Ship a single-binary delivery path — a Homebrew formula and/or `bun --compile` build so Vend installs without a dev toolchain. — Removes the install barrier for new users (P5 local-first onboarding); pairs the examples template to make Vend genuinely grab-and-go off a machine, not just a cloned repo."
vend chain "Thread the structured stop-reason onto the run record so honest-empty and budget-exhausted stops are countable (not stdout-only). — Unblocks clean consistency/trust measurement — the probe + `vend audit` currently can't split honest-empty from budget-exhausted, and trust measurement is exactly what the keystone cadence is accruing."
vend chain "Let a play declare which model/BAML-client it runs on — per-play executor selection. — The natural successor to E-032 (per-play tooling) + E-035/E-036's selection seams; enables cost-routing (cheap client for screening, Opus for decomposition) and is an enabler for the open-model runner above."
```

## Forks

The genuine decisions only the human can make — each recommendation-first. Assent or override:

### Fork — Once the in-flight cadence sweep (E-045) ungates the autonomy loop, which concrete product epic gets the next fat-envelope allocation — the typed multi-node DAG, or the Frontier 7 onboarding bundle (examples template + single-binary delivery)?
- **Why it matters:** The DAG closes the most vision-distance — it is the literal v1 'typed, graph-structured orchestration' centerpiece — but it is several blocks and architecturally risky. Frontier 7 is build-ready, PM-staged, lower-risk, and directly compounds the autonomy-loop keystone you are mid-accruing (more driveable projects → more cleared runs → forward-E1 toward the ≥10 bar). The call is depth-of-vision now vs. trust-and-adoption momentum now, and it sets which payoff lands first.
- **Options:**
  1. Frontier 7 onboarding — ship the driveable examples template + Homebrew/bun --compile delivery (build-ready, PM-staged, smaller, compounds the keystone)
  2. Multi-node DAG — build the v1 architectural centerpiece (typed graph: fan-out / join / conditional) now (most vision-distance, but several blocks and architecturally risky)
  3. Open-model runner (P6) — but this likely wants the DAG underneath it first, so it implies sequencing the DAG anyway
- **Vend recommends:** Pull Frontier 7 onboarding next (option 1) — it is build-ready and the highest-leverage move that compounds the keystone instead of competing with it; hold the DAG as the subsequent fat-envelope architectural pull once the wallet is fully ungated and the open-model runner needs the substrate.

_Staged by Vend's `steer` play — not promoted; pull a signal / assent to a fork to clear._
