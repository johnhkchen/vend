# Survey — staged demand board

A ranked board read off the whole project, highest-leverage first. Un-promoted: review and pull a row.

| Signal | Value | Budget (envelope) | Status |
|---|---|---|---|
| **Run the measurement sprint: cast ≥10 real runs with `vend run --intervened`/`--no-intervened`, then `vend audit` for the E1 walk-away rate + trend (E2 variance already read at ~21%).** — It is the named TOP signal and the one thing gating the macro-wallet keystone — E-014 returned HOLD pending E1 forward data; collecting it flips the go/reroute verdict and unblocks the whole 2-hour mechanic. Unmeasured, not weak — but the wallet stays parked until it reads. | **Keystone** | small (~0.5–1h, a human sweep not a build — instruments already shipped E-014) | ready (advances [P4, P3] · grounded in demand.md 'Not yet pulled' lines 107–112 (Measurement sprint, top signal, obs 21210) + E-014 HOLD verdict line 70 ('E1 walk-away still needs forward self-reported runs → HOLD holds')) |
| **Build the 'work for 2 hours' macro-wallet: a feature-block budget allocated at the counter that vend spends DOWN autonomously across casts (pull/chain until exhausted or board cleared), each cast fit into the remaining budget at its measured price.** — The founding gesture made literal — the rung directly above E-013 (measured envelopes, done) and the core-feature capstone of P2/P4/P7. Highest vision-distance of any unbuilt move: it turns the proven engine into the actual product promise. | **Keystone** | ~2–3h / several blocks | blocked: E-014 go verdict (the measurement sprint above must read first) (advances [P2, P4, P7] · grounded in demand.md 'Not yet pulled' lines 113–126 (the macro mechanic; 'Keystone; E-013 landed'; '⚠ Gated by E-014 … Status: HOLD')) |
| **Do the deferred E-017 human sweep: cast `vend survey` live on this repo, confirm a ranked board lands at `docs/active/pm/staged/survey-board.md`, verify the empty-board-writes-a-file behavior is intended, and recalibrate `surveyPlay.budget` from the logged actuals.** — E-017 is 'active', not done — AC#4 (the live cast) is the explicitly deferred proof; until it runs, Survey's real-world board quality, the honest-empty product behavior, and its 300k budget floor are unvalidated. This closes the cold-start half of the experience and accrues E1 data. | **High** | small (~1h, a sweep) | ready (advances [P2, core feature: the run-0 cold-start clearing (Survey live)] · grounded in T-017-02 review.md Recommendation ('Ready for the human sweep…') + AC#4 deferred + open concerns #2 (empty-board materializes) and #3 (budget is a generous floor, not calibrated)) |
| **Recalibrate every play's cold-start token envelope from measured run-log tails via `vend envelope <play>` (batch #5): expand's 100k that spent 211k, survey's uncalibrated 300k floor, and the other plays E-015 flagged.** — P7 (budget is a hard contract) is only honest if the prices are measured, not guessed — a recurring grounded defect across plays where cold-start floors are guesses. E-013's recalibration core exists; this is applying it once the logs have tails, so accept-the-default becomes earned. | **High** | small (~1h, recalibration pass per E-013) | ready (advances [P7] · grounded in demand.md line 71 E-016 finding (1) 'expand's cold-start budget too thin (100k set, 211k spent) … folds into batch #5'; line 95 E-015 'Likely the other plays' cold-start envelopes need the same'; T-017-02 review concern #3) |
| **Run a run-to-run consistency probe on the demand-extraction plays (expand/survey) the way `run-probe.ts` did for decompose — measure variance and check whether the honest-empty gate fires over-eagerly.** — The product IS consistency, and E-016 already observed 3 casts on the same fragment giving signal / budget-blow / honest-empty — Survey reads the WHOLE project, an even larger variance surface. Unmeasured variance in the articulation plays directly threatens the core value prop and P3. | **High** | ~1 block (≈2h; a 5×N sweep + analysis, reusing run-probe.ts / variance.ts) | ready (advances [P3, core feature: consistency over a probabilistic process] · grounded in demand.md line 71 E-016 finding (2) 'high run-to-run variance … the honest-empty looked over-eager; a consistency note, cf. E-014' + T-017-01 review gap 'run-to-run consistency … unmeasured here by design') |
| **Hold the design-language session: assemble the project-wide visual surface (amber-andon palette per IA-9, run-stream, budget meter, shelf cards) into a capped design charter downstream of the IA spine.** — The vision's v1 surface is a TUI and nothing visual exists yet — this generative session is the prerequisite that precedes any TUI epic, converting the captured IA spine into a buildable look. Without it the two-gesture counter has no surface to land on. | **High** | ~1 block (≈2h) | ready (advances [P2, core feature: the v1 terminal counter (the two-gesture surface)] · grounded in demand.md 'Not yet pulled' lines 134–137 (Design-language session, 'High; precedes any TUI epic'); vision/stack 'v1 surface is a terminal TUI'; source tree has zero TUI modules) |
| **Grow the chain from the 2-node propose→decompose composition into the typed multi-node DAG: arbitrary play graphs cast through the one metered seam.** — The vision is 'typed, graph-structured agent orchestration', but the engine only composes a single hardcoded pair today (chain-propose-decompose.ts). The general DAG closes a large slice of vision-distance and is repeatedly named as the next capability on the proven engine. | **High** | several blocks | ready (advances [core feature: typed graph-structured orchestration, P6] · grounded in demand.md lines 83–85 ('then the multi-node DAG'); src/play/chain-propose-decompose.ts header ('the propose→decompose chain … the CAPSTONE') — only a 2-node chain exists) |
| **Slot a second executor behind the executor interface to prove P6 (open-model executor alongside Claude Code).** — Charter P6 says the design never assumes Claude Code is the only executor, but src/executor/ holds only claude.ts — the abstraction is asserted, not demonstrated. A second executor is the verification that the seam actually holds, de-risking the executor-agnostic promise. | **Standard** | ~2–3h | ready (advances [P6] · grounded in charter P6 'Executor-agnostic underneath'; demand.md line 85 'open-model executors' (far-horizon pull); src/executor/ contains only claude.ts (verified)) |

## Pull these

A human pulls any one staged signal onto the board with one gesture:

```
vend chain "Run the measurement sprint: cast ≥10 real runs with `vend run --intervened`/`--no-intervened`, then `vend audit` for the E1 walk-away rate + trend (E2 variance already read at ~21%). — It is the named TOP signal and the one thing gating the macro-wallet keystone — E-014 returned HOLD pending E1 forward data; collecting it flips the go/reroute verdict and unblocks the whole 2-hour mechanic. Unmeasured, not weak — but the wallet stays parked until it reads."   # recommended next pull (highest leverage)
vend chain "Build the 'work for 2 hours' macro-wallet: a feature-block budget allocated at the counter that vend spends DOWN autonomously across casts (pull/chain until exhausted or board cleared), each cast fit into the remaining budget at its measured price. — The founding gesture made literal — the rung directly above E-013 (measured envelopes, done) and the core-feature capstone of P2/P4/P7. Highest vision-distance of any unbuilt move: it turns the proven engine into the actual product promise."
vend chain "Do the deferred E-017 human sweep: cast `vend survey` live on this repo, confirm a ranked board lands at `docs/active/pm/staged/survey-board.md`, verify the empty-board-writes-a-file behavior is intended, and recalibrate `surveyPlay.budget` from the logged actuals. — E-017 is 'active', not done — AC#4 (the live cast) is the explicitly deferred proof; until it runs, Survey's real-world board quality, the honest-empty product behavior, and its 300k budget floor are unvalidated. This closes the cold-start half of the experience and accrues E1 data."
vend chain "Recalibrate every play's cold-start token envelope from measured run-log tails via `vend envelope <play>` (batch #5): expand's 100k that spent 211k, survey's uncalibrated 300k floor, and the other plays E-015 flagged. — P7 (budget is a hard contract) is only honest if the prices are measured, not guessed — a recurring grounded defect across plays where cold-start floors are guesses. E-013's recalibration core exists; this is applying it once the logs have tails, so accept-the-default becomes earned."
vend chain "Run a run-to-run consistency probe on the demand-extraction plays (expand/survey) the way `run-probe.ts` did for decompose — measure variance and check whether the honest-empty gate fires over-eagerly. — The product IS consistency, and E-016 already observed 3 casts on the same fragment giving signal / budget-blow / honest-empty — Survey reads the WHOLE project, an even larger variance surface. Unmeasured variance in the articulation plays directly threatens the core value prop and P3."
vend chain "Hold the design-language session: assemble the project-wide visual surface (amber-andon palette per IA-9, run-stream, budget meter, shelf cards) into a capped design charter downstream of the IA spine. — The vision's v1 surface is a TUI and nothing visual exists yet — this generative session is the prerequisite that precedes any TUI epic, converting the captured IA spine into a buildable look. Without it the two-gesture counter has no surface to land on."
vend chain "Grow the chain from the 2-node propose→decompose composition into the typed multi-node DAG: arbitrary play graphs cast through the one metered seam. — The vision is 'typed, graph-structured agent orchestration', but the engine only composes a single hardcoded pair today (chain-propose-decompose.ts). The general DAG closes a large slice of vision-distance and is repeatedly named as the next capability on the proven engine."
vend chain "Slot a second executor behind the executor interface to prove P6 (open-model executor alongside Claude Code). — Charter P6 says the design never assumes Claude Code is the only executor, but src/executor/ holds only claude.ts — the abstraction is asserted, not demonstrated. A second executor is the verification that the seam actually holds, de-risking the executor-agnostic promise."
```

_Staged by Vend's `survey` play — not promoted; pull to clear._
