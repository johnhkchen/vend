---
id: E-005
title: thread-real-model-id
status: open
advances: [P3]
serves: >
  Log the true model id on every run so the consistency layer reads truth, not a
  sentinel.
---

## Intent — the bigger-picture play

`runs.jsonl` records `model: "claude-cli-default"` while the real id
(`claude-opus-4-8[1m]`) rides on the terminal `result`. Thread the real id through the
runner into the run-log record.

## Value to the design

Data fidelity for the steering / consistency layer (**P3** — the ledger must be
trustworthy; "you got what you paid for" needs the *real* model). Cheap and small.

## Done looks like

- `runs.jsonl` `model` carries the id read off the terminal `result`, not the sentinel.
- Falls back to the sentinel only when the result carries no model id.

## Context & constraints

- Source: `docs/active/work/T-002-04/proof.md` kaizen #3.
- Touches `src/play/decompose-epic.ts` (model stamping) and the executor result type.
- Tiny — likely a single ticket's worth.
