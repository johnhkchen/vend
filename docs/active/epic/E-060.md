---
id: E-060
title: fresh-seed-full-slice-clear
status: done
kind: sorcery
advances: [P2, P5, P7]
serves: >
  A designer's two gestures clear a real slice end-to-end on a fresh seed — the non-dev round-trip becomes whole and re-runnable, not just a rendered board.
---

```
fresh-seed-full-slice-clear   {2}{W}{U}
sorcery — white, blue   (rarity: rare)
```

_Proposed by Vend's `propose-epic` play._

## Intent — the bigger-picture play

E-058 drove the hackathon seed live and the A3 risk materialized honestly: on a fresh seed the board can render but a slice does not clear. E-059 is wiring SEED.md intent into steer so the board RENDERS (finding #1); it deferred findings #2 and #3 as separate signals. This epic closes those two so `vend work` on a fresh seed actually clears ≥1 slice. (#3) decompose must graceful-degrade when `codebase-memory-mcp` is absent — proceed without it and log an honest reduced-grounding note rather than firing a missing-capability andon, since the make-or-break steer→board path never needs the MCP and requiring it raises onboarding friction against P2/P5. (#2) the seed's cold-start budget envelope must be calibrated from the run-log fat tails (E-013/recalibrate, measured not guessed) and wired as the hackathon-seed drive default, so a tight two-gesture `--budget` funds a real cold-start clear instead of colliding instantly with the P7 hard budget contract. Close with a LIVE re-drive on a sandboxed seed copy that clears a real slice and flips the gold master to its positive form. This is the play that makes the Set-B non-dev channel a completed round-trip; without it the channel renders but never delivers.

## Value to the design

Realizes the first whole fresh-seed clear: removes the two blockers that let a designer get a board but no work. Graceful-degrade keeps the clearing chain local-first and frictionless on a fresh project (P5), the calibrated seed budget makes the two-gesture transaction actually fundable on the cold-start path (P2 under the P7 contract), and the live re-drive accrues a cleared forward-E1 record on the Set-A autonomy cadence — directly advancing the core feature (clearing intent into allocatable work) on exactly the path the non-dev bet depends on.

## Done looks like

On a fresh seed with no codebase-memory-mcp present, the full two-gesture drive (vend steer → vend work) clears ≥1 real slice end-to-end: decompose proceeds without andon and the run record carries an honest reduced-grounding marker; the seed's default budget funds the cold-start vend work to a slice clear with no instant budget-exhausted; and a LIVE closing re-drive on a sandboxed seed copy produces a run-log record plus a cleared forward-E1, with examples/templates/hackathon-seed/EXPECTED-OUTCOME.md updated to the positive gold master (board renders AND a slice clears).

## Context & constraints

Grounds against the E-058 gold master (examples/templates/hackathon-seed/EXPECTED-OUTCOME.md, work/T-058-05/) and E-059's in-flight steer wiring. PREREQUISITE: E-059 must land first — the board must render before a slice can clear off it; this epic is strictly downstream, pull only after E-059. Resolved fork (carried, vend-recommended): graceful-degrade — NOT ship-the-MCP (onboarding friction vs P2/P5) and NOT hard-gate (blocks the seed loop until the user installs); a decompose-time override may revisit. Keep IA-8 honest: the budget quote stays the p90 price, the funding guard is not the price (E-050 funding-headroom); calibrate the envelope from E-013/recalibrate run-log tails, do not hand-pick. The reduced-grounding signal should ride the run record (pairs Frontier 6 stop-reason threading) so a degraded clear is countable, not invisible. The degrade stays a missing-optional-capability branch where tool-resolution already lives — not a new andon path. In bounds: violates no non-goal (still a gated playbook clear, N3; vend clears and lisa executes, N4). Out of scope: the steer/board render wiring itself (E-059), shipping or bundling the MCP, and any deep-grounding quality work beyond the honest reduced-grounding marker. Right-sized as one ~2-hour block: two scoped fixes plus one closing live drive; if the budget calibration proves to need its own recalibrate tooling beyond reading existing tails, that spins out rather than inflating this card.
