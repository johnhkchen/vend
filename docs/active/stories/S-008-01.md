---
id: S-008-01
title: uncommitted-code-gate
type: story
status: open
priority: high
tickets: [T-008-01, T-008-02]
---

Part of **epic E-008 (done-means-committed)** — the D-005 durable fix: a
`check:committed` gate, fired by a lisa hook, that fails when source sits
uncommitted so "done" can't diverge from git truth.

Shared contract first (R12 → the `check:committed` script), then the enforcement
point (the hook). Epic-scoped ids (R6 — `008` is clean).

## DAG

```
T-008-01  check:committed script (the shared gate definition — the Central Rule)
   └─ T-008-02  wire the lisa on-stop hook to invoke it (the enforcement point)
```

**Out of this slice** (follow-ups): a container-side *HEAD-builds* gate (catch
inconsistent partial commits), and hard-blocking if the hook can only warn.
