---
id: S-015-01
title: bound-dispense-exploration
type: story
status: open
priority: high
tickets: [T-015-01, T-015-02]
---

Part of **epic E-015 (bound-dispense-exploration)** — add `--max-turns` at the
`claude -p` dispense seam so agentic wandering is bounded mid-flight, shrinking the
token fat-tail E-014's E2 probe quantified (decompose blew its 50k envelope ~80% of the
time). Makes the token budget enforceable mid-flight (P7 / IA-8) and de-risks the
macro-wallet's per-cast accounting.

Mechanism first (R3/R12 — the pure `--max-turns` flag threaded through the seam), then
the policy + observability (R1 — a warranted default + turns-used logging, live-verified
to shrink the tail). Epic-scoped ids (R6 — `015` clean).

## DAG

```
T-015-01  --max-turns mechanism (buildArgs + dispense + CastOptions, pure flag tested)
   └─ T-015-02  warranted default + turns-used observability + live bound-check
```

**Out of this slice:** the system-prompt economy lever (separate angle); per-play turn
budgets; decompose's envelope recalibration (E-013's domain).
