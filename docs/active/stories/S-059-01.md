---
id: S-059-01
title: seed-intent-into-steer
type: story
status: open
priority: high
tickets: [T-059-01, T-059-02]
---

Part of **epic E-059 (seed-intent-reaches-steer)** — the make-or-break wire. `vend steer` reads its steering
context from `docs/knowledge/charter.md` + a board-ids snapshot (`src/play/steer.ts:109`); the seed's product
idea lives in a root `SEED.md` no code reads, so a fresh seed steers honest-empty (E-058 gold master). Thread
the intent into steer's input through the existing go-and-see snapshot (`buildProjectSnapshot`) — the string
the prompt already grounds demand against — so a one-line `SEED.md` becomes a readable demand gradient. No
BAML change (intent rides inside `{{ project }}`). Coupled gap: the seed's tuned charter sits at root but
`vend init` overlays a generic stub into the path steer reads — enrich the template overlay to write the tuned
charter to `docs/knowledge/charter.md`. Honest-empty preserved (no `SEED.md` ⇒ byte-identical). FREE.

## DAG

```
T-059-01  buildProjectSnapshot gains optional intent; steer/survey read root SEED.md tolerantly; pins present+absent; honest-empty byte-identical
   └─► T-059-02  TEMPLATE_REGISTRY.hackathon overlays the tuned charter onto docs/knowledge/charter.md (mergeManifests overrides the base stub)
```
