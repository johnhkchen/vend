---
id: S-036-01
title: open-model-baml-support
type: story
status: open
priority: high
tickets: [T-036-01, T-036-02]
---

Part of **epic E-036 (open-model-baml-support)** — the authoring-layer half of open-model support:
declare an open-model BAML client (`OpenModelStub`, provider `openai-generic` — the universal
OpenAI-compatible shape, matching E-035's executor) and show Vend's render targets it (request-shaping
in openai format, a real difference from `ClaudeStub`'s anthropic format) while SAP-parse stays
**provider-agnostic**. Paired with E-035, the full open-model path is config-level capable: render
(BAML) → dispense (executor) → parse (BAML). Scoped by the pull ("showing support via BAML should
suffice"); the **live agentic runtime is the deferred remainder**, named not claimed.

Build client+render → coherent path+boundary: T-036-01 the `OpenModelStub` client + render-targets-it
(baml:gen green; openai-format request via the subprocess bridge) → T-036-02 follow E-035's executor
selection so render-client tracks it + parse-is-agnostic proof + record the boundary in `stack.md`.
Epic-scoped ids (R6 — `036` clean).

## DAG

```
T-036-01  OpenModelStub (openai-generic, render-only) + render-targets-it (openai format, bridge-proven)
   └─ T-036-02  render-client follows VEND_EXECUTOR (E-035) + parse-is-agnostic + record the boundary (stack.md)
```

**Out of this slice:** the agentic open-model runtime (the runner that autonomously executes a play on
an open model — the real heavy lift, deferred); BAML as live transport (stays render-only — dispense
rides E-035's `Executor`); per-play BAML client selection (scope creep); a hosted-API client with
committed secrets (local-first, env-config).
