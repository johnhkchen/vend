---
id: S-067-01
title: snapshot-charter-codes-at-cut
type: story
status: open
priority: high
tickets: [T-067-01-01, T-067-01-02, T-067-01-03]
---

**Scope:** Charter-code resolution at the cut point, end to end: a new pure resolver module (charter text → code-to-one-liner snapshot map), materialize.ts's render pair and impure verb (charter threaded in as a parameter, the bare `_Advances: P4_` line replaced with code-plus-carried-text), and the decompose runner that supplies the charter it already holds for ClearContext. gates.ts's bounds gate and the BAML decompose prompt are untouched; only the write side changes shape.

**Story acceptance:** Casting decompose-epic against a fixture epic through the stub executor writes ticket and story files where every cited P/N code carries its charter one-line text inline with the code kept (e.g. 'P4 — Autonomy by default, not supervision'), a grep of the written bodies for a bare unexplained code comes back empty, and a cut whose charter cannot resolve a cited code is refused with a named andon before any file is written — zero partial output.

**Honest boundary:** Everything here is fixture-proven and FREE (stub executor, golden render tests, no tokens). Deliberately deferred: retroactively patching artifacts already on the board (history keeps its bare codes), read-time stripping in downstream tooling (the write-side fix makes it unnecessary — that is the point), and the live metered gold-master cast that closes the epic, authorized by the human at the counter and named here rather than hidden in a ticket.

## DAG

```
T-067-01-01  charter-code-snapshot-resolver
T-067-01-02  materialize-carries-code-text-at-cut  ← T-067-01-01
T-067-01-03  bare-code-write-guard  ← T-067-01-02
```

Wave rationale: The resolver ticket runs alone first — both consumers build on its settled code→one-liner contract. The materialize integration and the write guard then run sequentially, not fanned out: both edit materialize.ts (overlapping file), and the guard asserts over the exact rendered bodies the integration produces, so its shape isn't settled until the integration lands.

**Out of this slice:** Amending or restructuring the charter itself (it stays the single mutable anchor; snapshots capture, they don't fork). Backfilling snapshots into already-cut stories/tickets. The bounds gate's dangling-ref check (already lives in gates.ts, unchanged). N-code stripping or any other normalization in lisa/kitchen read-side tooling.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-10._
