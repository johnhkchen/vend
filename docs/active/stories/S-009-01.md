---
id: S-009-01
title: propose-epic-on-the-engine
type: story
status: open
priority: high
tickets: [T-009-01, T-009-02, T-009-03]
---

Part of **epic E-009 (register-propose-epic)** — register **ProposeEpic** (signal →
epic card) as a real play on the E-007 engine, casting through the same `castPlay` as
DecomposeEpic and capture-note. Completes the **signal → epic → tickets** pipeline.

Pure cores first (R1): the BAML contract + the pure gates/render before the register +
effect + cast. Epic-scoped ids (R6 — `009` is clean). Mirrors the CaptureNote pattern
(`src/play/note.ts`) — a registered play with its own `assemble*Inputs` + `cast*` entry.

## DAG

```
T-009-01  baml ProposeEpic(signal, charter, project) -> EpicCard
   └─ T-009-02  propose-core: pure PE gates (value/bounds/structural, id-disjoint via
                E-004's guard) + EpicCard → markdown render
         └─ T-009-03  register proposeEpicPlay + effect (write the card) + live cast
```

**Out of this slice** (follow-ups): chaining ProposeEpic → DecomposeEpic in one press,
the full `vend run <any-play>` input-assembly generalization.
