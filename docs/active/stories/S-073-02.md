---
id: S-073-02
title: enforce-cross-review-clear-gate
type: story
status: open
priority: high
tickets: [T-073-02-01, T-073-02-02]
---

**Scope:** The run-level clear/refuse decision: consume the cross-vendor verdict recorded by S-073-01 and make a FAIL a blocking gate outcome in the cast classify/clear path (src/engine/cast-core.ts classify → gate rows, run-log.ts outcome 'gate-failed'). A PASS clears with the verdict attached; no complement seat means the gate is inert. Also the end-to-end refusal demonstration.

**Story acceptance:** A completed cast whose complement-seat verdict is FAIL does NOT settle as cleared — the run records a gate-failed outcome carrying the cross-review verdict; a PASS verdict clears with the verdict attached; a run with no complement seat clears normally (gate inert). An intentionally-bad diff routed to a reviewing stub primed to refuse is demonstrably blocked end to end, with the refusing cross-vendor verdict on the ledger line.

**Honest boundary:** Enforcement is fixture-proven and FREE — the reviewing verdict is supplied by a stub executor (primed-pass / primed-fail); the live cross-vendor refusal against a real second-vendor endpoint is the deferred metered proof, authorized at the counter and named here. No human-approval step is introduced: this is an autonomous gate event (N2 — Not a babysitting dashboard/P4 — Autonomy by default, not supervision), not a dashboard. This story assumes the diff-capture and verdict-record substrate from S-073-01 is settled.

## DAG

```
T-073-02-01  gate-clear-on-cross-review-verdict  ← T-073-01-04
T-073-02-02  prove-cross-review-refusal-path  ← T-073-02-01
```

Wave rationale: The gate-enforcement ticket runs alone first because it defines the single clear/refuse seam the demonstration depends on; the refusal-path demonstration ticket depends on it. The story is intentionally serial — the enforcement decision is one seam and its proof must exercise the finished decision.

**Out of this slice:** Diff capture and verdict routing (S-073-01, already landed); per-playbook rubric authoring; adding new executors; the release-day gold-master bake-off.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
