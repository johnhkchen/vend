---
id: S-082-02
title: learned-capacity-quota-fraction-heat
type: story
status: open
priority: high
tickets: [T-082-02-01, T-082-02-02]
---

**Scope:** A new pure capacity-learning module in src/play/ (sibling to lane-heat.ts, over RunRecord and run-log's cost-weighted totalTokens) plus src/play/lane-heat.ts's inference and evidence string. Consumers (cast.ts, play.ts, decompose-effect.ts) carry InferredSeat.reason verbatim into the seatInferred ledger marker, so provenance upgrades without touching them; the ledger schema is consumed here, never changed.

**Story acceptance:** Over fabricated ledgers carrying cap markers, Vend states each lane's learned capacity per reset window and its current quota-fraction sourced only from those records, and inferDefaultSeat's reason reads in quota-fraction terms ('claude at ~85% of learned window'); over ledgers with no cap evidence for a lane, capacity is explicitly unlearned — never an invented number — and the entire existing lane-heat test suite passes unchanged, proving the relative-burn fallback keeps E-071's behavior byte-compatible.

**Honest boundary:** All inference is fixture-proven and FREE (fabricated records, no tokens) and reads the LOCAL ledger only (P5 — Local-first) — on this repo's real ledger the quota denomination stays honestly dormant until live cap events accrue through S-082-01's marker; that dormancy is the design, not a gap. Deferred by name: pricing counter-gesture envelopes in quota (the Budget/wallet lane-quota dimension) is E-071's item (d), a follow-on epic once this substrate exists.

## DAG

```
T-082-02-01  learned-window-capacity  ← T-082-01-01
T-082-02-02  quota-fraction-lane-heat  ← T-082-02-01
```

Wave rationale: The capacity ticket needs only the marker's settled field shape (T-082-01-01), not live detection, so it fans out in parallel with S-082-01's detection ticket — disjoint files. The heat ticket runs last and alone: it consumes the capacity module's settled contract and is the sole writer of lane-heat.ts, so no sibling can collide.

**Out of this slice:** Capturing the cap event itself (S-082-01); lanes beyond KNOWN_SEATS; provider-published quota lookups or any provider API fetch; budget/wallet spend-algebra changes; a live quota dashboard or monitor UI (N2 — Not a babysitting dashboard); runtime 429 interception or mid-run rerouting (N4 — Not an executor).

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
