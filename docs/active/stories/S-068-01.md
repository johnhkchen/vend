---
id: S-068-01
title: price-true-cost-units
type: story
status: open
priority: critical
tickets: [T-068-01-01, T-068-01-02, T-068-01-03, T-068-01-04]
---

**Scope:** The token-accounting seam end to end: budget.ts `countTokens` (the canonical cost-weighted sum plus the weight vector it reads) and run-log.ts `totalTokens` (the deliberate run-log⊥budget inline mirror), plus the parity-denominated fixed ceilings that MUST move with them — recalibrate.ts's FUNDING_FLOOR_TOKENS/FUNDING_CEILING_TOKENS band and shelf/gather.ts's TIER_BUDGET hand priors. recalibrate/walk-away/spend/wallet are NOT edited — they read `totalTokens`/`countTokens` and recompute for free.

**Story acceptance:** countTokens/totalTokens weight the four buckets by cost (cache_read ≈0.1×, output ≈5×) instead of summing at parity, so boilerplate-demo's recorded E-008 decompose recomputes from its logged buckets (525,180 old units) to a cost figure that fits a sane ceiling; recalibrate over the existing runs.jsonl yields cost-denominated p90 envelopes with NO history re-run; and the FUNDING_CEILING runaway wall + cold-start priors bite at true-cost magnitudes — all unit-proven.

**Honest boundary:** Everything here is fixture/unit-proven and FREE: the recompute runs over buckets already in runs.jsonl, no metered cast. The {input 1.0, cache_read ~0.1, cache_creation 1.25, output ~5} weights are the STARTING point — T-068-01-01 confirms the exact ratios against current Claude pricing at implement time (a named prerequisite, not hidden scope). A live metered decompose that actually clears E-008 under the new units is the epic's closing proof, authorized at the counter — deferred, not owned by this story.

## DAG

```
T-068-01-01  confirm-pricing-cost-weights
T-068-01-02  cost-weight-count-tokens  ← T-068-01-01
T-068-01-03  cost-weight-total-tokens  ← T-068-01-01
T-068-01-04  redenominate-fixed-ceilings  ← T-068-01-01
```

Wave rationale: The weight-vector ticket (T-068-01-01) runs ALONE — every other ticket reads the settled weights and confirmed ratios. The three consumers then fan out on DISJOINT files: countTokens in budget.ts, totalTokens in run-log.ts, and the fixed-ceiling re-denomination in recalibrate.ts/gather.ts — no file overlap, so they parallelize safely once the vector is pinned.

**Out of this slice:** The classify warn-not-discard change and the over-envelope record marker (S-068-02); orphan-epic hygiene (S-068-03); prompt/context pruning (explicitly OUT per the epic — the meter, not the prompt, is the driver; DECOMPOSE_MAX_TURNS already bounds the loop); rewriting historical runs.jsonl (buckets are read, never mutated).

---
_Materialized by Vend's `decompose-epic` play — 4 ticket(s), 2026-07-11._
