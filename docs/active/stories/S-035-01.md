---
id: S-035-01
title: second-executor
type: story
status: open
priority: high
tickets: [T-035-01, T-035-02]
---

Part of **epic E-035 (second-executor)** — extract the `Executor` interface `castPlay` depends on
(today welded to Claude's `dispense`), refactor `ClaudeExecutor` behind it with a selector (default
Claude, byte-identical back-compat), and prove the seam pluggable with a **second, non-Claude
executor** (an OpenAI-compatible / local-model adapter). The downstream parse→gate→effect→log pipeline
is untouched — a non-Claude executor feeds the **same** gated/metered/logged machinery. **Honest
boundary:** a single-completion adapter proves interface + transport + metering, NOT agentic open-model
parity (read-repo/run-tools plays) — that's the named downstream.

Build interface → second impl: T-035-01 the `Executor` interface + `ClaudeExecutor` + `executorFor`
selector (stub executor proves the seam through `castPlay`) → T-035-02 the OpenAI-compatible executor
(SSE stream + usage, fixture-tested; live smoke + agentic gap deferred). Epic-scoped ids (R6 — `035`
clean).

## DAG

```
T-035-01  Executor interface + ClaudeExecutor + executorFor selector (default Claude) — stub proves seam
   └─ T-035-02  OpenAICompatExecutor — /v1/chat/completions SSE → StreamMessage + ResultMessage; fixture-proven
```

**Out of this slice:** an agentic open-model runner (read-repo/run-tools — the real parity work,
downstream); per-play executor selection (scope creep after E-032 — global/env first); a live
open-model endpoint in the gate (fixture-proven; live smoke deferred); changing the
parse→gate→effect→log pipeline (the abstraction leaves it untouched).
