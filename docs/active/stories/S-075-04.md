---
id: S-075-04
title: write-line-grammar-and-grouping-probe
type: story
status: open
priority: medium
tickets: [T-075-04-01, T-075-04-02, T-075-04-03]
---

**Scope:** The `vend svg` write line in src/cli.ts (~:955 — group/card/link pluralization) as a clean edit, plus a DIAGNOSIS spike into the grouping degeneracy that line exposes (the status-axis grouping in src/present/project.ts collapsing a mostly-done board toward one 'done' group under the default DESIGNER_PRESET), and the fix-or-document fork the spike gates.

**Story acceptance:** The write line reads with correct grammar ('1 group'/'N groups', same for card/link); AND the grouping degeneracy is resolved one of two honest ways — a full mostly-done board splits into meaningful groups (proven by a test) OR the collapse is documented in project.ts as expected with the reason. If the diagnosis reveals a structural grouping rework, a separate epic is proposed rather than absorbed here.

**Honest boundary:** The grammar edit is FREE and unconditional. The degeneracy fork is capped by the fix-or-document decision (PE-7): a structural grouping rework is explicitly OUT and becomes a separate epic; this story only diagnoses and takes the bounded branch.

## DAG

```
T-075-04-01  write-line-plural-grammar
T-075-04-02  grouping-degeneracy-diagnosis
T-075-04-03  grouping-fork-fix-or-document  ← T-075-04-02
```

Wave rationale: The grammar ticket runs ALONE and immediately (disjoint file region, no dependency). The spike runs in parallel with it; the fork ticket depends on the spike's verdict — it cannot choose fix-vs-document until the diagnosis lands.

**Out of this slice:** Any structural grouping rework (a separate epic if the spike calls for it); renaming CLI commands; new SVG features; the designer render-and-watch probe.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-13._
