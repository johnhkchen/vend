---
id: S-075-03
title: shelf-honesty-labels
type: story
status: open
priority: high
tickets: [T-075-03-01, T-075-03-02]
---

**Scope:** Two board/shelf honesty labels: the cold-start confidence label in src/shelf/shelf-row.ts (ShelfConfidence + confidenceLabel + the shelfRows mapping) and the empty-board guidance line in src/shelf/menu.ts (`renderMenu([])`, surfaced by home-shell.ts), plus shelf-row.test.ts / menu.test.ts / home.test.ts. recalibrate already carries the sub-threshold count in result.confidence.successes; this only threads it onto the `default` variant — no ledger math changes.

**Story acceptance:** A play with 1–2 real successful runs reads '(default — N runs, measured at 3)', a genuinely-zero-run play still reads '(default — no runs yet)', and the E-026 'measured (0 runs)' lie stays unconstructable (the discriminated union still forbids a runs-bearing measured-0). The empty bare-vend board shows a plain line, not '(no actions)'. Tests assert all three confidence cases and the reworded board line.

**Honest boundary:** Pure/typed edits, FREE, fixture-proven. 'measured at 3' is COLD_START_MIN_SUCCESSES read from the ledger, not redefined here; recalibration math is untouched.

## DAG

```
T-075-03-01  cold-start-confidence-count
T-075-03-02  plain-empty-board-line
```

Wave rationale: Two tickets over disjoint files (shelf-row.ts vs menu.ts) sharing no symbol — they fan out in parallel safely.

**Out of this slice:** The operator audit/ledger prose (S-075-02); changing COLD_START_MIN_SUCCESSES or any recalibration math; the SVG face strip.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
