---
id: S-025-01
title: wallet-priced-casts
type: story
status: open
priority: high
tickets: [T-025-01]
---

Part of **epic E-025 (wallet-priced-casts)** — the one-wire kaizen fix E-024's first live spend
proved missing: thread the wallet's recalibrated per-step price into the chain cast so a fitted pull
runs under the budget the wallet reserved (227k), not the play's static default (150k), and actually
clears. Authorization (`canAfford`) == execution (the cast's budget).

Single ticket (the fix is one seam + its call site); the proof is the live re-sweep E-024 deferred.

## DAG

```
T-025-01  per-step budgets on the chain + castWork passes the recalibrated envelopes (auth == exec)
```

**Out of this slice:** per-candidate tier pricing (E-024's downstream); re-deriving per-play
envelopes (E-013 owns them); bumping propose's static default (the wire is the fix, not the default).
