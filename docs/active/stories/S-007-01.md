---
id: S-007-01
title: casting-engine-first-slice
type: story
status: open
priority: high
tickets: [T-007-01, T-007-02, T-007-03, T-007-04]
---

Part of **epic E-007 (casting-engine)** — the keystone first slice: generalize
E-001's *hardcoded* `DecomposeEpic` runner (`src/play/decompose-epic.ts`) into a
**play registry + generic cast loop**, prove DecomposeEpic casts through it
unchanged, then prove a **second play** casts through the *same* engine — *author
once, cast forever*, no per-play hardcoding.

Applies the rule-set: shared contract first (R12 → the `Play` interface), pure
cores before integration (R1), file-overlap edges (R4). Epic-scoped ids (R6) —
`007` is a clean namespace.

## DAG

```
T-007-01  play-registry (the Play interface + registry — the shared contract)
   └─ T-007-02  cast-loop (generic render→dispense→parse→gate→effect→log)
         └─ T-007-03  register DecomposeEpic on the engine (refactor runner + dispatch)
               └─ T-007-04  second play proves the loop is play-agnostic (≥2 plays)
```

**Out of this slice** (follow-up registrations onto the *proven* engine, their own
pulls): full `ProposeEpic`, the survey-as-cast that fully resolves F4, the
multi-node DAG, open-model executors, conversational authoring.
