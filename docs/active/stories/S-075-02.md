---
id: S-075-02
title: operator-lines-plain-pass
type: story
status: open
priority: high
tickets: [T-075-02-01]
---

**Scope:** The operator-facing trust prose: formatWalkAwayFindings in src/ledger/walk-away.ts (the `vend audit` readout) and homeLedgerLine in src/shelf/home.ts (the DL-6 ledger foot), plus walk-away.test.ts / home.test.ts. Only label strings change; the numbers, the honest-empty branches, and the shared `pct` rounding seam are untouched.

**Story acceptance:** Every line an operator reads in `vend audit` and the Home foot speaks plain kitchen-table English — no 'E1 walk-away', 'intervention bit unrecorded', 'censored', or 'andon rate' — while the numeric content and honest-empty labels keep the same meaning; tests assert the new wording and that Home and `vend audit` still round identically through the shared `pct`.

**Honest boundary:** Pure string edits, FREE, fixture-proven — no behavior, number, or threshold changes. Meaning-equivalence with the old wording is proven by updated unit tests, not a live audit run.

## DAG

```
T-075-02-01  plain-operator-trust-lines
```

Wave rationale: One ticket — the two surfaces mirror each other's vocabulary by design (they must read identically), so rewording them together in one session is what keeps them in sync; splitting would invite drift.

**Out of this slice:** The shelf confidence label and the empty-board line (S-075-03); the SVG face strip (S-075-01); changing any trust threshold or metric.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-13._
