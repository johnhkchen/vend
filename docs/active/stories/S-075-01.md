---
id: S-075-01
title: svg-face-jargon-strip
type: story
status: open
priority: high
tickets: [T-075-01-01]
---

**Scope:** The read-surface jargon classifier in src/present/translate.ts (JARGON_CLASSES + the shared scrubFace/jargonTokens/faceJargon machinery it backs), its pure unit test (translate.test.ts), and the live-board empty-face-jargon assertion in src/present/svg-file.test.ts. Only the classifier families change; projectNode/projectGraph field routing is untouched.

**Story acceptance:** Casting the live board through DESIGNER_PRESET yields an empty faceJargon — zero residual Baml…/Ci…/Claude p… tokens on any card face — and translate.test.ts covers each newly-caught family; the honest face content (plain title / why / breakdown) is otherwise byte-unchanged, only the jargon leak removed.

**Honest boundary:** Fixture- and live-board-proven and FREE (pure functions, no tokens, no LLM). The render-and-watch designer probe on the improved surface is the SECOND half of the strip-then-probe fork and is explicitly deferred — not here.

## DAG

```
T-075-01-01  extend-face-jargon-classifier
```

Wave rationale: One ticket — extending the classifier and proving it (new-family unit tests + the live-board empty predicate) is one cohesive pure edit over one file pair; nothing else in the epic depends on it.

**Out of this slice:** Rewording the operator audit/ledger prose (S-075-02); any new SVG feature (color-blind redundancy, layout stability — separately staged); the designer render-and-watch probe.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-13._
