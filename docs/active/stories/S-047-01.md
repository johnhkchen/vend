---
id: S-047-01
title: live-real-play-graph
type: story
status: open
priority: high
tickets: [T-047-01, T-047-02]
---

Part of **epic E-047 (live-real-play-graph)** — prove the E-046 DAG substrate LIVE with real plays, not
stubs. E-046 proved the machinery deterministically (stub nodes); this casts a real fan-out + join
graph through `castGraph`: `survey → [propose-epic ×2 in parallel] → capture-note (join)`, built from
the 6 existing plays (the fan-out adapters use `parseBoardSignals`; the join consumes both upstream
epic paths). The headline is **real concurrency** — two real `claude -p` casts at once, which the
linear `runChain` structurally cannot do.

A metered spend (not a free proof), bounded by P7. Honest: this is the **first composed playbook and
deliberately modest** (the current plays weren't designed to compose richly — E-046 named "richer
playbooks" downstream); its value is proving the substrate carries real plays + concurrency live. It
mints 2 real epics (E-043 prevents orphans; the settle assesses quality + decompose status).

Build+cast → settle: T-047-01 (define the real-play `DagSpec` with adapters, unit-test the wiring with
stubs deterministically, then cast LIVE through `castGraph` and capture the concurrency + the join
receiving both produceds) → T-047-02 (settle — prove the proposes ran concurrently, minted-epic
quality + decompose status, P7 held, an honest read of the first composition; board update; no
over-claim). Epic-scoped ids (R6 — `047` clean).

## DAG

```
T-047-01  build + cast (METERED) — survey→[propose×2]→note DagSpec (real adapters, stub-tested wiring) · cast live via castGraph · capture concurrency + join-of-both
   └─ T-047-02  settle (free) — proof of real concurrency (not sequential) · minted-epic quality + decompose status · P7 held · honest first-composition read · board update · NO over-claim
```

**Out of this slice:** a rich/profound playbook (modest first composition); conditional edges /
cross-branch budget (E-046 follow-ons); a new join-consumer play (uses existing `capture-note`);
claiming more than a first live composition.
