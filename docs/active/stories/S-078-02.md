---
id: S-078-02
title: charter-convention-teaches-itself
type: story
status: open
priority: medium
tickets: [T-078-02-01, T-078-02-02, T-078-02-03]
---

**Scope:** The three surfaces where a newcomer collides with the P-label convention: src/gate/gates.ts (refusal wording plus exporting the module-private matchIds label detector as the shared seam) with gates.test.ts; a new src/doctor/charter-convention-probe.ts wired into the doctor preflight on the board-hygiene-probe precedent; and CHARTER_STUB in src/init/init-core.ts with its tests. Gate verdict logic, the epic schema, and the charter schema are untouched.

**Story acceptance:** On a charter with zero P-labeled invariants: a refused plan's reason names the missing-labels cause and states the labeling fix instead of bare 'advances is empty'; vend doctor shows the charter-convention check amber with the how-to while still exiting 0; and a fresh vend init charter resolves at least one P-label through the same exported detector the gates use, carrying the one-line comment that casts cite the labels. On a labeled charter the doctor check reads green with the count and all existing refusal wording and verdicts are unchanged.

**Honest boundary:** All fixture-proven and FREE — stub charters and temp dirs, no metered cast anywhere in the story. Repairing any real project's dangling cites or unlabeled charter is operator work, out by the epic's own boundary. Amber is diagnostic by design: it never blocks and never flips doctor's exit code (N2 — Not a babysitting dashboard) — making it blocking is refused, not deferred.

## DAG

```
T-078-02-01  unlabeled-charter-refusal-names-the-cause
T-078-02-02  doctor-charter-convention-check  ← T-078-02-01
T-078-02-03  init-charter-template-ships-p-labels  ← T-078-02-01
```

Wave rationale: T-078-02-01 runs alone first: it settles the shared charter-label detection seam (exporting gates.ts's matchIds) that both other tickets consume. T-078-02-02 and T-078-02-03 then fan out in parallel — disjoint directories (src/doctor/ vs src/init/), both reading the settled detector, no file overlap.

**Out of this slice:** Any change to gate verdicts or to the requirement that advances codes resolve (legibility only); the hackathon/kitchen overlay charters (tuned separately — if they lack labels, that is a fresh signal, not this slice); auto-labeling or migrating existing charters; any blocking doctor behavior; the help guard (S-078-01).

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-13._
