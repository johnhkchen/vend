---
id: S-070-01
title: degrade-not-discard-on-invalid-seat
type: story
status: open
priority: high
tickets: [T-070-01-01, T-070-01-02, T-070-01-03]
---

**Scope:** The seat disposition path end to end: the run record's seat-defaulted marker schema (`src/log/run-log.ts`), the materialize seat guard and its `MaterializeResult`/`EffectResult` report (`src/play/materialize.ts`, `src/engine/play.ts`), the effect's seat handling (`src/play/decompose-effect.ts`), and the cast loop's record write + stdout warning (`src/engine/cast.ts`). `KNOWN_SEATS` (`src/play/agent-seat.ts`) is REUSED to choose default-vs-route, not changed. Ticket bodies, epic cards, and the BAML schema are untouched.

**Story acceptance:** Casting a board-writing gesture with an unknown seat (`--agent kodex`) through a stub executor materializes the FULL board with every ticket byte-identical to the default mint (no `agent:` key), the run record carries a named seat-defaulted marker (requested raw seat, default applied, why), and cast emits an honest requested-vs-default warning — no discard, no `unknown-seat` refusal fired. A valid `--agent codex` still stamps `agent: codex` as E-069 landed it, and `bun run check` is green.

**Honest boundary:** Everything here is fixture-proven and FREE — stub executor, pure unit tests, no tokens; the live metered cast is not run in this story. Whether to PURGE `unknown-seat` from the `RUN_OUTCOMES` vocabulary is deferred: the effect path stops PRODUCING it, but the append-only ledger keeps tolerating it on read (a record could exist from E-069's same-day window), so the outcome literal stays for back-compat.

## DAG

```
T-070-01-01  run-log-seat-defaulted-marker-schema
T-070-01-02  materialize-degrades-on-unknown-seat
T-070-01-03  cast-records-and-warns-on-seat-default  ← T-070-01-01, T-070-01-02
```

Wave rationale: The run-log marker schema (T-070-01-01) and the materialize/effect disposition flip (T-070-01-02) touch DISJOINT files (`run-log.ts` vs `materialize.ts`/`play.ts`/`decompose-effect.ts`), so they fan out in parallel — neither reads the other's edits, and each is independently green. The cast wiring (T-070-01-03) runs last, alone: it threads the effect's marker onto the run record (needs the schema field from -01) and reads the effect's defaulting report (needs the flip from -02), so it fans in on both.

**Out of this slice:** Changing how lisa DISPATCHES to a seat; adding seats beyond claude|codex; per-ticket seat overrides; altering refusal for genuine structural invalidity (graph / story-contract / bare-code refusals stay); and purging the `unknown-seat` outcome literal from the ledger vocabulary (see honestBoundary).

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-11._
