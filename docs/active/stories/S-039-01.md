---
id: S-039-01
title: macro-wallet-live-clear
type: story
status: open
priority: high
tickets: [T-039-01, T-039-02]
---

Part of **epic E-039 (macro-wallet-live-clear)** — close the loop E-037 opened and E-038 unblocked.
E-037 watched the wallet live but **cleared 0** (the per-cast time-censor guillotined `propose-epic`
at its 72,785 ms p90); E-038 **fixed it** (`timeoutMsFor × 2` headroom, proven deterministically).
This re-runs the bounded metered sweep and confirms a pull **actually clears** — propose finishes,
decompose runs, a real epic+tickets mint, and the ledger gains the **first CLEARED forward-E1 record**
(toward the ≥10 cadence).

The deliverable is a **watched metered run**, not a free proof. Key lesson carried from E-037's
sweep-log: its #1 was a **self-referential meta-signal** ("run the sweep"), a degenerate target — so
this run aims at a **concrete** #1 (real product demand now exists: Frontier 7 `vend init`, the
frontiers). It may still 0-clear (heavier-than-2× signal, decompose-side censor, token exhaustion, or
a degenerate #1) — record honestly whatever happens; even "propose now finishes but decompose censors"
is progress (the bottleneck moved). One clear ≠ the ≥10 bar; no "forward-confirmed" over-claim.

Run → settle: T-039-01 the **live re-sweep** (metered — deterministic pre-check that E-038's headroom
is live + a fresh concrete board, then `vend work --no-intervened ~1h,~1M`; capture propose finishing +
≥1 cleared pull + the first cleared forward record, or where it stopped) → T-039-02 the **settle**
(free — `vend audit` cleared-vs-censored forward read, clear-quality, P7/auth, honest verdict + cadence
to ≥10, board update). Epic-scoped ids (R6 — `039` clean).

## DAG

```
T-039-01  live re-sweep (METERED) — fresh CONCRETE board · vend work --no-intervened ~1h,~1M · propose FINISHES · ≥1 cleared + first cleared forward record (or honest stop)
   └─ T-039-02  settle (free) — audit cleared-vs-censored · clear-quality · P7/auth · honest verdict + cadence to ≥10 · board update · NO over-claim
```

**Out of this slice:** meeting ≥10 forward in one session (cadence, named); removing the P7 bound;
IA-14 auto-widen (E-038's deferred rung); hand-authoring what it clears (autonomous — quality
assessed); "forward-confirmed" off one clear.
