---
id: S-022-01
title: articulation-consistency-contract
type: story
status: open
priority: high
tickets: [T-022-01, T-022-02]
---

Part of **epic E-022 (articulation-consistency-contract)** — decide what consistency Vend
promises for expand/survey/steer by measuring whether their run-to-run dispersion
(0.50–0.72) is *equivalent-diversity* or *genuine-disagreement*, then capture the contract
(converge vs by-design). The remaining half of E-019's verdict after E-020. Measure-then-
decide (the E-014/E-019 shape).

Diagnose first (R12 — extend E-019's consistency probe with a semantic-equivalence judge),
then synthesize the contract + the recommendation + the human fork (R1). Epic-scoped ids
(R6 — `022` clean).

## DAG

```
T-022-01  extend the probe — semantic-equivalence judge (equivalent-diversity vs disagreement)
   └─ T-022-02  the consistency contract + converge-vs-by-design recommendation + the fork
```

**Out of this slice:** building any convergence mechanism (temperature/normalization/
canonical-form gate/dedup/caching) — a downstream epic, pulled only if the fork chooses
"converge"; the validity axis (the gates already own it).
