---
id: S-058-01
title: vend-init-template-seam
type: story
status: open
priority: high
tickets: [T-058-01]
---

Part of **epic E-058 (hackathon-designer-onboarding)** — the `vend init --template <name>` seam (PM brief
piece A), the reusable foundation every example overlay reuses. Extends the shipped `vend init` (E-040):
after the base `SCAFFOLD_MANIFEST` applies, overlay the named template's files through the *same* converge
planner (write-if-absent, never clobber, reported as DATA); an unknown name is a clean refusal with the
available list (mirror E-040's `not-lisa` refusal). Honest-empty held: the overlay adds structure + a
tuned charter + a `SEED.md`, NEVER demand. Pure-core (`planTemplate`) ⊥ effect, like `init-core.ts`. FREE.

## DAG

```
T-058-01  vend init --template <name> — pure overlay planner over the base scaffold + the write effect; unknown→clean refusal; honest-empty
```
