---
id: S-077-03
title: honest-progress-line
type: story
status: open
priority: high
tickets: [T-077-03-01, T-077-03-02]
---

**Scope:** The live progress line — the pure formatCastProgress (src/engine/cast-core.ts:408-411) and its call site (cast.ts:306-313): the token-envelope segment and the turn segment, plus regression tests in cast-core.test.ts. Continues T-072-04, which fixed the SUMMARY line and deliberately left the live line untouched (its design.md:115).

**Story acceptance:** Over-envelope, the live line marks the token fraction detect-after in-line (e.g. `392k/200k tokens (detect-after)`); and a regression test pins that the live turn fraction pairs the deduped agent-turn count with maxTurns (same unit, ≤ cap) and never renders the executor num_turns against the cap.

**Honest boundary:** Grounding corrected the epic's premise: the live TURN fraction is ALREADY like-units (deduped agent-turns / maxTurns, fixed by T-072-02-01) — a literal `23/15` on the turn fraction is not reproducible from current code. The residual live-line defect is the UNLABELED token overshoot. So this story ships the token detect-after marker plus a regression test that pins the turn fraction honest — NOT a rewrite of the turn counter. Pure-formatter proven, FREE.

## DAG

```
T-077-03-01  token-fraction-detect-after-label
T-077-03-02  live-turn-fraction-regression-test  ← T-077-01-01
```

Wave rationale: The token-label ticket has no cross-slice dependency and starts immediately; the turn-fraction regression test depends on S-077-01's characterization (you must know what the cap binds to assert the pairing is honest). The two touch disjoint segments of the same pure formatter, so they don't collide.

**Out of this slice:** The summary line (already done, T-072-04); moving turn execution into vend or changing the cap (N4 — Not an executor); the detect-after budget CONTRACT itself (only its in-line label). No change to token accounting.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
