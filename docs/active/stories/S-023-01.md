---
id: S-023-01
title: survey-convergence
type: story
status: open
priority: high
tickets: [T-023-01, T-023-02]
---

Part of **epic E-023 (survey-convergence)** — converge the one play E-022 measured as genuinely
inconsistent (survey: `genuine-disagreement` 0.00 over N=3), but only where it matters. E-022's
judge compared *whole boards*; this story asks the sharper question the contract actually cares
about — **does survey's load-bearing #1 pull flip run-to-run, or only the tail re-order?** — and
converges the head only if it's actually unstable. The IA-17 contract's first *enforcement*.

Measure first (T-023-01 — a head-isolating top-pick stability probe), then the warranted decision
(T-023-02 — head stable → amend the contract to tail-divergence-by-design; head flips → the
consensus-cast lever, the equivalence judge as a gate). Measure-then-decide (the E-014/E-022
shape). Epic-scoped ids (R6 — `023` clean).

## DAG

```
T-023-01  top-pick stability probe — does survey's #1 pull flip, or only the tail? (head-isolating)
   └─ T-023-02  the warranted path: amend IA-17 (head stable) OR consensus-cast lever (head flips)
```

**Out of this slice:** converging expand or steer (E-022 closed both by-design); a global
temperature/sampling change (survey-scoped only); the validity axis (the gates own it).
