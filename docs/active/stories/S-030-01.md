---
id: S-030-01
title: value-budget-shelf
type: story
status: open
priority: high
tickets: [T-030-01, T-030-02]
---

Part of **epic E-030 (value-budget-shelf)** — the supply shelf (IA-2): each authored playbook shown
with its **worth** (role) and its **warranted budget** — the recalibrated envelope from the run log's
actuals (E-013), with confidence — not the static `budgetForTier` guess the board menu shows. The
E-025 lesson (use the measured price, not the static default) applied to the display the human reads
before allocating. Clean-typographic, no card chrome (E-029 DL-6/DL-9).

Build core → surface: T-030-01 the worth field on `Play` + the pure `shelfRows(plays, records)`
(worth + recalibrated envelope + confidence) → T-030-02 the render (clean-typographic) + the `vend
shelf` CLI surface. Epic-scoped ids (R6 — `030` clean).

## DAG

```
T-030-01  worth on Play + pure shelfRows(plays, records) — recalibrated envelope + confidence
   └─ T-030-02  render (DL-6/DL-9, no cards) + `vend shelf` surface; free live proof over the run log
```

**Out of this slice:** changing the board (`vend`) view; the board+shelf Home fusion (DL-6 — the
fusion is downstream); per-play value tiers (a signal concept, not a tool's); recalibration math
(E-013 owns it).
