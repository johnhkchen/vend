---
id: S-029-01
title: design-language
type: story
status: open
priority: high
tickets: [T-029-01, T-029-02]
---

Part of **epic E-029 (design-language)** — capture Vend's visual design charter (`docs/knowledge/
design-language.md`, DL-1…DL-N), the clean-typographic complement to the IA spine, so a TUI epic
implements a settled look. Direction set by the human: clean typographic — type/whitespace carry it,
amber reserved for the andon, card model stays a lens not chrome. Generative, capped, anti-stale.

Build atoms → surfaces (the design-system shape): T-029-01 the visual tokens (restraint principle,
palette, type hierarchy, the honest-meter + andon rules) → T-029-02 the surfaces composed from them
(Home / Counter / Ledger / production-line, ASCII mocks) + the card-as-lens decision. Epic-scoped ids
(R6 — `029` clean).

## DAG

```
T-029-01  visual atoms — restraint principle, palette (amber-reserved), type hierarchy, meter+andon rules
   └─ T-029-02  surfaces — Home / Counter / Ledger / run-stream mocks + card-as-lens; assemble the charter
```

**Out of this slice:** building any TUI (downstream); a component library / theming engine; re-opening
the clean-typographic direction (set); re-deriving the IA (this is its visual complement).
