---
id: S-031-01
title: home-fusion
type: story
status: open
priority: high
tickets: [T-031-01, T-031-02]
---

Part of **epic E-031 (home-fusion)** — the single DL-6 Home screen: the pull board (demand) leads at
column 0, the supply shelf (E-030) recedes beneath as the inventory that serves the recommendation,
and a one-line ledger summary (E-028's provenance-split — forward vs attested) sits at the foot. The
`renderHome` composite DL-6 names as its one honest gap — built by composing the emitters we already
have (`renderMenu` + `shelfRows`/`renderShelf` + the walk-away split), not re-deriving. The press
contract (`.vend/menu.json` + `vend <sel>`) is preserved bit-for-bit: Home enriches what `vend`
*prints*, the board stays the selection namespace.

Build composite → surface: T-031-01 the pure `homeLedgerLine` + `renderHome` (clean-typographic,
DL-6/DL-1/DL-9, unit-tested) → T-031-02 wire bare `vend` to the composite (press preserved; free live
proof). Epic-scoped ids (R6 — `031` clean).

## DAG

```
T-031-01  pure homeLedgerLine + renderHome({ boardMenu, shelfRows, ledger }) — the DL-6 composite
   └─ T-031-02  wire bare `vend` to renderHome; preserve `.vend/menu.json` + press; free live proof
```

**Out of this slice:** unifying the selection namespace so shelf playbooks are directly castable from
Home (migrates the press board→shelf — its own epic); a TUI render (this is the CLI composite the TUI
implements against); changing board ranking (E-003) or shelf recalibration (E-013).
