---
id: S-081-01
title: turn-record-in-the-capped-unit
type: story
status: open
priority: high
tickets: [T-081-01-01, T-081-01-02]
---

**Scope:** The turn-unit seam end to end: the forensics work dir (docs/active/work/T-081-01-01/), then src/engine/cast.ts (the resolveTurnsUsed harvest at :423 and the appendRunLog turn spread at :596) and src/log/run-log.ts (RunRecordInput/RunRecord turn fields, build/revive normalization) plus run-log.test.ts, cast.test.ts, and the T-077-01-01 characterization test that currently pins the old unit. cast-core.ts's fold and formatter belong to S-081-02.

**Story acceptance:** A cast through a stub executor writes a ledger row whose turn field equals the summary line's deduped agent-turn figure (progress.turns) — one unit, line to ledger — with any retained executor event count under its own honestly-named key; reviving a pre-E-081 ledger line returns it byte-identical with its old unit noted in the schema doc, not rewritten; and the 45-in-a-15-capped-dispense anomaly has a written root cause whose arithmetic is reconciled against a replayed real transcript.

**Honest boundary:** Everything here is fixture-proven and FREE: the forensics replays already-captured local transcripts (.vend/transcripts/) against their real ledger rows, and the ledger fix proves through stub executors and committed sanitized excerpts — no tokens. The epic's 'fresh cast from the installed binary' verification is the deferred live metered step that closes the epic at the counter, named here, not hidden in a ticket. Historical ledger lines keep their old unit — noted, never laundered.

## DAG

```
T-081-01-01  transcript-forensics-turn-and-token-census
T-081-01-02  ledger-records-the-capped-unit  ← T-081-01-01
```

Wave rationale: T-081-01-01 runs alone and first: its message-kind census decides whether sidechain assistant ids inflate the deduped count and whether num_turns earns a ledger key — facts both fixes build against. T-081-01-02 then fans out safely alongside S-081-02's fold ticket: this story edits run-log.ts and cast.ts's ledger spread while the fold ticket edits cast-core.ts — disjoint primary files — and the ledger tests pin the relational contract (ledger turn field === summary figure === progress.turns), which stays true under any later fold correction.

**Out of this slice:** The live token figure and the fold itself (S-081-02); pre-cast receipt, turn denominators on uncapped casts, and board succession (steer-jank items 1, 4, 5 — separate demand); turn-denominated budgets; any envelope-recalibration change (it reads wall-clock and terminal token usage, which were never wrong); rewriting historical ledger records.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
