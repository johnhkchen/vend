---
id: S-072-01
title: cli-teaches-itself
type: story
status: open
priority: high
tickets: [T-072-01-01, T-072-01-02]
---

**Scope:** src/cli.ts only — the USAGE banner (regroup free-vs-metered), parseArgs verb routing (a new `help` command intercepted for `--help`/`help` before parseSelectOrBrowse rejects them), and the unknown-verb error path, plus a small pure edit-distance suggester and its cases in src/cli.test.ts. No cast, no other module touched.

**Story acceptance:** `vend --help` and `vend help` exit 0 and print the complete command list grouped free (shelf/doctor/user-guide/--version/envelope/audit/svg/init) vs metered (run/chain/expand/annotate/survey/steer/select) — every real verb present, none erroring; an unknown verb (`vend frobnicate`, a near-miss `vend steeer`) prints a one-line targeted error naming the wrong token plus `did you mean <nearest>?` instead of only the 9-line wall; parseArgs unit tests assert both.

**Honest boundary:** Everything here is FREE and fixture-proven — pure parse + string work, no tokens, no live cast. GROUNDED CORRECTION vs the survey premise: the current USAGE already lists doctor/user-guide/envelope/audit/--version, so this story does NOT add missing commands — its real gap is the absent `--help`/`help` handler, the flat (ungrouped) list, and the missing suggestion. What DOES commands stays untouched.

## DAG

```
T-072-01-01  help-command-and-grouped-usage
T-072-01-02  did-you-mean-on-unknown-verb  ← T-072-01-01
```

Wave rationale: 01-01 lands the `help` command + grouped USAGE (edits the USAGE constant and the parseArgs verb table); 01-02 adds the did-you-mean suggester on the unknown-verb path and depends on 01-01 because both edit src/cli.ts and 01-02 builds on the regrouped banner. Sequential, same file — deliberately NOT parallel, because the files are not disjoint.

**Out of this slice:** The live progress line (S-072-02) and humane budget units (S-072-03); the shelf foot-line `new here? vend user-guide` and the `(no actions)` header leak (survey items 6/1 — they ride with the SVG/vocabulary work); any change to command behavior — only how commands are discovered and how a wrong one is corrected.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
