---
id: S-077-01
title: max-turns-seam-characterization
type: story
status: open
priority: high
tickets: [T-077-01-01]
---

**Scope:** The executor seam on the live decompose path — src/executor/claude.ts buildArgs (`--max-turns` push at line 231), the DECOMPOSE_MAX_TURNS=15 default (decompose-epic-core.ts:51 → decompose-epic.ts:157), the castPlay accumulator (cast-core.ts accumulateCastProgress / resolveTurnsUsed / resolveMaxTurns), and a new characterization test in the impure shell (cast.test.ts). No production behavior changes.

**Story acceptance:** A committed test drives a decompose cast through the real executor argv seam and pins three facts: `--max-turns 15` is what actually binds, the live accumulator counts distinct deduped assistant message-ids while turnsUsed captures the executor's num_turns (a different unit), and cap-hit behavior at the seam is recorded — the impure-shell assertion T-072-04-01 explicitly left out (its progress.md:69).

**Honest boundary:** Characterization ONLY — pins current behavior, changes none (N4 — Not an executor: does not move turn execution into vend). Grounding corrected two premises the test must respect: there is no Agent SDK/`query()` (it is a `claude -p` subprocess argv seam) and `--max-turns` is a play default, not a CLI flag. This is the factual precondition for S-077-03's turn labeling and S-077-04's next-repair-action; any turn-counter or cap-hit BEHAVIOR change is deferred out of this epic.

## DAG

```
T-077-01-01  characterize-max-turns-live-seam
```

Wave rationale: One ticket — indivisible diagnostic groundwork that both S-077-03 (honest turn labeling) and S-077-04 (next-repair-action) depend on. It lands first and alone; nothing here parallelizes.

**Out of this slice:** Any change to the turn counter, the cap value, or cap-hit handling; the token-fraction label (S-077-03); resumable persistence (S-077-04). Extends — does not redo — T-072-04-01's synthetic diagnosis.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-13._
