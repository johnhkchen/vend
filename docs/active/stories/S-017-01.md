---
id: S-017-01
title: survey-play
type: story
status: open
priority: high
tickets: [T-017-01, T-017-02]
---

Part of **epic E-017 (survey-play)** — the run-0 bootstrap: `vend survey` reads a rough
project → a ranked, staged demand board (IA-3/4). `expand-fragment` (E-016) at project
scale; reuses its `Signal` shape and gate pattern. The second brick of automating STEER.

Pure core first (R1/R3 — the ranked `Signal[]` output + the `Survey` render/parse + the
gates, esp. **read-never-invent** PE-1 / **honest-empty** IA-4), then register the play +
the staging effect + the `vend survey` gesture (R1). Mirrors the E-016 / ProposeEpic play
pattern. Epic-scoped ids (R6 — `017` clean).

## DAG

```
T-017-01  pure core — ranked Signal[] + Survey render/parse + gates (read-never-invent)
   └─ T-017-02  register surveyPlay + staging effect (the board) + `vend survey` gesture + cast live
```

**Out of this slice:** the N-fragment composition (one dispense for now); latent-fruit
harvest; SteerProject-lite (the capstone); board auto-promote (staging only).
