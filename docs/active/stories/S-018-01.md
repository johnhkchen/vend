---
id: S-018-01
title: steer-project-lite
type: story
status: open
priority: high
tickets: [T-018-01, T-018-02]
---

Part of **epic E-018 (steer-project-lite)** — `vend steer` reads a project → a ranked
board **and** the real forks (genuine decisions), staged for human assent. The steer
capstone: productizes `project-steering.md`'s 10 moves; composes the board half of E-017
(Survey) and adds **fork-surfacing**. The third and last brick of automating STEER.

Pure core first (R1/R3 — the `Fork` type + the `Steer` output (board `Signal[]` reusing
E-016 + `Fork[]`) + the gates, esp. **read-never-invent** PE-1 and the new **fork-
genuineness** gate), then register the play + the staging effect + the `vend steer` gesture
(R1). Mirrors the E-016 / E-017 play pattern. Epic-scoped ids (R6 — `018` clean).

## DAG

```
T-018-01  pure core — Fork type + Steer output (board + forks) + gates (read-never-invent, fork-genuineness)
   └─ T-018-02  register steerProjectPlay + staging effect (the steer) + `vend steer` gesture + cast live
```

**Out of this slice:** composing `surveyPlay` under the hood (one steer dispense for now —
that composition wants the multi-node DAG, a separate signal); the interactive answer-a-fork
→ auto-act loop (forks are surfaced; the human acts via existing gestures); board auto-promote.
