---
id: S-072-03
title: humane-budget-units
type: story
status: open
priority: medium
tickets: [T-072-03-01, T-072-03-02]
---

**Scope:** src/cli.ts `parseBudgetArg` (accept `40m`/`2h`/`350k`/`1.5m` unit suffixes alongside the raw `<ms>,<tokens>`) and the funding dispatch arms' echo-back line, reusing `formatBudget` from src/shelf/menu.ts; tests in src/cli.test.ts. Pure parse + one echo line — the budget hard-wall/detect-after contract is untouched.

**Story acceptance:** `--budget 40m,350k` and `--budget 2h,1.5m` parse to the SAME Budget the raw ms/token forms produce, and the raw `2400000,350000` form still parses byte-identically; on a funded gesture the parsed budget is echoed back in humane form (`funding ~40m/350k`) via formatBudget; parseBudgetArg unit tests cover unit-suffix, raw, and malformed inputs.

**Honest boundary:** FREE and fully unit-tested — pure parse plus a reused pure formatter; the echo is a print, no tokens. Serialized AFTER S-072-01 because both stories edit src/cli.ts (not disjoint files) — the dependency is file-safety, not logic. Ceiling: accepts h/m/s time suffixes and k/m magnitude suffixes only (the units the shelf already speaks); compound grammar (`1h30m`) is out.

## DAG

```
T-072-03-01  parse-humane-budget-units  ← T-072-01-02
T-072-03-02  echo-parsed-budget-humanely  ← T-072-03-01
```

Wave rationale: 03-01 extends parseBudgetArg (the load-bearing pure change) and 03-02 adds the echo at the funding dispatch arms; 03-02 depends on 03-01. Both hang off S-072-01's last cli.ts ticket (T-072-01-02) so the two cli.ts stories never edit the file concurrently — same-file serialization is the honest reason slice 3 does not fan out with slice 1.

**Out of this slice:** Changing budget SEMANTICS (the P7 — Budget is a hard contract hard-wall/detect-after contract stays exactly as-is — only the input notation changes); compound durations like `1h30m`; the live spend line's own formatting (S-072-02 owns that surface).

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
