---
id: S-068-03
title: orphan-epic-hygiene
type: story
status: open
priority: medium
tickets: [T-068-03-01, T-068-03-02]
---

**Scope:** A board-hygiene check that catches an epic left with zero stories AND zero tickets after a terminally-failed decompose leg: a PURE detector over vend's built graph (near graph-core/doctor-core) and its surfacing as a `vend doctor` board-hygiene check — an impure probe that loads the board and emits a red Check — so doctor goes non-green on a half-minted board instead of staying green on it.

**Story acceptance:** Over a board carrying an epic with no child stories/tickets, the detector returns that epic id and `vend doctor` reports a red check naming the orphan epic + a fix-it hint (exit non-zero); a fully-populated board returns [] and doctor stays green. Pure-detector and doctor-probe tests prove both branches.

**Honest boundary:** Detection/flagging, FREE and pure — this REPORTS the orphan; it does not auto-delete the epic card or auto-retry the decompose (destructive/metered repair is deferred). The chain-rollback alternative the epic names is NOT taken here: flagging via doctor is the chosen non-destructive surface; rollback stays deferred and named.

## DAG

```
T-068-03-01  orphan-epic-detector
T-068-03-02  doctor-orphan-check  ← T-068-03-01
```

Wave rationale: The pure detector (T-068-03-01) lands first — the doctor probe consumes it; the doctor surfacing (T-068-03-02) depends on it. Only two tickets, a short serial chain. Independent of S-068-01/02 (disjoint files: graph-core/doctor vs budget/run-log/classify), so this whole story can run in parallel with them.

**Out of this slice:** Auto-repair (deleting the orphan card or re-casting decompose — metered/destructive, deferred); the chain-rollback implementation (the alternative surface, deferred); changing decompose's own terminal-failure classification (S-068-02 owns the verdict); the accounting units (S-068-01).

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-11._
