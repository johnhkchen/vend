---
id: S-019-01
title: articulation-consistency-probe
type: story
status: open
priority: high
tickets: [T-019-01, T-019-02]
---

Part of **epic E-019 (articulation-consistency-probe)** — measure the run-to-run
consistency of expand/survey/steer and check honest-empty over-eagerness. Generalizes the
decompose variance probe (`run-probe.ts`) to any-play N-cast; reuses `variance.ts`. The
"validate" half of Vend's own "Consolidate & validate" steer recommendation.

Generalize the harness first (R1/R12 — the pure variance + outcome-mix core + an any-play
N-cast sweep over a disposable ledger), then run the bounded sweep + the findings/verdict
(R1; the live runs are the human sweep, the E-014 shape). Epic-scoped ids (R6 — `019` clean).

## DAG

```
T-019-01  generalize the probe — pure variance+outcome-mix core + any-play N-cast harness
   └─ T-019-02  run the bounded sweep (expand/survey/steer) + findings note + verdict
```

**Out of this slice:** gated-vs-ungated for the articulation plays (run-to-run is the
question here); gate auto-tuning from the result (IA-14-style follow-up); a full stats framework.
