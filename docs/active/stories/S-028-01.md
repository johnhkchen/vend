---
id: S-028-01
title: audit-provenance-split
type: story
status: open
priority: high
tickets: [T-028-01]
---

Part of **epic E-028 (audit-provenance-split)** — `vend audit` must split the walk-away rate by
provenance (post-hoc *attested* back-fill vs live *forward* self-reports), so a verdict cites the
forward-only count and can't count back-fill as measured (the E-026 over-claim). The reader
(`reviveRecord`) currently strips the `intervenedAttestation` marker, so the two are invisibly merged;
surface it, split the stat, render both. Single ticket; the live proof is free (forward 2 / attested
13 over the real ledger).

## DAG

```
T-028-01  reviveRecord surfaces the marker + auditWalkAway splits attested/forward + render the split
```

**Out of this slice:** re-deriving E-026's verdict (corrected by hand — this prevents recurrence); a
per-provenance *trend* split (the rate is the load-bearing fix); changing what counts as a walk-away.
