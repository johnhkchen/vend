---
id: S-011-01
title: chain-propose-decompose
type: story
status: open
priority: high
tickets: [T-011-01, T-011-02]
---

Part of **epic E-011 (chain-propose-decompose)** — cast ProposeEpic → DecomposeEpic as
one gesture (signal → epic → tickets), the engine's first composition of plays. Threads
ProposeEpic's minted epic into DecomposeEpic's input; a STOP in either stage halts.

Shared contract first (R12 → the generic chain primitive), then the specific
propose→decompose chain + the gesture (R1). Epic-scoped ids (R6 — `011` is clean).

## DAG

```
T-011-01  chain primitive + surface a play's produced output (the threading)
   └─ T-011-02  the propose→decompose chain + the `vend` gesture + end-to-end gating
```

**Out of this slice** (follow-ups): arbitrary N-play DAGs, branching/parallel
composition, the full `vend run <any-play>` input-assembly generalization.
