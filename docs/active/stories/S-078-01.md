---
id: S-078-01
title: help-is-free-pre-dispatch-guard
type: story
status: open
priority: high
tickets: [T-078-01-01]
---

**Scope:** parseArgs in src/cli.ts — the pure pre-dispatch parse spine — plus cli.test.ts. The guard short-circuits to the existing {cmd:'help'}/USAGE path (today reached only at argv[0], cli.ts:309) before the verb table dispatches to any per-command parser. No per-command parser, dispatch, or ledger code changes.

**Story acceptance:** A parse-level sweep asserts --help and -h at ANY argv position, for every verb in parseArgs's table, resolves to the free help command; and the field repro (vend chain --help) driven through the real CLI exits 0 printing the USAGE banner with no executor invoked and no runs.jsonl line appended.

**Honest boundary:** Entirely FREE and fixture-proven — the e2e proves the ABSENCE of a metered cast, so proving it costs zero tokens. Richer per-command help text is deliberately deferred: the guard prints the one global USAGE banner (the epic's stated boundary — one pre-dispatch check, not a help-text audit). The ~15k already burned in the field is not recoverable; this kills the repro forward only.

## DAG

```
T-078-01-01  pre-dispatch-help-guard-and-zero-spend-regression
```

Wave rationale: One ticket, alone: the guard and its regression test are a single seam and a single session — splitting the test from a one-line guard would ship an ungated unit (P3 — Gates are the contract). The whole story runs in parallel with S-078-02: disjoint files (src/cli.ts vs gate/doctor/init).

**Out of this slice:** Per-command help text or any arg-parsing overhaul; rewording existing usage-error paths or changing their exit codes; the already-working vend --help / vend help head-position paths; every charter-convention surface (that is S-078-02's slice).

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-13._
