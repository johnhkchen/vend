---
id: S-059-02
title: reprove-the-channel-live
type: story
status: open
priority: high
tickets: [T-059-03]
---

Part of **epic E-059 (seed-intent-reaches-steer)** — the corrected gold master. With the seed intent wired
(S-059-01), re-drive the Set-B channel proof **live**: run `vend steer` on a fresh hackathon seed, confirm a
real grounded board stages off `SEED.md` (no longer honest-empty), render it on the SVG surface (E-055/E-056)
beside the running app, and re-capture `examples/templates/hackathon-seed/EXPECTED-OUTCOME.md` as a *positive*
re-runnable bar — turning E-058's negative gold master into a closed loop. Scope is the **board-renders**
proof (the make-or-break), NOT a full slice clear (gated on E-058 finding #3, out of scope). ONE metered cast;
the human authorizes the spend (P7); accrues a forward-E1 record.

## DAG

```
T-059-03  LIVE: vend steer on the seed → grounded board stages off SEED.md → render on the SVG surface → re-capture EXPECTED-OUTCOME.md (positive gold master) + forward-E1
```
