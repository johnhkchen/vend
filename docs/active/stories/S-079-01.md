---
id: S-079-01
title: settle-verdict-is-free
type: story
status: open
priority: high
tickets: [T-079-01-01, T-079-01-02]
---

**Scope:** A new src/settle/ slice: settle-core.ts, a pure verdict computation over the loaded board graph, a last-settle marker under .vend/, presweep-core's SweepVerdict, gate results, and work-dir review artifacts — plus the `vend settle` verb in src/cli.ts's parse/dispatch table. Composes existing machinery only (graph load, presweep-core, cross-review artifacts); no play, no executor, no tokens.

**Story acceptance:** On a fixture board, `vend settle` exits 0 without invoking an executor or touching .vend/runs.jsonl, printing a one-screen verdict — board delta since the last settle, per-epic cleared counts, gate and presweep result, open review concerns by name, exceptions in red each with an exact next action — and an immediately repeated settle reports an empty delta because the marker advanced.

**Honest boundary:** Settle is typed-gesture only here — event-triggering arrives with the andon cord (S-079-03), named there, not hidden in these tickets. How the gate line is sourced (an inline presweep run is cheap; whether `bun run check` is re-run or its last recorded result is read is a design-phase decision inside T-079-01-02) — settle stays token-free either way. All fixture-proven; no live loop is observed in this story.

## DAG

```
T-079-01-01  settle-core-pure-verdict  ← T-078-02-03
T-079-01-02  settle-verb-one-screen-free  ← T-079-01-01
```

Wave rationale: T-079-01-01 runs alone in wave 1: its verdict shape and its per-epic all-done derivation are the settled ground for three dependents (the settle verb, sweep-core, and the cord reader). T-079-01-02 follows on the frozen core shape. Safe because the core is pure plain-data in the committed-core discipline, so its contract settles before any consumer starts.

**Out of this slice:** The sweep gesture (S-079-02); the loop-settled marker and event-triggering (S-079-03); any TUI or standing dashboard — settle prints and exits (N2 — Not a babysitting dashboard); rehoming the --intervened self-report (fork 1, a separate ruling); and the slice-4 adopt-path mint-validation jank — declined at decompose because three slices already top the envelope, so it remains the next staged candidate, not scope here.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
