---
id: S-002-01
title: first-gate-honest
type: story
status: open
priority: high
tickets: [T-002-01-01, T-002-01-02, T-002-01-03]
---

Part of **epic E-002 (ci-structural-backstop)** — one honest structural gate, end
to end: `bun run check:test` run independently by a thin Dagger module
(`ci-strategy.md` first slice). *Dagger invokes; it never defines.*

**Id scheme (R6 / F1, live):** E-001's second story already took `S-002` and
`T-002-01…04`, so the epic-scoped `T-002-NN` namespace collides. This story uses
**story-scoped** ticket ids (`T-002-01-NN`) — disjoint from the live board, and the
exact case the E-004 id-guard exists to catch for the machine.

## DAG

```
T-002-01-01  ci-module-bootstrap (thin shell, Node runtime, pinned engine)
   └─ T-002-01-02  test-gate (Test sub-class + thin router; invokes check:test)
         └─ T-002-01-03  verify-no-drift (dagger call test agrees + can-fail; Docker)
```

**Out of scope** (generalizes out of the one gate): `lint`, `typecheck`,
`consistency`, parallel DAG, keep-warm tuning.
