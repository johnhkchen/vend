---
id: S-080-02
title: verdict-and-closeout-tell-truth
type: story
status: open
priority: high
tickets: [T-080-02-01, T-080-02-02]
---

**Scope:** Settle's verdict truthfulness and sweep's closeout completeness: epic-line filtering and the first-settle delta line in src/settle/settle-core.ts + src/settle/settle.ts, and provenance carriage in src/sweep/sweep-core.ts (pathspec + message assembly) + src/sweep/sweep.ts (dirty-provenance observation, commit invariant). Seam recorder files are untouched.

**Story acceptance:** Against a fixture board holding status-done epics and open epics, the rendered settle verdict lists only the epics not yet status-done (an all-tickets-done but still-open epic still shows 'sweep ready'); a settle with no prior last-settle marker renders 'delta: first settle — no baseline' instead of claiming all historical done tickets as fresh delta, while its continuation marker still records the full done set; and a sweep closeout on a tree where tracked .lisa/provenance.jsonl has uncommitted changes lands one commit carrying the card flips and the provenance file together — cards-only when provenance is clean.

**Honest boundary:** Fixture-proven and FREE. The first-settle wording cannot be observed live on THIS board — a .vend/last-settle.json already exists here — so it is proven through the null-marker code path, and the live re-observation belongs to the epic's closing trial. Only .lisa/provenance.jsonl is carried by sweep: no general 'commit any dirty .lisa state' policy, and presweep's SWEEP_PREFIXES scope is not widened.

## DAG

```
T-080-02-01  sweep-commit-carries-provenance
T-080-02-02  verdict-open-epics-honest-baseline  ← T-080-01-01
```

Wave rationale: T-080-02-01 touches only src/sweep/* — disjoint from every other ticket in the plan — so it fans out in the first wave beside T-080-01-01. T-080-02-02 waits on T-080-01-01 because both edit renderSettleResult and the settle test files; after it lands, S-080-01's cord-surface ticket takes that same file last.

**Out of this slice:** The cord failure trace and its settle surfacing (S-080-01 owns those); archiving done epic cards off the board — the verdict filters display, it moves no files; auto-sweep without the deliberate confirmation keystroke (N2 — Not a babysitting dashboard); widening presweep scope to cover .lisa/.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
