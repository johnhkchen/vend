---
id: S-077-02
title: degrade-not-discard-charter-cites
type: story
status: open
priority: critical
tickets: [T-077-02-01, T-077-02-02, T-077-02-03, T-077-02-04]
---

**Scope:** The two whole-cast refusal surfaces for charter cites — the read-side bounds gate (src/gate/gates.ts:283-298, dangling `advances` codes) and the write-side bare-code guard (src/play/materialize.ts:252-268 / 456-457 + decompose-effect.ts:137-140, inline prose cites) — plus the normalize precedent stripNonGoalAdvances (decompose-epic-core.ts:203-210) and the RunRecord/summary surface (src/log/run-log.ts, cast summary line). Extends the twice-set degrade-not-discard precedent (9027f9d, E-070).

**Story acceptance:** A decompose whose story/ticket prose or `advances` cites a charter-unresolvable code materializes the board with the cite stripped or annotated by an honest marker and a degrade disposition recorded on the RunRecord — it no longer refuses the whole cast (the field-report's two-bare-codes N4 — Not an executor/N2 — Not a babysitting dashboard case). A fixture with a STRUCTURAL defect (bad graph, duplicate/missing id, missing required field, absent story contract) still refuses.

**Honest boundary:** Fixture-proven and FREE (stub executor, no tokens); the live metered re-cast of the reporter's E-045 is NOT run here. Reserves refusal for structural invalidity only — it touches only the charter-cite branches, not the structural gates' verdicts. It degrades editorial cites; it builds no repair/regeneration loop (none exists — out of scope per the epic).

## DAG

```
T-077-02-01  degrade-disposition-shape
T-077-02-02  inline-prose-cite-degrades  ← T-077-02-01
T-077-02-03  advances-cite-degrades  ← T-077-02-01
T-077-02-04  degrade-on-run-record  ← T-077-02-02, T-077-02-03
```

Wave rationale: The degrade-disposition shape (T-077-02-01) settles first — both appliers and the ledger must agree on the `{code, location, action}` record. The inline-prose applier (materialize.ts) and the advances applier (decompose-epic-core.ts / gates.ts) then run in parallel on disjoint files; the run-record surfacing joins them last.

**Out of this slice:** The structural-invalidity refusals themselves (kept as-is); any repair/regeneration/auto-fix loop; the progress-line labels (S-077-03); resumable persistence (S-077-04). Does not re-cost or edit the epic card.

---
_Materialized by Vend's `decompose-epic` play — 4 ticket(s), 2026-07-13._
