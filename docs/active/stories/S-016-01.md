---
id: S-016-01
title: expand-fragment
type: story
status: open
priority: high
tickets: [T-016-01, T-016-02]
---

Part of **epic E-016 (expand-fragment)** — the demand-extraction primitive: a rough
fragment → a board-ready, priced signal (Vend articulates *for* you). Sits upstream of
ProposeEpic; the primitive the rest of the articulation lift reuses. Spec sources:
`cognitive-overhead-articulation.md` (O1), `job-stories-articulation.md` (the ACs),
`clearing-dynamics.md` (author + assent).

Pure core first (R1/R3 — the `Signal` shape + the `ExpandFragment` render/parse + the
pure gates, esp. **read-never-invent** PE-1 / **honest-empty** IA-4), then register the
play + the staging effect + the `vend expand` gesture (R1). Mirrors the proven
ProposeEpic/CaptureNote play pattern. Epic-scoped ids (R6 — `016` clean).

## DAG

```
T-016-01  pure core — Signal shape + ExpandFragment render/parse + gates (read-never-invent)
   └─ T-016-02  register the play + staging effect + `vend expand <fragment>` gesture + cast live
```

**Out of this slice:** ProposeEpic's signal→epic step (downstream); board auto-promote
(staging only); latent-fruit harvest, Survey, SteerProject-lite (consumers/capstones — later).
