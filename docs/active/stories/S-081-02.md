---
id: S-081-02
title: live-line-tracks-weighted-spend
type: story
status: open
priority: high
tickets: [T-081-02-01]
---

**Scope:** The live-progress fold and its formatter in src/engine/cast-core.ts — CastProgress, accumulateCastProgress (:375, the charge-first-event-per-id policy and the deliberate terminal-usage skip), and formatCastProgress (:408, the unlabeled figure) — plus cast-core.test.ts and committed sanitized fixture excerpts from the captured jank transcripts. The onMessage wiring in cast.ts (:348-355) stays untouched unless design requires a final-line render from settled facts.

**Story acceptance:** Replaying the captured 2026-07-13 steer-jank transcript through the fold yields a final live token figure that agrees with that run's ledger totalTokens within a stated, test-pinned tolerance — the 105k-vs-214k ~2× undercount closed — and the rendered progress line labels what its number is, so the figure an operator allocates by is the meter's figure, named.

**Honest boundary:** Fixture-proven and FREE — the proof replays a real captured transcript against its real ledger row; no live cast is funded by this story. The epic-closing verification on a fresh installed-binary cast is the named deferred live step. The tolerance is stated and labeled, never silently absorbed: if per-message folding structurally cannot reach the terminal cumulative truth within an honest band, that residual is surfaced as a labeled fact on the line, not papered over.

## DAG

```
T-081-02-01  fold-charges-what-the-meter-charges  ← T-081-01-01
```

Wave rationale: One ticket; it waits only on the forensics spike, because the spike's census determines which message kinds/buckets the fold misses and how sidechain messages must be treated in BOTH of the fold's counters — the deduped turn counter lives in this same function, so guessing here would corrupt S-081-01's ledger number. It fans out safely against T-081-01-02: this ticket owns cast-core.ts; that one owns run-log.ts and cast.ts.

**Out of this slice:** The ledger's turn field and any run-log schema change (S-081-01); the budget check itself (check() meters terminal usage and was never wrong); detect-after semantics and the humane envelope denominator (shipped, E-072/E-077); steer-jank items 1, 4, and 5.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-13._
