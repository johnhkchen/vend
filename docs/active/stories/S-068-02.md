---
id: S-068-02
title: warn-not-discard-over-envelope
type: story
status: open
priority: high
tickets: [T-068-02-01, T-068-02-02, T-068-02-03]
---

**Scope:** The cast verdict + record seam: the `classify` decision in BOTH engine/cast-core.ts and play/decompose-epic-core.ts (they mirror), a one-way over-envelope warning marker on the run-log record (run-log.ts, the `reducedGrounding` pattern), and the runner wiring (cast.ts / decompose-epic.ts effect) that materializes the plan and stamps + surfaces the warning.

**Story acceptance:** A run whose gates CLEAR but whose spend exceeds its token ceiling now materializes its stories/tickets AND logs a cleared record carrying the over-envelope warning, instead of being discarded as budget-exhausted; a gate-failed or timed-out run still discards with no output. A fixture cast plus unit tests in both classify cores prove both branches.

**Honest boundary:** Fixture-proven and FREE (stub executor). This changes only the DISPOSITION of an already-detected overshoot — detect-after (a token wall can't un-spend, per John's 2026-07-11 ruling), never prevention. P3 — Gates are the contract is held explicitly: an over-envelope run materializes ONLY if its gates pass. How a cleared-but-overshot run is scored by recalibrate's censored-set is settled inside T-068-02-02's design; no live drive here.

## DAG

```
T-068-02-01  over-envelope-record-marker  ← T-068-01-03
T-068-02-02  classify-warn-not-discard  ← T-068-02-01
T-068-02-03  runner-materialize-and-surface  ← T-068-02-02
```

Wave rationale: The run-log marker (T-068-02-01) lands first and depends on S-068-01's run-log edit (T-068-01-03) so the two totalTokens/marker changes to run-log.ts never collide. The classify change (T-068-02-02) then edits the two pure cores; the runner wiring (T-068-02-03) depends on it. Serial by necessity — each ticket's output is the next's input: record shape → decision → wiring.

**Out of this slice:** Changing the token-accounting units (S-068-01); PREVENTING overshoot (out — detect-after is the reality, warn not prevent); the wall-clock timeout latch (unchanged — a timed-out run still discards); orphan-epic hygiene (S-068-03).

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-11._
