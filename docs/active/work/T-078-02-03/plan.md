# Plan — T-078-02-03

## Objective

Ship a generic init charter that demonstrates the stable P-label convention and teaches how casts
cite it, then prove the exact seed in both the pure manifest layer and a real fresh-workspace write.

## Ownership guard

Before implementation:

1. Record `git status --short --branch`.
2. Treat existing `.lisa/provenance.jsonl` and ticket-frontmatter changes as Lisa-managed.
3. Do not stage, revert, or include unrelated paths.
4. Own only:
   - `src/init/init-core.ts`
   - `src/init/init-core.test.ts`
   - `src/init/init-effect.test.ts`
5. Use `apply_patch` for source edits.
6. Use no ordinary `git add` or `git commit`.

## Step 1 — enrich `CHARTER_STUB`

Edit the existing template literal in `src/init/init-core.ts`.

Preserve:

- the title;
- the generic project-specific authoring guidance;
- the reference to `vision.md`;
- the final newline.

Add:

- an `Invariants` heading;
- the exact one-line HTML teaching comment;
- three generic P-labeled invariant bullets.

Verification after the edit:

- the string visibly contains `P1`, `P2`, and `P3`;
- the comment contains “Casts cite these labels” and `advances`;
- no manifest path, order, or entry is changed;
- no overlay string is changed.

## Step 2 — add the pure detector contract

Edit `src/init/init-core.test.ts`.

1. Import `matchIds` from `../gate/gates.ts`.
2. Select the base charter file entry from `SCAFFOLD_MANIFEST` once.
3. Place that fixture where both base-charter and overlay tests can use it.
4. Add a focused base-charter convention test group.
5. Assert the fixture exists.
6. Assert `matchIds(contents, "P")` returns the deliberate labels.
7. Assert exact containment of the HTML comment line.
8. Retain the hackathon overlay tests against the shared fixture.
9. Update comments that incorrectly say the base charter bytes are unchanged.

Focused success criteria:

- the test proves semantics using the production detector, not a local regex;
- comment bytes are pinned;
- the test touches no filesystem;
- existing overlay assertions still demonstrate the override wins.

## Step 3 — add the fresh-workspace disk proof

Edit `src/init/init-effect.test.ts`.

1. Import `matchIds` from `../gate/gates.ts`.
2. In the existing bare-project full-tree test, read the generated charter file.
3. Derive the expected charter file entry from `SCAFFOLD_MANIFEST`.
4. Assert the expected entry exists.
5. Assert the on-disk charter resolves at least one P-label.
6. Assert on-disk bytes equal the expected manifest bytes.
7. Keep all assertions inside the existing cleanup-protected `try` block.

Focused success criteria:

- the charter was absent before apply and present after apply;
- the generated path is exactly `docs/knowledge/charter.md`;
- the generated bytes are labeled according to the shared detector;
- no production effect change is necessary.

## Step 4 — format and inspect the owned diff

Run:

```bash
git diff -- src/init/init-core.ts src/init/init-core.test.ts src/init/init-effect.test.ts
git diff --check -- src/init/init-core.ts src/init/init-core.test.ts src/init/init-effect.test.ts
```

Inspect for:

- only the planned seed and tests changed;
- no duplicated charter fixture;
- no local label regex;
- exact one-line comment;
- comments remain accurate;
- no incidental formatting churn.

## Step 5 — run focused tests

Run:

```bash
bun test src/init/init-core.test.ts src/init/init-effect.test.ts
```

Success criteria:

- both test files pass;
- the pure detector assertion passes;
- the fresh-workspace effect assertion passes;
- hackathon and standalone init tests remain green;
- no temp directories leak from failures.

If a focused failure occurs:

- diagnose it within the three owned files;
- record any plan deviation in `progress.md` before broadening scope;
- do not edit gate or overlay production modules unless the story contract genuinely requires it.

## Step 6 — run the repository gate

Run:

```bash
bun run check
```

This is the required project gate and includes:

- BAML code generation;
- TypeScript typecheck;
- complete Bun test suite.

Success criteria:

- generation exits zero;
- typecheck exits zero;
- all tests pass;
- no generated file remains modified by the check;
- owned source paths contain only intended changes.

## Step 7 — commit the meaningful source unit

After checks are green, run exactly:

```bash
lisa commit-ticket \
  --ticket-id T-078-02-03 \
  --message "feat(init): scaffold labeled charter invariants" \
  --include src/init/init-core.ts \
  --include src/init/init-core.test.ts \
  --include src/init/init-effect.test.ts
```

The three files are one meaningful unit because the content change and both required proof layers
must land together.

Do not use:

- `git add`;
- `git add -A`;
- ordinary `git commit`;
- a broad directory include.

## Step 8 — verify commit ownership

After the Lisa transaction:

1. Run `git status --short --branch`.
2. Confirm all three ticket-owned source paths are clean.
3. Confirm unrelated Lisa-managed paths remain outside the commit.
4. Inspect `git show --stat --oneline HEAD` or the returned commit hash.
5. Inspect the commit's path list.
6. Confirm the commit contains exactly the three includes.
7. Confirm no source file is staged, modified, or untracked.

## Step 9 — write `progress.md`

Record:

- each completed implementation step;
- the exact seed convention added;
- pure and effect test coverage;
- focused-test results;
- full-gate results;
- diff inspection;
- commit command and hash;
- any deviation from this plan;
- final worktree ownership state.

## Step 10 — review against acceptance

Check each clause explicitly:

1. `CHARTER_STUB` carries P-labeled invariants.
2. The pure test reads the real manifest charter bytes.
3. The pure test resolves labels through exported `matchIds`.
4. The pure test pins the exact “casts cite these labels” comment line.
5. The effect test writes a fresh charter at `docs/knowledge/charter.md`.
6. The written charter resolves at least one P-label.
7. Existing overlays remain unchanged.
8. Existing charters remain protected by no-clobber.
9. `bun run check` is green.
10. Ticket-owned source is committed through Lisa.

## Step 11 — write review disposition

If every acceptance clause and repository gate is green, write:

```json
{"disposition":"pass","reason":null}
```

If work is genuinely blocked, write:

```json
{"disposition":"block","reason":"<non-empty actionable reason>"}
```

The JSON must contain exactly the required object shape.

## Step 12 — stop on this ticket

After `review.md` and `review-disposition.json` exist:

- do not start another ticket;
- do not update ticket phase or status;
- remain on `T-078-02-03`;
- wait for Lisa to publish artifacts and confirm completion.
