# Vend — Cleared demand (compacted ledger)

Signals pulled, cleared, and verified — moved off the live board (`docs/active/demand.md`)
to keep it lean (overproduced plans are inventory; cleared ones are history). One line per
epic: **what it delivered**, grouped by the goal it served. Full epic cards live in
`docs/active/epic/`; full proofs in `docs/active/work/<ticket>/`.

The board's job is *demand*; this file's job is *the record of demand met*. When a live
signal clears, crystallize it to one line here and delete its verbose board entry.

---

## The pipeline spine — author a play → cast it → chain plays (the core capability)

- **E-001 — dispense slice** — the one metered lever: `DecomposeEpic` via `claude -p`, gated · budgeted · streamed · countably logged. Unblocked everything. (`4a1d632`, 4/4 live paths.)
- **E-007 — casting engine** — one play-agnostic `castPlay`; two plays cast through the single metered seam (mana + gates + log). The v1 leap that made written specs runnable.
- **E-009 — register ProposeEpic** — the play above decompose: a demand signal → an epic card. `signal → epic` made real.
- **E-011 — chain (signal → tickets)** — `vend chain <signal>` casts ProposeEpic → DecomposeEpic as one gesture; proven on both the success and the andon-halt path. "Vend the whole roadmap" is real.

## Graph-structured orchestration (the v1 vision · P1/P6)

- **E-046 — typed-DAG fan-out/join substrate** — the foundational slice of the v1 vision's named capability ("typed, graph-structured agent orchestration"). Generalized the linear chain (`runChain` — a *path* graph) into a **typed DAG**, mirroring the chain's pure-core ⊥ impure-shell split: `dag-core.ts` (the `DagNode`/`DagEdge`/`DagSpec` model + a pure/total deterministic `topoSort` + `validateDag` rejecting dangling edges, dup ids, and **cycles**); `graph-core.ts` (`runGraph` — the pure executor: a **join** node receives a *multi-entry* upstream map (the load-bearing generalization — `upstream: string` → `NodeUpstreams`), **fan-out** feeds all downstreams, halt skips only the **dependent subgraph** so independent siblings run on); `graph.ts` (`castGraph` — the impure concurrent **wave dispatcher** running independent ready nodes in parallel, returning a topo-ordered deterministic `GraphResult`). Proven with a deterministic **diamond example** (A→{B,C}→D, stub nodes) + the rigorous **fails-vs-linear** test (the 2-upstream join is inexpressible by `runChain` — over any linearization, D's step gets a single upstream string). +34 tests (1121). *(Autonomously minted by E-045's sweep, hand-decomposed; conditional edges, cross-branch budget, a live real-play graph, and the open-model runner are named follow-ons.)*

- **E-047 — live real-play graph** — proved E-046's DAG substrate LIVE with real plays (not stubs): `survey → [propose-epic ×2] → capture-note` through `castGraph`. **Concurrency PROVEN** — the two propose casts share an *identical `startedAt`* (`03:55:54.569Z`) and ran overlapping (~69s / ~133s): **two real `claude -p` casts concurrently**, exactly what `runChain` (sequential) structurally cannot do. The 2-upstream **join went unproven live** — propose-1 hit `budget-exhausted`, so `castGraph` *correctly skipped* `capture-note` (the halt-dependent-subgraph semantics, live-correct; the live join stays stub-proven). Modest first composition (the current plays don't compose richly — honestly stated). **Self-referential payoff: the run minted E-048 `cross-branch-budget-wallet`** — the demand for its *own* failure mode (both branches drew from one envelope, one starved; E-046 had named cross-branch budget as a follow-on). ~$2.06, bounded (P7). 1127 tests. (`work/T-047-02/`.)

- **E-048 — cross-branch budget wallet** — made the budget hard contract (P7) hold under the real concurrency E-046/E-047 shipped: concurrent fan-out branches now draw from **ONE shared wallet** with a correct hard stop, not per-branch budgets that silently overspend the allocation. The load-bearing correctness — **the two IA-8 denominations DIVERGE under concurrency**: `debitWave` (`wallet.ts`) folds a settled wave **tokens SUMMED, wall-clock MAX** (overlapping branches cost ~the longest, not their sum — the per-cast `debit` that sums *both* over-charges wall-clock, the bug this fixes); `authorizeWave` (`spend-core.ts`) generalizes `fitNext` to a ready-*set* (each node's time ≤ remaining, cumulative tokens ≤ remaining → `{dispatch, stopped}`, hard-stop at the wave boundary). Threaded ONE shared wallet through `castGraph`'s wave dispatcher (authorize before `Promise.all`, debit after settle). **Single-chain / `spendDown` UNCHANGED** (a linear graph = single-node waves; `debitWave` single-element == `debit`, back-compat asserted; `vend work` untouched). Deterministic **shared-vs-per-node worked example** (3-branch partial wave, stub nodes, **no live model**): a fan-out the shared wallet stops at the correct wall which the old per-node budgets would have **overspent**. 1150 tests. *(Self-minted by E-047's live cast, hand-decomposed; also **unblocks E-047's deferred live join** — a shared budget lets both branches finish, so `capture-note` runs. The wave-budget core's feat landed inside commit `5a6027d`, mislabeled `docs(T-048-03)` by the concurrent loop — code correct + green, only the label misleads.)*

- **E-049 — conditional DAG edges** — made the typed DAG **branch** instead of always fanning a node's output to every downstream. `EdgePredicate = (produced: string) => boolean` + an optional `when?` on `DagEdge` (`dag-core.ts`): `runGraph` fires an out-edge only if its predicate holds over the upstream's `produced`, a node runs iff EVERY in-edge fires, and the branch-not-taken's subgraph **cascade-skips via the EXISTING halt-dependent-subgraph machinery** (the design spine — reuse, not new infra), landing in `skipped` with a **DISTINCT "branch not taken" andon** kept apart from "dependent on halted upstream" (halt is the louder andon, takes precedence). Mirrored byte-identical into `runGraphConcurrent` (a test asserts the wave dispatcher == sequential `runGraph` for the predicated spec, incl. under a budgeted E-048 wallet — that path untouched) + a deterministic **router worked example** through `castGraph` (predicate-transparent; no test value-imports `graph.ts`). Absent `when` ⇒ unconditional fan-out (back-compat). 1162 tests, no live model. **Dogfood milestone: the FIRST epic Vend both self-PROPOSED and self-DECOMPOSED via its own `vend chain` pipeline** (propose minted a clean, well-grounded E-049 first try; decompose exhausted the thin 120k warranted envelope and cleared at 350k — a second confirmation, after E-045, that the decompose envelope under-bounds), then human-augmented (codebase-memory notes) + verified green. Turns the typed graph from a fixed pipeline into one that encodes conditional process — the v1 graph-orchestration vision. *(Surfaced two hygiene signals: decompose-envelope recalibration, and restricting autonomous casts' tool-set so a headless `claude -p` can't hang on `AskUserQuestion`.)*

- **E-052 — live multi-upstream JOIN proven** — closed E-047's last stub-only gap. Cast the `survey → [propose ×2] → capture-note` diamond LIVE through the wallet-threaded `castRealPlayGraph` (T-052-01 threaded E-048's ONE shared wallet as `castGraph`'s 3rd arg — `graph-real-play.ts:187`), and proved the 2-upstream JOIN end-to-end: `capture-note` was cast (NOT skipped), received both proposes' produced epic paths, and **MATERIALIZED** a note consolidating both (E-053 + E-054, 10 refs each) — the multi-upstream join firing on REAL upstreams, the thing `runChain` structurally cannot express. Concurrency held (both proposes one wave, ~64s overlap); ONE shared wallet bounded the collective spend (funded 4.2M, ~473k drawn, no per-branch leak — E-052's actual fix). **Honest journey: 4 metered runs** — the shared wallet worked from run 01, but the live join *also* needed per-CAST budgets sized to real variable burn (propose `cache_read` swings 42k→635k; the note's 8k default can't fund a real cast) — a calibration distinction the runs surfaced, NOT a substrate gap. LIVE metered (~14 real `claude -p`, 2.48M tokens), distinct from the free stub proof. **With this the DAG substrate is proven LIVE in full — E-046 typed DAG · E-047 live concurrency · E-048 shared wallet · E-049 conditional edges · E-050 cold-start budget · E-051 unattended casts · E-052 live join — no stub-gated property left.** The third epic Vend self-proposed + self-decomposed via `vend chain` (decompose self-funded via E-050). *(A caution recorded: an in-progress read mid-loop was briefly mis-called as failed before the loop's verdict landed — the clean run-04 evidence was preserved throughout; lesson: check whether the loop is still running before judging.)*

- **E-054 — node-throw semantics (the DAG substrate COMPLETED)** — closed the LAST unbuilt graph primitive: a node whose cast THROWS. Added `NODE_ERRORED = "errored"` (`run-log.ts` `RUN_OUTCOMES`) + a pure deterministic `erroredSummary(id)` (`graph-core.ts:68` — outcome errored, `materialized: false`, `produced` undefined, `runId` a pure function of `id`) that routes a throw through the EXISTING `decideThread` → halt-dependent-subgraph path (reuse, not reinvent). Threaded into BOTH runners: `runGraph`'s topo for-loop (`try/catch` :237) and `runGraphConcurrent`'s wave (per-thunk catch INSIDE the thunk :471-478, so `Promise.all` never rejects). A thrown cast now appears as a marked `errored` node, its dependent subgraph skips, independent siblings still complete, and the dispatcher ALWAYS resolves to a clean `GraphResult` — never a crashed wave. Dual-runner equivalence proven (`runGraph` == `runGraphConcurrent` for a throwing-node spec). 1218 tests, deterministic stub thunk, no live model. **With this the typed-DAG substrate is COMPLETE — the dispatcher is TOTAL over node behavior: success, non-success outcome, AND exception all resolve to a clean `GraphResult`; nothing stub-gated or unbuilt left on it.** Cycle detection correctly EXCLUDED (already done — `validateDag`/`topoSort` + both runners refuse cycles). The 4th epic Vend self-proposed + self-decomposed via `vend chain` — *honoring the signal's "do not re-propose cycle detection" scoping* — and the first to run on E-053's rational band (propose floored 350k, decompose capped 700k).

## The counter & shelf — the two-gesture surface (P2)

- **E-003 — context-aware shelf** — bare `vend` renders a ranked menu driven by playbooks + live project state, persisted to `.vend/menu.json`. The early CLI of the two-gesture counter.
- **E-030 — value/budget shelf** — `vend shelf`: each play shown with its worth + its *measured* recalibrated envelope (E-013) and confidence, not a static guess.
- **E-031 — board+shelf Home fusion** — bare `vend` renders the DL-6 Home: board leads, shelf recedes, provenance-split ledger at the foot. Press preserved bit-for-bit.

## The gate frame — gates are the contract (P3)

- **E-004 — id-collision guard** — `DecomposeEpic` refuses to materialize on a cross-board id clash (no clobbering live work).
- **E-008 — done-means-committed** — `check:committed` fails on uncommitted source, fired by the `on-stop` hook. Fixed the recurring D-005 andon.
- **E-010 — HEAD-builds gate** — `check:head` verifies the committed HEAD itself builds (isolated worktree), wired to `on-clear`. "Done" = committed *and* builds.
- **E-012 — commit-gate scope-gap** — widened `check:committed` to `.lisa/hooks/` (the gate now polices the hooks that police commits). First epic both minted *and* decomposed by `vend chain`.
- **E-032 — per-play tooling** — a play declares its MCPs/tools (`tools?` on `Play`); binding via a committed portable `.mcp.json`; cast wires only-declared servers (least-privilege); missing required MCP → amber andon.
- **E-033 — per-commit green gate** — `.githooks/pre-commit` runs `check:test`, blocks a red commit naming the failure (fail-closed on red, fail-open on can't-run). Per-commit layer of the frame.
- **E-034 — red-commit audit** — `check:history`: a bounded linear sweep rebuilding+testing each commit in a range to flag any that wasn't green. The post-hoc backstop to E-033; on its first run it found + corrected a real red commit (`a14d96a`).
- **E-043 — idempotent-mint guard** — closed a correctness gap the autonomous loop *demonstrably hit* (E-039 double-minted the doctor epic: E-042 + orphan E-041, same title). Root cause: `proposeEpicEffect` re-mints `nextEpicId`=max+1 each run (TOCTOU-safe, but not idempotent under retry — each run grabs the next slot), and `detectCollisions`/E-004 only catches id *reuse*, never a fresh id for a duplicate *proposal*. Fix: a pure `findExistingByTitle` in `id-guard.ts` + `proposeEpicEffect` **adopts** an existing same-title epic before minting (so a retry converges on one card and the chain decomposes it — no orphan); new-title path + E-004 unchanged. Deterministic proof: running the effect twice with the same card mints once. 1085 tests. *(The complement to E-004: E-004 guards id-reuse, E-043 guards duplicate-proposal.)*
- **E-027 — board-freshness gate** — `vend work` refuses a board staged before the project last moved (pure `isBoardStale` + amber `renderStaleBoard` andon, IA-9 "successful refusal not a crash") with a `--stale-ok` override (IA-5); the refusal lands *before* funding the wallet — exit 1, no cast, no ledger append. Stops the loop clearing already-done/superseded work (the E-025/E-026 stale-board escape). Live proof free + deterministic (`work/T-027-01/stale-board-proof.md`). *(Implementation + proof landed 2026-06-19; the board/frontmatter sweep was completed 2026-06-20 — the prior commit message had over-claimed "done" without flipping them.)*

## The budget contract — budget is a hard contract (P7)

- **E-013 — measured envelopes** — `vend envelope <play>` reads run-log actuals, bounds at the value-tier percentile, de-biases a raw estimate, with honest cold-start/confidence. The budget contract measured, not guessed.
- **E-015 — `--max-turns` bound** — a safety bound at the seam (default 15, override wins), `turnsUsed` logged. (Found: decompose's tail is genuine cost, not turn-sprawl → recalibrate the envelope, not the cap.)
- **E-024 — macro-wallet** — the depleting feature-block wallet (P7) + the autonomous spend loop (P4) + the `vend work --budget` counter gesture. Wired + green; *live multi-cast spend remains a live signal*.
- **E-025 — wallet-priced casts** — threads the recalibrated per-step prediction into the chain so authorization == execution. Verified: the sweep that cleared 0 under the bug now clears 1.

- **E-050 — measurement-funding headroom (the IA-14 auto-widen)** — broke the envelope **censoring ratchet**: `recalibrate` right-censors `budget-exhausted`/`timed-out` runs out of the p90 success sample (correct — they're lower bounds) but then re-funds the next run at that same thin guess, so an under-bounding prior (`decompose-epic`'s 120k) censors forever and never learns its tail (twice confirmed: E-045, E-049). The fix exploits a fact the ratchet wasted — **a censored run already LOGS its actual usage** (the E-049 decompose recorded 264,866 tokens before the 120k wall, a strong lower bound). New pure `fundingEnvelope` (`recalibrate.ts`): when under-calibrated (`source: "prior"` cold-start OR windowed censored-rate ≥ `CENSORED_WIDEN_RATE` = 1/3), the FUNDING envelope a cast runs under is `max(priced, maxCensoredActual × MEASUREMENT_HEADROOM = 2)` per dimension — generous enough to finish and RECORD; trusted-measured ⇒ `funding == priced` (back-compat, well-calibrated plays unchanged). Threaded into the cast funding path (`fundedStepDefault` in the chain's `resolveStepBudgets`; `work.ts`'s propose/decompose), an explicit `--budget`/override still winning. **The quoted PRICE / shelf / `formatEnvelopeLabel` is UNCHANGED — the honest p90 (IA-8); the headroom is a GUARD ≠ price** — the token-dimension twin of E-038's `timeoutMsFor`. P7 holds: the guard is a FINITE multiple of an OBSERVED bound, never unbounded. Deterministic stub-record proof (the E-049 shape now funds to complete-and-record where the old path re-censored), no live model. 1176 tests. The IA-14 actuation `recalibrate.ts:14–16` had deferred — the loop now self-corrects its calibration instead of ratcheting tighter-and-wronger. *(The fix for a finding the dogfood surfaced; hand-authored, since using the broken decompose to author its own fix would be circular.)*

- **E-053 — rational funding band** — clamped the per-cast funding guard (`fundingEnvelope`, E-050) to a rational TOKEN band `[350k, 700k]`: a **FLOOR** (`FUNDING_FLOOR_TOKENS` = 350k — no cast starved by a too-tight p90; fixes the `vend chain` propose that budget-exhausted at 176,101 / 169,873, a 3.6% tail-draw halt) and a hard **CEILING** (`FUNDING_CEILING_TOKENS` = 700k — the P7 wall; runaway self-funding, E-051's decompose to ~733k, capped). Tokens only (wall-clock keeps its E-038 headroom); the band is on the GUARD — the quoted price / shelf / `formatEnvelopeLabel` stays the honest p90 (IA-8), and the macro-wallet still authorizes on price + debits actuals (GUARD ≠ PRICE). Auto-flowed through `work.ts` + the chain (E-050 already threaded `fundingEnvelope`), so it was a clamp in one pure function + an end-to-end confirmation. 1205 tests. **Completes the calibration arc — E-038 (time headroom) → E-050 (cold-start widening) → E-053 (rational bounds both ways): the budget is now a band a human can reason about, not a number swinging 170k↔733k.** *(Hand-authored from a dogfood directive — the budget-flaky `vend chain` can't author its own budget fix.)*

## Trust & consistency — measure before autonomy (P3/P4)

- **E-014 — trust evidence gate** — instrumented walk-away (A2) + gate-variance (A5) from run-log data; returned an honest HOLD rather than fabricate a verdict. (Later un-gated: go.)
- **E-019 — articulation consistency probe** — a 9-cast live sweep over expand/survey/steer; surfaced honest-empty over-fire + unbounded dispersion → three follow-on signals.
- **E-020 — honest-empty recalibration** — prompt-only fix turning abstention into a rare source-gated exception; over-fire eliminated (expand 33→0%, survey 67→0%).
- **E-044 — steer-ranker concrete-demand** — the last E-039 follow-up (both live sweeps ranked a self-referential "run the sweep" #1 that propose can't mint; the operator re-pointed by hand — an unattended one can't, P4). Prompt-only recalibration of `steer.baml` + `survey.baml`: a demand signal must be concrete product demand (decomposes into an epic); operational meta-tasks (run Vend on itself, settle a prior run, dogfood the loop) are process notes — demoted beneath all concrete demand or excluded. The E-020 model: semantic judgment in the prompt, NOT a brittle keyword gate (which would false-positive on legit "improve `vend work`" signals); the structural gates (`steer-core.ts`) unchanged. Deterministic proof: `baml:gen` green + a prompt-contract assertion (1087 tests); live confirmation rides the next steer cast (the E-038→E-039 shape).
- **E-022 — consistency contract** — captured IA-17 (consistency = gated validity, not lexical identity); a semantic-equivalence judge resolved the converge-vs-by-design fork with data.
- **E-023 — survey-convergence** — measure-then-decide said *accept, not build*: survey's semantic head is stable 1.00 (only the tail re-orders). No lever built; IA-17 amended.
- **E-026 — forward-E1 instrument** — `vend work --intervened/--no-intervened` self-measures walk-away. *Forward accrual remains a live signal* (≥10-genuine bar not yet met).
- **E-028 — audit provenance split** — `vend audit` splits walk-away into forward-live vs attested back-fill, so the over-claim (15 carriers read as forward) is structurally impossible.

## The articulation trilogy — automate STEER (the demand-extraction lift)

- **E-016 — expand-fragment** — `vend expand "<fragment>"`: a rough fragment → a grounded, staged, priced signal (read-never-invent). Vend articulates *for* you.
- **E-017 — survey play** — `vend survey`: read a rough project → a ranked staged demand board. `expand` at project scale; the cold-start bootstrap.
- **E-018 — steer play** — `vend steer`: read a project → a ranked board *plus* the genuine human forks. The steer capstone; STEER automated end-to-end.

## Autonomy proven live (P4/P7)

- **E-037 — macro-wallet live proof** — cast the headline gesture for real, once, bounded: a fresh board staged, a bounded wallet funded, `vend work --no-intervened` spent down to a **clean twin P7 stop** (`andon: timed-out`, IA-9 amber — truthful receipt, zero partial state), auth==exec held (E-025, ~72.8s cast ≈ propose-epic's 72,785ms envelope, 0 tokens debited). **The gesture is now WATCHED — P4/P7 demonstrated live, not just wired.** Forward-E1 moved **1/2 → 3/4 (sample 2/10 → 4/10)** on 2 genuine `--no-intervened` records. **But an honest 0-clear:** `propose-epic` time-censored the board's top signal *before* it could mint, so nothing cleared and the go stays **provisional + forward-leaning, NOT forward-confirmed** (the verdict refused the T-026-04 over-count trap). *The epic is done (the proof ran + settled); Frontier 1 stays open — the residual is the propose-epic time-censor + the ≥10 cleared-forward cadence.* (`work/T-037-03/verdict.md`.)

- **E-038 — per-cast timeout headroom** — broke the censoring ratchet E-037's sweep hit: the per-cast wall-clock timeout *was* the recalibrated p90 envelope (`timeoutMsFor` returned `budget.timeMs` verbatim), so a signal heavier than p90 was guillotined → `timed-out` → censored out of the very sample that set the envelope (it couldn't percentile past a tail it censored). Fix: `timeoutMsFor` returns `budget.timeMs × TIMEOUT_HEADROOM (=2)` — the runaway-guard gets slack while price/affordability/shelf keep reading the honest p90 (IA-8); P7 holds (macro wallet still hard-stops on actuals). Deterministic proof maps E-037's ~72–73s censored runs onto the headroomed wall (both finish, no guillotine at 72.8s) while `canAfford` gates on bare T; constant pinned ≥2 (anti-drift); IA-14 (auto-widen on censored rate) named as the deferred rung. 1000 tests. *(Removes the guillotine deterministically; the live confirmation that the heavy signal now clears is Frontier 1's next pull.)*

- **E-039 — macro-wallet live CLEAR** — the payoff of E-037+E-038: re-ran the bounded metered sweep (`vend work --no-intervened`, ~1h/1M) and the wallet **cleared 2 real pulls** — `vend init`→E-040, `vend doctor`→E-042 (each minted as a real epic+tickets, `lisa validate` green). **`propose-epic` finished at 93s / 82s — past the 72,785ms wall that censored E-037 ⇒ E-038 proven LIVE.** Clean P7 wallet-exhausted stop, auth==exec held (E-025). Forward-E1 moved on **cleared** (not censored) evidence for the first time: **0→4 cleared records** (ledger 30–33, `intervened:false`+`success`), forward sample 4/10→8/10 (`vend audit` 88% 7/8). **The headline gesture is now WATCHED CLEARING, not just refusing.** Honest boundary held: 2 cleared ≠ the ≥10 bar — go stays **provisional + forward-leaning, NOT forward-confirmed**. Surfaced + adjudicated a `propose-epic` double-mint (orphan E-041, a childless duplicate of E-042 — deleted). (`work/T-039-02/verdict.md`.)

- **E-045 — forward-E1 cadence sweep (≥10 bar MET)** — the bounded `--no-intervened` sweep that crossed the trust bar provisional since E-014. Forward (live) **8 → 10** records (composition **5 cleared / 5 censored / 1 genuine `--intervened`**); the pre-wired E-014/E-026 rule (≥10 forward self-reports) is **met** — the macro-wallet's go upgrades **provisional → bar-met**. Honest caveat held (no over-claim): **9/10 ran untouched, only 1 genuine intervention** ⇒ *"didn't-break," not "stress-tested"* — bar-met, NOT "bulletproof." Forward-only numbers (the 23-report combined pool is not the basis — the T-026-04 trap avoided). **Live-confirmed the hardening:** E-044 (the sweep's #1 was the *typed DAG*, a concrete frontier — no self-referential meta-task) + E-043 (single clean mint, no orphan). The clear was high-**quality** — a sound, well-grounded epic for a real frontier (E-046). **Next cadence target: genuine-intervention *depth* (still 1), not more walk-aways.** (`work/T-045-02/`.)

- **E-051 — disallow AskUserQuestion on autonomous casts** — closed the last manual-babysit failure mode: an unattended `claude -p` cast could call Claude Code's built-in `AskUserQuestion`, which has no answerer on a piped cast and hangs it (E-049's decompose hit this live — the agent improvised a mid-decompose question and stalled the pane). Added a symmetric `disallowedTools` option to `buildArgs`/`DispenseOptions` → `--disallowedTools` (spelling verified against `claude -p --help`, variadic), under the same empty-omitted-emits-nothing back-compat as E-032's `--allowedTools`; a `PlayTools.deny` denylist (orthogonal to strict MCP scoping — a deny-only declaration stays passthrough) + an `AUTONOMOUS_DENY` constant route the `AskUserQuestion` deny through the autonomous plays ONLY (propose/decompose/work), interactive/TUI byte-identical. Deterministic (`buildArgs` PURE — argv assertions, no live model). 1188 tests. **The SECOND epic Vend self-PROPOSED + self-DECOMPOSED via `vend chain` — and the run that minted it was the first FULLY UNATTENDED chain: the decompose self-funded via E-050 (~733k, no manual budget) and didn't hang. Both failure modes the dogfood surfaced (budget → E-050, tools → E-051) are now closed — `vend chain` is unattended end-to-end.**

## Distribution & onboarding (P2/P5) — built by Vend, autonomously

- **E-040 — `vend init` scaffold** — the first product epic **the macro-wallet minted *and* the loop then built end-to-end** (E-039 cleared it; the loop implemented all 4 tickets). `vend init` scaffolds a driveable vend+lisa project (refuse-or-apply, idempotent — re-runnable without clobber), proven `lisa init → lisa-valid`. The foundation of Frontier 7 (getting Vend onto a fresh machine).
- **E-042 — `vend doctor` preflight** — a preflight probe (envinfo-backed `Check`/`renderDoctorReport`) that reports environment readiness, wired as a CLI command **and reused as a cast precondition guard**. Also autonomously cleared by E-039's sweep + built by the loop. (+71 tests across E-040+E-042; gate green at 1071.)

## Executor-agnostic underneath (P6)

- **E-035 — second executor** — extracted the `Executor` interface + `executorFor` selector; refactored Claude behind it (byte-identical) + added a non-Claude `OpenAICompatExecutor` (`/v1/chat/completions`). Vend is an orchestrator, not a Claude wrapper. (Single-completion adapter — *not* agentic open-model parity; that's a live signal.)
- **E-036 — open-model BAML support** — the authoring-layer half: `OpenModelStub` (provider `openai-generic`, render-only) so BAML's `b.request.*` targets open models; proven the *request shape* differs from anthropic (`/chat/completions` vs `/v1/messages`, no `max_tokens`, flat-string content) while `b.parse.*` stays provider-agnostic; render-client follows `VEND_EXECUTOR` (one switch drives dispatch + render). Boundary recorded in `stack.md`: config-level at both layers, the **live agentic runtime** deferred. 998 tests. (The live runtime is a live signal — Frontier 2.)

## Presentation (P5)

- **E-021 — presentation surface** — `src/present/`: graph loader → spec → vocab-translate → projection → paper/Mermaid renderer, one-way-authority guarded. Rendered the 61-ticket board into a jargon-free designer projection. (Linear renderer + annotation round-trip are downstream live signals.)

- **E-055 — projection-to-SVG renderer** — the unblocked visual surface for Frontier 4 (the Linear renderer is MCP-blocked): a THIRD consumer of E-021's `Projection` IR (beside `paper.ts`), rendering the work-graph to a large static SVG. `projectionToSvg(projection, overlays?)` is PURE, mirroring `paper.ts`'s contract exactly (honest-empty, never writes the projection back, MCP-independent, no live model); built on pure SVG primitives + a deterministic hand-rolled grid/swimlane geometry (NO force-directed/external engine; no fs/clock/random ⇒ byte-identical renders); a `vend svg` CLI seam (`loadGraph` → `projectGraph` → write `.vend/work-graph.svg`, authority-guard green). **Live-proven:** `vend svg` rendered the real board to a valid SVG — 62 groups, 133 cards, 89 links → exactly 133 `<rect>` (one/card), 89 `<line>` (one/`depends_on`), 195 `<text>` (133 card + 62 group labels). 1272 tests. The visual half of the non-dev round-trip (P5) as a portable offline file — no Linear MCP needed (live Linear stays deferred; the annotation→demand round-trip is the complement). The 5th epic Vend self-proposed + self-decomposed via `vend chain`, honoring every scope pin (deterministic layout, mirror paper.ts, Linear/round-trip named out-of-scope).

- **E-056 — SVG glanceable default + edges-as-payload** — turned E-055's 62-column / 17128px strip into the glanceable, decision-bearing board the personas need. (1) **Glanceability:** flipped the non-dev `DESIGNER_PRESET.groupBy` from the fine `story` axis to coarse `status` (≈5 columns) — every grouping stays in `GROUPINGS`, only the default changed; `DEV_PRESET` keeps a finer axis. (2) **Edges-as-payload:** enriched `ProjectionLink` with a `blocked` flag (computed PURELY in `projectGraph`/`buildLinks` from the frozen graph — blocked = upstream `from` not done), and `projectionToSvg` weights blocked edges heavy/red (`EDGE_BLOCKED` #E53935 × 4 stroke) while satisfied deps stay light — so "what to unblock first" pops (Devraj reads edges before boxes — his funding map). Pure, byte-identical, one-way-authority, MCP-independent, no live model. 1281 tests. **Persona-grounded** (`pm/persona-research.md`): makes the render-and-watch validation finally runnable. The 6th epic Vend self-proposed + self-decomposed via `vend chain`, honoring every scope pin. *(On a 100%-done board `status` collapses to one column with no blocked edges — the glanceability + edge-weight show on a live mid-flight board; `leverage` may glance better on a maturing board, a boarded tuning thought.)*

- **E-057 — annotation→demand round-trip** — closed Frontier 4's non-dev loop: a `vend annotate <node-id> "<feedback>" [--seat designer|dev]` gesture casts an annotation-bearing `expand-fragment` that STAGES a priced, provenance-bearing `Signal` under `pm/staged/` — never the board (the one-way-authority "stage-not-promote" contract inherited from E-016, not rebuilt). `renderStagedSignal(signal, annotation?)` appends the HUMAN origin (seat X on node Y) + a back-link after the machine-origin trailer; the annotation is an OPTIONAL argument (absent for a plain `vend expand` — pure reuse, not a fork). A teammate steers without touching code: feedback returns as proposed demand a steward pulls. 1295 tests, deterministic (stubs the cast like expand's tests). **Maya's telephone-killer — and with E-055/E-056/E-057 the non-dev round-trip is WHOLE: render the board → glanceable + blocked-edge weight → annotate → priced demand.** The 7th epic Vend self-proposed + self-decomposed via `vend chain`, honoring the reuse scope (named the live comment-fetch + the proposed→pulled dashboard as follow-ons).

## Infrastructure & knowledge

- **E-002 — CI structural backstop** — `/ci` Dagger gate: `dagger call test run` runs `check:test` in-container, drift-free.
- **E-005 — thread the real model id** — `runs.jsonl` logs the true `claude-opus-4-8[1m]`, not `claude-cli-default`. Data fidelity.
- **E-029 — design language** — captured `docs/knowledge/design-language.md` (DL-1…DL-9): the clean-typographic visual spine a TUI implements against.
- **Information architecture** — captured `docs/knowledge/information-architecture.md` (IA-1…IA-17): the recommendation-first home, the cold-start arc, the Counter, the andon-as-refusal stance, the recalibration loop. (Remaining design threads are live signals.)
- **Measurement sprint** (2026-06-19) — collected both trust numbers (E1 100%/13 back-fill, E2 21% variance cut); un-gated the macro-wallet to **go** (provisional, forward-leaning).

---

*Kaizen one-liners that cleared (from E-001's first live runs): cross-board id guard (→E-004), `--max-turns` (→E-015), real model id (→E-005), wallet-priced casts (→E-025), per-commit green gate (→E-033/E-034). All folded into the epics above.*
