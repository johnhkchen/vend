---
id: T-900-01
story: S-900-01
title: implement-word-count
type: task
status: done
priority: high
phase: done
depends_on: []
---

## Context

Implement `mdwc`: read a Markdown file path from `argv`, strip the markup, count
the words, print the integer, exit `0`. This ticket delivered the whole tool. It
is **done** — the behavior shipped and is captured here. The scope is frozen (see
the charter); there is no follow-up work.

## Acceptance Criteria

- [x] `mdwc file.md` prints the integer word count and exits `0`.
- [x] An empty or absent file prints `0`, never an error.
- [x] One file, no dependencies, no flags.
