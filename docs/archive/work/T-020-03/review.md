# T-020-03 — Review: recalibrate-survey-honest-empty

> The handoff document — what a reviewer needs to understand the work without reading every diff.
> Summarizes the change, evaluates coverage, surfaces open concerns. Implements `plan.md`.

## What changed (one screen)

A **prompt-only** recalibration of `baml_src/survey.baml` — the survey-side twin of the merged
T-020-02 (expand). Two edits, one authored file:

1. **`## Honest-empty (IA-4)` prompt section** — rewritten from a co-equal "If the project grounds NO
   real demand gradient … ABSTAIN / Otherwise author" branch into a **rare, source-gated exception**:
   a concrete "can you read even ONE real demand off the project — an open item, a TODO, a run-log
   fact, a vision-distance not yet closed?" test, plus a **board-scale calibrated example pair** (a
   complete/frozen all-`done` project → ABSTAIN with an empty board; a project with open work /
   vision-distance → STAGE the ranked board), and an explicit "do NOT abstain because the board is
   large, partly-captured, or saturated."
2. **`Board.signals` `@description`** — tightened so the type-level instruction the model sees via
   `{{ ctx.output_format }}` matches the prose ("EMPTY ONLY when the project is complete/frozen and
   grounds NO demand … a project with ANY open work or vision-distance stages a ranked board").

**Files:**
- `baml_src/survey.baml` — modified (commit `6fed23e`).
- `baml_client/**` — regenerated by `baml:gen`; **gitignored**, not committed (propagates at
  build/check time; `complete/frozen` text verified present in `inlinedbaml.ts`).
- `docs/active/work/T-020-03/sweep-logs/{survey,survey-thin}.log` — live-probe AC evidence.

**Deliberately NOT changed:** `src/play/survey-core.ts` (+ test), `src/play/survey.ts`,
`src/probe/run-consistency-probe.ts`, `baml_src/expand.baml`. The over-firing is a model decision
shaped by the prompt, not a gate bug (design Option A rejected); the `survey`/`survey-thin` probe
targets already existed (T-020-01). No field names, types, or enum touched, so the generated
`Board`/`Signal`/`SignalTier` shape is byte-stable.

### One structural note for the reviewer

Survey **reuses** expand's `Signal`/`SignalTier` type (never redefined). T-020-02 already tightened
`Signal.what`/`.why`'s blank-clause descriptions, so survey inherits the per-signal recalibration for
free — this ticket only needed the board-scale prompt section + the `Board` wrapper's description
(no "Edit 3"). That is why this diff is smaller than its T-020-02 twin.

## Test coverage

| Surface | Coverage | Status |
|---|---|---|
| Typed shape unbroken (`Board`/`Signal`/`SignalTier`) | `bun run check` — `baml:gen` → `tsc --noEmit` → `bun test` | ✅ **639 pass / 0 fail**, tsc clean, baml:gen clean |
| Gate semantics intact (`honest-empty`/`read-never-invent`/`leverage-rank`) | `survey-core.test.ts` (unchanged) | ✅ green within the 639 |
| Demand-rich board stages signals (HE ~0, was 67%) | probe `survey` N=2 — headline mix | ✅ **HE 0% (0/2)** — signal/signal, flip gone |
| Complete/frozen board still abstains (negative control) | probe `survey-thin` N=2 — ≥1 marker | ✅ **HE 100% (2/2)** — still abstains |

No new unit tests: the change is prompt prose + a field `@description` (BAML prompts are authoring
surfaces, not unit-tested per the house rule; their tested core, `survey-core.ts`, is unchanged). The
existing `survey-core.test.ts` is the regression guard and stays green.

## AC verification — directional live probe

The AC is directional (live, non-deterministic casts, small N per E-014). **How to read survey's
result:** unlike expand (whose honest-empty STOPs and folds into `budget-exhausted` — the D4
asymmetry), survey's honest-empty is a CLEAR+marker `success`, so its rate shows **directly in the
headline consistency-report mix** (`honest-empty rate N%`). Read that line, not the raw tally.

**Baseline (T-019-02, N=3):** `honest-empty rate 67%` — outcome flipped HE / signal / HE on the
identical demand-rich live board.

**This change (N=2 each), completed:**

```
survey      (grounded live board):  signal 2 · honest-empty 0 · budget-exhausted 0  → honest-empty rate 0%
survey-thin (complete/frozen board): signal 0 · honest-empty 2 · budget-exhausted 0  → honest-empty rate 100%
```

Reproduction (exact):
```bash
bun run src/probe/run-consistency-probe.ts survey 2
bun run src/probe/run-consistency-probe.ts survey-thin 2
```

**Result — AC met (directionally).** Grounded `survey`: **honest-empty 67% → 0%** (0 of 2), and both
casts staged a signal (`signal → signal`) — the run-to-run **flip is gone** on the identical
demand-rich board (the unambiguous target). Negative control `survey-thin`: **honest-empty 100%**
(2 of 2 abstained with the "no demand staged" marker) — the complete/frozen board still abstains, so
the gate was **tightened, not disabled**. Both arms ran clean (`raw run-log outcomes: success 2`
each; zero budget-exhaustion — the E-018 budgets remain vindicated). Logs: `sweep-logs/{survey,
survey-thin}.log`.

One honest caveat (IA-8): the two grounded signals diverge at **content dispersion 0.75** — high
run-to-run *content* variance. That is the *separate* E-019 "bound signal dispersion" concern, NOT
this ticket's honest-empty over-firing target (which is resolved). Recorded here so it is not lost.

## Open concerns / limitations

- **Live-probe nondeterminism + fuzzy groundedness.** Survey's grounded board is genuinely fuzzier
  than expand's authored fragment: a large, well-captured board *can* defensibly read as "saturated →
  nothing new to stage." So a residual honest-empty at small N is not automatically a failure. The
  clean signal is the *disappearance of the run-to-run flip* on identical demand-rich input. A larger
  re-sweep (N≥5) would firm this up — out of scope for this ticket, a candidate kaizen follow-up.
- **Survey's signal-content dispersion remains unmeasured** (the T-019-02 n=1 signal-arm caveat). This
  ticket targets *outcome* over-firing (honest-empty), not *content* convergence — that is the
  separate E-019 "bound signal dispersion" signal, not this one.
- **The `survey` probe reads the current repo board**, whose demand gradient is whatever exists at
  cast time. It is demand-rich today; if the board were ever fully cleared the grounded arm's
  expected outcome would legitimately shift toward abstention. Not a concern now, noted for future
  re-sweeps.
- **No code/gate change** means the IA-4 contract and gate semantics are provably untouched (639
  green) — the risk surface is confined to model behavior, which the negative control guards.

## Verdict

Deterministic bar **met** (639 green, no shape drift; committed `6fed23e`). The AC is directional
live evidence captured under `sweep-logs/`; the recalibration is the minimal, reversible,
correct-layer change consistent with its merged T-020-02 twin. Handing off.
