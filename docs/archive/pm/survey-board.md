# Survey — staged demand board

A ranked board read off the whole project, highest-leverage first. Un-promoted: review and pull a row.

| Signal | Value | Budget (envelope) | Status |
|---|---|---|---|
| **Add conditional edges to the typed DAG — branch the graph on a node's result (take edge A vs B depending on what the upstream node returned), with deterministic semantics in runGraph/castGraph.** — The v1-vision centerpiece beyond fan-out/join: it's what lets a playbook encode branching judgment ('if the gate fails, route to repair; else continue'). Without it the graph can only fan out and join, not decide. Unblocks real playbook control flow and is the substrate the open-model runner sits on. | **Keystone** | ~1 block (≈2h) | ready (advances [graph-structured orchestration (the core feature / v1 vision), P3] · grounded in demand.md Frontier 3 'conditional edges (branch on a node's result)'; E-046.md line 32 names it the first out-of-scope follow-on epic on the now-built substrate (dag-core/graph-core/graph).) |
| **Cross-branch budget accounting — make the macro-wallet (P7) span parallel DAG branches, so a fan-out's concurrent casts draw from and respect one shared envelope with a correct hard stop.** — E-046 added concurrency (castGraph) but budget is still single-chain. Real concurrency without a shared wallet means the P7 hard contract leaks across branches. Ties the new graph substrate back to the budget invariant the whole product rests on. | **High** | ~1 block (≈2h) | blocked: wants E-047 (live real-play graph) landed to exercise real parallel branches (advances [P7] · grounded in demand.md Frontier 3 'cross-branch budget accounting (the macro-wallet across parallel branches)'; E-046.md line 32 'budget accounting across parallel branches' (named follow-on); E-045 decompose-budget finding.) |
| **Agentic open-model runner (P6 end-to-end) — a local agent loop behind the Executor interface where an open model autonomously reads the repo, runs tools, iterates turns, and materializes a ticket.** — E-035/E-036 made the execution + authoring seams config-capable, but no open model has yet *autonomously* cleared work. This is the real lift that proves model-plurality is real, not just declared — closes the largest remaining P6 vision-distance. | **High** | several blocks | blocked: likely wants Frontier 3 (conditional edges + cross-branch budget) underneath it (advances [P6] · grounded in demand.md Frontier 2 'Agentic open-model runner (the deferred remainder of E-035/E-036)'; E-046.md line 32 names 'the open-model runner itself (the P6 consumer, downstream)'.) |
| **Driveable hackathon examples/ template — a brand-new user clones it, runs the loop end-to-end in one short session, and experiences stepping out of the loop.** — The product's value only clicks once you've felt the engineer-out-of-the-loop shift; telling doesn't do it, showing does. Compounds the keystone autonomy loop — more driveable projects → more cleared runs → more forward-E1 evidence — and doubles as a product-level consistency probe. | **High** | ~1–2 blocks | ready (advances [P5, core feature: more driveable projects feed the clearing/trust loop] · grounded in demand.md Frontier 7 'a driveable hackathon examples/ template'; pm/onboarding-examples-discovery.md §'the value only clicks once you've seen the loop'; pm/brief-hackathon-example.md.) |
| **Linear renderer — project the work-graph (epics/stories/tickets) into Linear for non-dev teammates, read-only and one-way (Vend keeps authority).** — The projection layer ships (E-021); this opens Vend to visual-thinking, non-dev teammates without violating N2/N4 — the first half of closing the non-dev round-trip (P5). | **High** | ~2 blocks | ready (advances [P5] · grounded in demand.md Frontier 4 'Linear renderer'; pm/linear-surface-mock.md and pm/linear-surface-prep.md (staged design).) |
| **Annotation → demand round-trip — a teammate's annotation on the projected graph flows back into the demand board as a priced signal, reusing expand-fragment.** — Completes the non-dev loop (P5): not just read-only projection out, but intent flowing back in and getting cleared. Reuses the existing expand-fragment articulation path, so the lift is mostly wiring. | **High** | ~1–2 blocks | blocked: downstream of the Linear renderer (needs a surface to annotate) (advances [P5, core feature: clearing raw intent into priced demand] · grounded in demand.md Frontier 4 'Annotation → demand round-trip — reuses expand-fragment'.) |
| **Delivery packaging — Homebrew formula and/or `bun --compile` single-binary so Vend installs onto a fresh machine in one command.** — Frontier 7's distribution half: the examples template teaches the loop, but a new user can't drive it until Vend is on their machine. Direct P5 (local-first, usable on one machine) and the foundation under all onboarding. | **Standard** | ~1 block (≈2h) | ready (advances [P5] · grounded in demand.md Frontier 7 'Homebrew / bun --compile delivery'; pm/deployability-discovery.md; pm/PRD-distribution-onboarding.md.) |
| **Per-play executor / BAML-client selection — a play declares which model/executor it runs on.** — Natural successor to E-032 (per-play tooling) + E-035/E-036's selection seams; lets a playbook route cheap screening vs hard decomposition to the right model at authoring time. Advances executor-agnosticism concretely (P6). | **Standard** | ~1 block (≈2h) | ready (advances [P6, P1] · grounded in demand.md Frontier 3 'Per-play executor / BAML-client selection — the natural successor to E-032 + E-035/E-036'.) |
| **Thread the structured stop-reason onto the run record — fold honest-empty vs budget-exhausted into a countable field on the run log instead of stdout-only.** — Today the probe + `vend audit` can't split honest-empty from budget-exhausted because the stop-reason is stdout-only. Threading it makes consistency/trust measurement countable — the measurement the whole gate-contract story rests on. Small and ready. | **Standard** | small (~1h) | ready (advances [P3, P7] · grounded in demand.md Frontier 6 'Thread the structured stop-reason onto the run record … Ready — small (~1h)'.) |
| **Mana economics — stable→variable prompt ordering in the dispense (shared [play+charter+KB] prefix at ~0.1×) plus per-function model routing in BAML (cheap client for screening, Opus for hard decomposition).** — Spends well *for* the author: cache-friendly ordering and sub-play model routing cut per-run cost while gates stay in code. Direct payoff on P7 economics and the auto-structure-for-efficiency product goal. | **Standard** | ~1–2 blocks | ready (advances [P7, core feature: spend well for the author] · grounded in demand.md Frontier 6 'Mana economics' (stable→variable prompt ordering; per-function model routing in BAML); mana-economics.md.) |
| **Fleet/DAG andon board — a live TUI surface to watch a parallel fleet of casts and surface andons (escalations) as they fire.** — Pairs directly with Frontier 3 now that castGraph runs real concurrency: without a watch surface, a parallel fleet is opaque. Makes the 'walk away but be told when it stops' half of P4 observable. | **Standard** | ~1–2 blocks | blocked: design pull first (advances [P4] · grounded in demand.md Frontier 5 'Fleet/DAG andon board — pairs Frontier 3. Design pull first.') |
| **Detached/notify mechanism — kick off a run, detach, and get notified on settle.** — Makes 'walk away' literal at the UI (P4): the run proceeds against its gates with no terminal babysitting and pings you only on settle. The core walk-away UX gesture the vision promises. | **Standard** | ~1 block (≈2h) | blocked: design pull first (advances [P4] · grounded in demand.md Frontier 5 'Detached/notify mechanism … Standard (design pull first)'.) |
| **Confirm's budget-adjust gesture — tune the time/token envelope at the counter before committing the run.** — Small refinement to the two-gesture transaction (P2/P7): lets the human set the envelope at pick-time without pushing config back to authoring. Narrow blast radius. | **Leaf** | small (~1h) | ready (advances [P2, P7] · grounded in demand.md Frontier 5 "Confirm's budget-adjust gesture — tune the envelope at the counter".) |
| **`git bisect run` integration in the history audit — use bisect to pinpoint a single red commit instead of E-034's linear sweep.** — Faster backstop for the per-history gate when one commit went red; narrow, unblocks nothing else, but cheap consistency-tooling win. | **Leaf** | small (~1h) | ready (advances [P3] · grounded in demand.md Frontier 6 '`git bisect run` integration — E-034's audit does a linear sweep; bisect is the faster backstop. Leaf.') |

## Pull these

A human pulls any one staged signal onto the board with one gesture:

```
vend chain "Add conditional edges to the typed DAG — branch the graph on a node's result (take edge A vs B depending on what the upstream node returned), with deterministic semantics in runGraph/castGraph. — The v1-vision centerpiece beyond fan-out/join: it's what lets a playbook encode branching judgment ('if the gate fails, route to repair; else continue'). Without it the graph can only fan out and join, not decide. Unblocks real playbook control flow and is the substrate the open-model runner sits on."   # recommended next pull (highest leverage)
vend chain "Cross-branch budget accounting — make the macro-wallet (P7) span parallel DAG branches, so a fan-out's concurrent casts draw from and respect one shared envelope with a correct hard stop. — E-046 added concurrency (castGraph) but budget is still single-chain. Real concurrency without a shared wallet means the P7 hard contract leaks across branches. Ties the new graph substrate back to the budget invariant the whole product rests on."
vend chain "Agentic open-model runner (P6 end-to-end) — a local agent loop behind the Executor interface where an open model autonomously reads the repo, runs tools, iterates turns, and materializes a ticket. — E-035/E-036 made the execution + authoring seams config-capable, but no open model has yet *autonomously* cleared work. This is the real lift that proves model-plurality is real, not just declared — closes the largest remaining P6 vision-distance."
vend chain "Driveable hackathon examples/ template — a brand-new user clones it, runs the loop end-to-end in one short session, and experiences stepping out of the loop. — The product's value only clicks once you've felt the engineer-out-of-the-loop shift; telling doesn't do it, showing does. Compounds the keystone autonomy loop — more driveable projects → more cleared runs → more forward-E1 evidence — and doubles as a product-level consistency probe."
vend chain "Linear renderer — project the work-graph (epics/stories/tickets) into Linear for non-dev teammates, read-only and one-way (Vend keeps authority). — The projection layer ships (E-021); this opens Vend to visual-thinking, non-dev teammates without violating N2/N4 — the first half of closing the non-dev round-trip (P5)."
vend chain "Annotation → demand round-trip — a teammate's annotation on the projected graph flows back into the demand board as a priced signal, reusing expand-fragment. — Completes the non-dev loop (P5): not just read-only projection out, but intent flowing back in and getting cleared. Reuses the existing expand-fragment articulation path, so the lift is mostly wiring."
vend chain "Delivery packaging — Homebrew formula and/or `bun --compile` single-binary so Vend installs onto a fresh machine in one command. — Frontier 7's distribution half: the examples template teaches the loop, but a new user can't drive it until Vend is on their machine. Direct P5 (local-first, usable on one machine) and the foundation under all onboarding."
vend chain "Per-play executor / BAML-client selection — a play declares which model/executor it runs on. — Natural successor to E-032 (per-play tooling) + E-035/E-036's selection seams; lets a playbook route cheap screening vs hard decomposition to the right model at authoring time. Advances executor-agnosticism concretely (P6)."
vend chain "Thread the structured stop-reason onto the run record — fold honest-empty vs budget-exhausted into a countable field on the run log instead of stdout-only. — Today the probe + `vend audit` can't split honest-empty from budget-exhausted because the stop-reason is stdout-only. Threading it makes consistency/trust measurement countable — the measurement the whole gate-contract story rests on. Small and ready."
vend chain "Mana economics — stable→variable prompt ordering in the dispense (shared [play+charter+KB] prefix at ~0.1×) plus per-function model routing in BAML (cheap client for screening, Opus for hard decomposition). — Spends well *for* the author: cache-friendly ordering and sub-play model routing cut per-run cost while gates stay in code. Direct payoff on P7 economics and the auto-structure-for-efficiency product goal."
vend chain "Fleet/DAG andon board — a live TUI surface to watch a parallel fleet of casts and surface andons (escalations) as they fire. — Pairs directly with Frontier 3 now that castGraph runs real concurrency: without a watch surface, a parallel fleet is opaque. Makes the 'walk away but be told when it stops' half of P4 observable."
vend chain "Detached/notify mechanism — kick off a run, detach, and get notified on settle. — Makes 'walk away' literal at the UI (P4): the run proceeds against its gates with no terminal babysitting and pings you only on settle. The core walk-away UX gesture the vision promises."
vend chain "Confirm's budget-adjust gesture — tune the time/token envelope at the counter before committing the run. — Small refinement to the two-gesture transaction (P2/P7): lets the human set the envelope at pick-time without pushing config back to authoring. Narrow blast radius."
vend chain "`git bisect run` integration in the history audit — use bisect to pinpoint a single red commit instead of E-034's linear sweep. — Faster backstop for the per-history gate when one commit went red; narrow, unblocks nothing else, but cheap consistency-tooling win."
```

_Staged by Vend's `survey` play — not promoted; pull to clear._
