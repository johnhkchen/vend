# Archived desk artifacts

Moved here 2026-07-11 during a board tidy — these staged boards (2026-06-19..29) were
mostly drained by the E-060..E-070 arc and superseded by a fresh `vend steer` cast the
same day (see `docs/active/pm/staged/steer.md`).

Still-open items noted at archive time, for the fresh board to re-rank on its own evidence:

- **Conditional edges on the typed DAG** (survey-board keystone) — fan-out/join shipped
  (E-046); branch-on-result is still unbuilt, and cross-branch budget accounting + the
  open-model runner sequence behind it.
- **Expand budget recalibration** (recalibrate-vend-expand-…) — its 211k data point is in
  pre-E-068 parity units; re-measure under the price-true meter before pulling.
- **Hackathon `examples/` template**, **Linear renderer / annotation round-trip** — the
  Linear signal stays live in `docs/active/pm/staged/graph-view-human-projection.md`
  (deliberately parked, vision-consequential).
- proposed-batch.md's #1 (live-verify the Set-B round-trip) was overtaken by the
  honey-kitchen Phase-2 drive; its honest residue is the forward-E1 0/10 counter-signal
  (see memory / tooling-feedback), a desk question, not a ticket.
