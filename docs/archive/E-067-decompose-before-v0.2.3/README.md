# E-067 decomposed by vend v0.2.3 — the pre-E-066 "before"

A frozen copy of what the shipped v0.2.3 `decompose-epic` play produced for `E-067`
(`snapshot-charter-codes-at-cut`) on 2026-07-10, kept as the BEFORE half of the E-066
story-contract live proof. The story body is the ten-line shell E-066 exists to fix.

- `E-067.md` — the epic card as proposed (identical input to both decomposes).
- `S-067-01.md` — the shell story (body: one provenance line).
- `T-067-01-0{1,2,3}.md` — the tickets as v0.2.3 cut them.
- `runs-excerpt.jsonl` — the two ledger records (propose $0.461 / 47 s; decompose
  $1.153 / 87 s / 7 turns, gates: value·allocation·bounds·structural, no
  story-completeness gate yet).

The AFTER — the same `E-067.md` card re-decomposed by the E-066 build (contract story,
`story-completeness` gate green, $1.242 / 108 s / 3 turns) — is the live set in
`docs/active/stories/` and `docs/active/tickets/`. Side-by-side comparison:
https://claude.ai/code/artifact/36c8058c-ca4d-4abb-ad8c-72f124bfce41
